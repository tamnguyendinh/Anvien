# Notes / Decisions Log - 2026-08-11

## Child 01 P1-C - Identity and Occurrence Conservation

- Scope: implemented only `P1-C` from `docs/plans/2026-07-26-anvien-graph-accuracy-multi-plan/2026-07-28-01-graph-identity-contract-and-strict-construction`.
- Decision: consume the current source-backed provider occurrence ID, lexical `scopeByDef` membership, label/meaning, owner ID, semantic name, optional arity, and normalized repository-relative file at the existing `graphIDForDef`/`buildWorkspace` owner. Tuple fields are length-prefixed; no hash, new topology/package, random/absolute/order input, or provider rewrite was introduced.
- Preservation: providers, TSJS binding patterns, projection source, `Graph.AddNode`, relationship merge, persistence/readers, later Children, and `E:\cheapapp.org` were not changed.
- Validation: focused identity matrix PASS `4/4`; resolution and provider packages PASS; full build PASS; built CLI fixture oracle PASS `10/10` occurrence conservation with `time 2/2`, `now 2/2`, separate `Shared` meaning lanes, and `0` missing endpoints; product regression PASS `61/61` packages after stale literal-ID QA alignment.
- Evidence: `docs/plans/2026-07-26-anvien-graph-accuracy-multi-plan/2026-07-28-01-graph-identity-contract-and-strict-construction/2026-07-28-01-graph-identity-contract-and-strict-construction-evidence.md` (`E1-P1C-IMPACT1`, `E1-P1C-SOURCE1`, `E1-P1C-BUILD1`, `E1-P1C-TEST1`, `E1-P1C-ORACLE1`).
- Coder report: `reports/coder/rp_coder_260811_083419_by_gpt-5-codex_child01_p1c_identity_occurrence.md`.
- Review state: first zero-trust review cleared the technical boundary but REJECTed stale actual-status prose; history-closure re-review PASSed with unchanged source/test hashes and `50/50` consistency assertions. Nine exact current-slice `.tmp` artifacts were removed while historical work was preserved.
- Detect state: fresh staged all-scope detect-changes PASSed at risk `medium` with `223` changed symbols / `19` accepted paths / one affected process, and zero persisted ResolutionGap, degraded node, or node-with-gap result. The isolated P1-C commit follows this ledger state.
- Later gates at P1-C closure: P1-D remained closed until commit `df0d7b7b`; the P1-D opening is recorded below. P1-E target validation, Child 02 persistence/readers, and target source remain closed.

## Child 01 P1-D - Canonical Definition Collision Pre-flight

- Git gate: P1-C is committed as `df0d7b7b753620f56c932f0e13391c1c016a8f72`; the worktree was clean before P1-D graph work began.
- Fresh graph: final post-plan-refresh inventory is `1,570` scanned, `679` parsed-code, `0` failed, `95,577` nodes, and `134,444` relationships.
- Blast radius: exact UID impacts are CRITICAL for `Graph.AddNode` (`296` symbols / `70` files / `9` modules / `77` processes), `Graph.init` (`314/69/11/84`), `emitter.emitNode` (`6/5/2/25`), `emitDefinitionNodes` (`24/9/7/33`), and `ResolveBoundInto` (`78/35/24/33`). CRITICAL is treated as a scope warning.
- Caller inventory: full inbound evidence reports `142` direct `AddNode` calls, split into `126` test calls and `16` production callsites across exactly 11 production families.
- Decision: global `AddNode` is a mixed mutation boundary; File metadata, diagnostics, structure, and resolution File nodes require evidence-backed enrichment/update. P1-D therefore preserves generic mutation and the ten non-resolution families, and edits only canonical Definition conflict detection plus minimum `ResolveBoundInto` fail-clear propagation.
- Closed boundaries: relationships, persistence/readers, non-resolution producer identity, P1-E, target execution, and later Children remain closed.

## Child 01 P1-D - Implementation Candidate, Validation, and First Supervisor Rejection

- First candidate review: zero-trust source inspection rejected an asymmetric subset comparison that could accept a same-batch canonical property removal only in one order; QA review also found two helper-only tests and a vacuous endpoint loop. No completion claim was made.
- Corrected source boundary: the emitter now tracks canonical Definition nodes seen in the current invocation. Same-batch reinsertion is exact-only in either order; a compatible pre-existing graph superset may retain its additional enrichment. Global mutation, File enrichment, relationships, and non-resolution callers remain unchanged.
- Corrected QA boundary: conflict/enrichment cases now use `ResolveInto`, compare the complete pre-existing node and exact node cardinality, test optional canonical-property drift in both orders, verify binding-accumulator disposal on failure, and require exactly one expected `DEFINES` relationship before endpoint checks.
- Runtime wording: the plan now uses the built CLI only for the positive fixture and a compiled real `ResolveBoundInto` test binary for malformed duplicate ScopeIR. Normal providers generate occurrence IDs from source position, so no provider hack or fake source collision is introduced.
- Latest graph: `1,571` scanned, `680` parsed-code, `0` failed, `95,698` nodes, and `134,629` relationships after the rejected-invariant correction.
- Full-build state: the first repo-native attempt stopped before compile because four editor-owned MCP executables held `anvien\bin\anvien.exe` through the global npm junction; doctor evidence marks them expected editor integrations and none was killed. The generated ignored binary was moved reversibly to repo-local `.tmp\p1d-held-anvien-before-full-build.exe` (SHA-256 `8CC545E20D36A91879DBE57596A1F1422996F78DB62761072CAC1CAECF78DC7B`) and the exact full-build script then exited `0` in `140.4s`: packaged runtime, web/launcher, CLI `1.2.8`, and self-analyze `1,571/680/0`, `95,699` nodes, `134,630` relationships. The held old binary was deleted after the old handles released; no process was killed. The first Supervisor independently reran the full build in `106.6s` and cleared this gate.
- Runtime boundary: built CLI positive fixture PASSed `1/1`, `0` failed, graph `20/19`, and exact source/copy hash parity. A separately compiled real resolution-package executable PASSed the malformed-ScopeIR fault boundary for range mismatch plus optional canonical property add/remove in both orders. The first Supervisor independently cleared the production/runtime boundary.
- Focused QA: the six-test P1-D/File-enrichment/P1-C endpoint matrix exits `0`; all six top-level tests and three collision subcases PASS. Full `internal/resolution`/`internal/graph`, all 11 production caller-family packages, and the exact `61/61` product packages also PASS; `E1-P1D-TEST1` is recorded complete.
- Regression closure: `go list -e ./...` reports 72 packages; only the measured 11 non-standalone corpus fixtures are excluded, and the exact product set PASSes `61/61`. The first Supervisor independently reran and cleared focused, package, family, and product regressions.
- Artifact lifecycle: the positive runtime repo/home, compiled resolution test executable, and old held binary remain absent. The first Supervisor found a broader top-level current-date inventory of 20 `p1d-*` `.tmp` files. Exact correction removes 11 paths: three failed commands, five exact-UID retries superseded by `*-final`, and three file-detail captures superseded by `*-final`. Nine latest/unique successful pre-flight captures remain; historical `p1d1*`/`p1d2*`/`p1d3*`, nested target-debug work, and unrelated `.tmp` files are preserved.
- Supervisor state: first formal report `reports/Supervisor/rp_supervisor_260811_114011_by_gpt-5-codex_child01_p1d.md` returns `REJECT` only for contradictory living-ledger prose and incomplete artifact lifecycle. It explicitly clears production source, full build, runtime, QA, and preserve-only siblings. The exact ledger/artifact correction is applied with production/test hashes unchanged; unconditional re-review, detect, and commit remain pending.
- Reject-correction verification: consistency assertions PASS `19/19`; `git diff --check` PASS; current-date top-level `p1d-*` inventory is exactly nine retained final/unique captures; fresh analyze PASSes `1,573/680/0`, `95,726/134,657`. P1-E, target, persistence/readers, relationships, and later Children remain closed.
- History-closure result: `reports/Supervisor/rp_supervisor_260811_122327_by_gpt-5-codex_child01_p1d_history_closure.md` returns `PASS` after independent source/diff, direct built runtime, focused/package/11-family/`61/61` QA, fresh graph/file-detail/CRITICAL impact, nine-artifact content, deleted-path, and living-authority checks; residual P1-D same-invariant surfaces are `none`. Detect/commit remain pending and P1-E stays closed.
- Detect state: post-PASS analyze `1,574/680/0`, `95,739/134,670` and staged all-scope detect-changes PASS across all 12 P1-D paths at risk `high`: `168` changed symbols, `13` affected symbols, `13` affected processes, every one of 24 changed gap occurrences/entities classified, and zero persisted gap/degraded-node/node-with-gap result. Only the isolated commit remains pending; P1-E stays closed.
- Commit boundary: final staged diff/ledger/hash/artifact checks PASS; the P1-D checklist and `E1-P1D-COMMIT1` record the repository convention that the isolated commit executes immediately after this final ledger state and Git reports the final hash externally. P1-E remains closed if the commit fails.
- Coder report: `reports/coder/rp_coder_260811_111652_by_gpt-5-codex_child01_p1d_definition_collision.md`; status `ACCEPTED FOR ISOLATED COMMIT; FINAL HASH REPORTED BY GIT`.

## Child 01 P1-E - Identity Integration and Bounded Target Validation

- Goal boundary: validate the accepted identity correction through the normal graph-generating runtime; no architecture redesign, persistence/reader implementation, target-source edit, or later-Child semantics.
- Git/source boundary: clean accepted base `10f66ffdd084a4a8710fd347836a1a083d4021bc`; P1-E changes no production/test file.
- Full build: exact repo-native build PASS in `114.4s`, packaged CLI `1.2.8`, binary SHA-256 `9AFD60607CD96352EFD34DB9A0E6F687F2D861AB123A45A1D292E3B7DC49362D`.
- Local determinism: packaged CLI PASS `5/5`; each graph is `20/19`, has `10/10` unique Definition targets and `10/10` `DEFINES`, zero missing endpoints, canonical SHA-256 `09DA4C6741D8D35DEA7B8680121F1B449C90E145D03B287E7917F67DB6ADA6FA`, and byte-identical Graph JSON SHA-256/size `C6C942B666931CE1AD505E3FE44B52C3887A6C16FBE0B2DAF94C25FE0AF8C1E9`/`44,199`.
- Target oracle: final packaged-CLI analyze exits `0` with `1,359/887/0`, graph `86,164/115,888`; exact Ladybug queries prove `time@207`, `time@214`, `now@262`, and `now@501` as four unique Variable IDs/scopes with four valid `DEFINES` and one File endpoint.
- Target preservation: same HEAD/branch, zero staged, same 13-entry status SHA-256 `D39E4DF20380C0F45F8200CEECD6F862C15FAABDE86ED1B0E5EB386422899351`, all `13/13` per-path hashes unchanged, oracle source hash unchanged, and settings hash unchanged. Only normal target `.anvien` output changed.
- Range boundary: provider/ScopeIR construct/selection ranges are validated at their source owner; graph nodes expose start/end lines while occurrence anchor and lexical scope remain in GraphID. P1-E does not invent graph-column, selection-range persistence, or reader semantics.
- Regression: TSJS `3/3`, identity/collision `9/9` plus three subcases, resolution/graph `2/2`, 11 caller families `11/11`, and exact product package set `61/61` PASS.
- Durable reports: `reports/coder/rp_coder_260811_140003_by_gpt-5-codex_child01_p1e_identity_validation.md`; `reports/QA/rp_qa_260811_140003_by_gpt-5-codex_child01_p1e_identity_validation.md`; JSON companion at the same QA slug.
- Artifact lifecycle: exact initial inventory `25` top-level current-slice paths. After durable evidence existed, text/log/cache/runtime-output cleanup reduced it to six. Exact containment/reparse checks allowed removal of three empty directories, the matched runtime tree, and compiled resolution executable. The last binary initially failed one exclusive-open probe, but fresh OS evidence found zero exact-file users and proved the MCP process mapped a different file object. Independent exclusive-open and exact one-file Git dry-run gates passed; the matching cleanup removed the binary. Cleanup is `25/25`, the current-slice pattern inventory is empty, no process was killed, and no historical/unrelated `.tmp` path was touched.
- Review state: the earlier hidden P1-E Supervisor lane was terminated by Owner and produced no durable verdict. On 2026-08-11 the Owner explicitly decided `P1-E thông qua. Chuyển slice đi.` This is recorded as an Owner acceptance/override for P1-E only, never as Supervisor PASS.
- Transition decision: run mandatory staged detect-changes, create the isolated P1-E commit, then open Pn-A as a new visible Child-level Supervisor task. Pn-A remains a real independent Supervisor gate; Pn-B/Pn-C and Child 02 stay closed.
- Detect state: exact nine-path staging plus fresh analyze `1,577/680/0`, graph `95,768/134,699`, precedes all-scope detect-changes. Detect exits `0` at risk `low` with `50` changed symbols / `9` changed files / `0` affected symbols or processes; gap and resolution-health deltas are `0`, and semantic fields/sources are complete for all `95,768` nodes.
- Commit boundary: `E1-P1E-COMMIT1` records the repository convention that the isolated P1-E commit executes immediately after this final ledger state; Git reports the final hash externally because the commit cannot contain its own hash.
