# Child 06A Accelerate Analyze Without Sacrificing Accuracy Plan Rules

## Metadata

- Date: `2026-08-24`
- Status: `binding auxiliary rule authority`
- Role: auxiliary readability split only; not a fifth standard ledger, phase, gate, report, evidence source, numeric control surface, or permission boundary
- Plan: [2026-08-24-06a-accelerate-analyze-without-sacrificing-accuracy-plan.md](2026-08-24-06a-accelerate-analyze-without-sacrificing-accuracy-plan.md)
- Evidence: [2026-08-24-06a-accelerate-analyze-without-sacrificing-accuracy-evidence.md](2026-08-24-06a-accelerate-analyze-without-sacrificing-accuracy-evidence.md)
- Benchmark: [2026-08-24-06a-accelerate-analyze-without-sacrificing-accuracy-benchmark.md](2026-08-24-06a-accelerate-analyze-without-sacrificing-accuracy-benchmark.md)
- Actual status: [2026-08-24-06a-accelerate-analyze-without-sacrificing-accuracy-actual-status.md](2026-08-24-06a-accelerate-analyze-without-sacrificing-accuracy-actual-status.md)
- Method authority: `E:\Anvien\reports\Investigation\rp_main_260824_204615_child06a_optimization_method_discussion.md`
- Required provenance and handoff reference: `E:\Anvien\reports\Investigation\rp_huong_dan_lam_viec_de_slice_p6e_tien_hanh_nhanh_hon.md`

The directory contains exactly four standard planner ledgers plus this one auxiliary rule file. Standard suffix/link checks continue to count only the plan, evidence, benchmark, and actual-status files.

## Binding Plan-Wide Rules

- Child 06 is closed at P6-D commit `81163e39718b94a509e41114cada224e8f269e36`. The only campaign order is Child 06 -> Child 06A -> Child 07; Child 07 opens only from the Child 06A closure commit.
- The empirical method comes from `E:\Anvien\reports\Investigation\rp_main_260824_204615_child06a_optimization_method_discussion.md`. The exact older path `E:\Anvien\reports\Investigation\rp_huong_dan_lam_viec_de_slice_p6e_tien_hanh_nhanh_hon.md` remains required provenance in handoffs, but its superseded fixed-work and execution-control details do not govern this plan.
- Work proceeds directly from the first incomplete checklist item. No separate permission, start-state, approval, or unrelated pre-optimization gate is part of execution.
- `P1-A` measures the real pipeline and creates the first current ranking. It performs no production optimization and makes no speedup claim.
- `P2-A` is the only implementation slice and remains open across all dynamically selected optimization attempts. The open slice controls the active parent/child hierarchy and does not prohibit accepted progress checkpoints.
- After an attempt completes its full Architect -> Planner -> Coder -> build/measurement -> Supervisor chain and receives Supervisor `PASS` plus Main `KEEP`, update the four ledgers immediately, run the required detect on the exact accepted owned scope, and create a scoped implementation progress commit before continuing. That commit is the rollback anchor for the next attempt; it does not close P2-A, check the active child/parent, or open P3.
- A rejected, rolled-back, incomplete, or unaccepted attempt creates no accepted implementation progress commit. Restore or retain the last accepted checkpoint and continue through the required fresh attempt chain.
- Docs/report progress commits are allowed whenever their current checkpoint is complete and scoped. A docs-only commit does not require implementation detect and must not stage accepted or unaccepted production/test bytes accidentally.
- `P3-C` has a local commit boundary: after final review and cleanup it runs its own final detect and creates exactly one P3-C closure commit, with no repeated detect/commit loop inside P3-C. This local rule does not prohibit scoped progress commits in P2-A or earlier phases/slices.
- P1-A creates the complete measured top-level parent list. P2-A processes every parent, largest elapsed time first; smaller measured parents remain mandatory queued work and cannot be skipped or terminalized because their time is small.
- Elapsed wall-clock time is the primary and controlling metric. Rank bottlenecks by current absolute elapsed time only. CPU, RAM, allocation, GC, I/O, wait, calls, bytes, denominators, and shares are secondary cause/comparability evidence and cannot select priority or prove speedup.
- Processing a selected parent begins by measuring inside only that parent and creating its complete measured child list in descending absolute elapsed-time order. P2-A processes the largest child first, continues that child until terminal, then processes every smaller child; a parent cannot close while any measured child remains unprocessed.
- Every child production attempt has this control chain: active parent row + complete child list + selected child timing/denominator/cause/owner/complete call path -> new Visible Architect decision for that attempt -> Planner refresh of the current P2-A attempt -> Coder edit -> full build -> child + parent + end-to-end remeasurement -> new Visible Supervisor accuracy/equivalence check -> disposition.
- Architect cannot open from a coarse parent label or incomplete child inventory. Each attempt must bind the exact selected child and the complete current parent-child list; drill-down remains an internal P2-A step, not a separate phase, implementation slice, or all-pipeline instrumentation project.
- The Architect decision is fresh for every production edit attempt, including another attempt on the same bottleneck after `KEEP`, `REWORK`, `ROLLBACK`, or any changed result. It must bind exact cause, technical direction, allowed owners, expected observable gain, preserved invariants, validation/resource boundary, and rollback condition. A prior decision cannot authorize later code.
- After each Architect decision and before Coder starts, Planner must replace the living Current P2-A Attempt card and concrete P2-A execution steps with that decision. Coder cannot guess, reuse, broaden, or substitute the direction.
- Each attempt's post-measurement Visible Supervisor reviews the exact changed candidate and affected accuracy/correctness/equivalence/output/lifecycle boundary. It does not edit code or audit documentation.
- `KEEP` and accepted-baseline promotion require all four: the selected child improved, its active parent improved, end-to-end graph-generation time retained the benefit, and that attempt's Supervisor returned `PASS`. A rejected candidate never becomes the baseline and cannot drive reranking or queue transition.
- Improvement means lower elapsed wall-clock time at child, parent, and total graph-generation boundaries. Better secondary metrics without all three elapsed-time gains cannot receive `KEEP`.
- A003-only Owner disposition (`2026-08-26`): Owner explicitly keeps the exact already-reviewed A003 canonical-decoder candidate despite the measured Cheapapp D001 variation `+0.356932100 s` and parent variation `+1.432134300 s`. Those positive local deltas remain objective measured facts; they are neither absent nor erroneous and are not automatically labeled noise. For A003 only, the variation is accepted in context because same-work process wall improves strongly on both targets, Restaurant D001 and parent also improve, and the exact independent verdict is `SUPERVISOR_A003_PASS`. This corrects the prior mechanical A003 disposition that any single positive local delta must force `NO_KEEP`; it does not create a numeric tolerance, weaken the measurement record, or apply automatically to A004 or any later attempt. Exact A003 source/test hashes are now restored, Owner `KEEP` resets the D001 unsuccessful streak to `0`, the A003 checkpoint is pending, and A004 Architect may open only after that checkpoint.
- After every child `KEEP`, remeasure the child, its parent, and end-to-end graph generation, refresh current accepted values, keep that same child active, and continue new attempts until its retained remaining cost is terminal. `KEEP` resets only that child's unsuccessful-attempt streak to `0`; it does not check off the child or move to another row.
- After child `SYSTEM_CHARACTERISTIC`, retain the last accepted correct state, remeasure that child, its parent, and end-to-end graph generation, refresh the complete child/top-level benchmark lists, check off only that child, and select the largest remaining unprocessed child of the same parent. After every child of the active parent is terminal, record accepted parent/full-pipeline remeasurement, check off the parent, refresh the complete top-level list, and select the largest remaining unprocessed parent.
- On Supervisor `REJECT`, restore or keep the last accepted baseline with exact attempt-owned rollback, open the next attempt by refreshing the selected bottleneck's internal drill-down on that accepted state, return rejection evidence and the violated invariant with that current ranking to a new Visible Architect, refresh P2-A from the new decision, and only then allow another Coder attempt. Coder never owns the correction direction.
- A rejected child stays active and cannot be bypassed by reranking or moving to another child while its correction loop is active, except when a concrete unavailable authority/dependency/evidence blocker is recorded or the three-attempt terminal rule applies.
- At one current accepted baseline, a child may have at most three consecutive production attempts without `KEEP`. Every attempt runs the complete current parent-child basis -> Architect -> Planner -> Coder -> build/remeasure -> Supervisor chain. `KEEP` promotes the accepted baseline and resets that child's unsuccessful-attempt streak to `0`.
- After three consecutive attempts without `KEEP`, restore/retain the last accepted correct baseline and mark only that child `SYSTEM_CHARACTERISTIC`. This terminal disposition means the child's remaining measured cost cannot be safely improved further inside the preserved current system boundary; it is not a speedup claim, accuracy waiver, or parent completion.
- Any further production edit after a Supervisor result is a new attempt with a new measurement basis, new Architect decision, new Planner refresh, and new post-measurement Supervisor check.
- `benchmark.md` is the numeric source of truth and optimization control surface. `evidence.md` proves command validity, boundary, ownership, Architect decision, Planner refresh, impact, source/build/tests, Supervisor result, equivalence, and disposition. `actual-status.md` points to exact benchmark rows and the active attempt. This plan contains method and current attempt steps, not metric copies.
- The Measured Bottleneck Checklist in this plan mirrors exact benchmark row IDs/names without copying metrics. P1-A inserts every measured parent; P2-A inserts every measured child under its parent, and marks a row only when its retained terminal state is evidenced. Benchmark remains the sole numeric/order authority.
- Record each measurement or result immediately in benchmark, its proof immediately in evidence, and the resulting current pointer immediately in actual status. Do not batch updates at the end.
- P1-A obtains its initial accepted real `anvien analyze` capture through exactly one visible measurement executor. That executor records the actual executable/options/workload/cache/runtime identity rather than using an invented tuple. After the capture is accepted, up to `3` ACTIVE read-only measurement analysts may share it for independent measured problems; they do not edit source or launch duplicate competing captures.
- The available measurement pool contains `10` pre-opened visible lanes, but unused lanes remain waiting and no work is invented to fill the pool. Run waves of at most `3` ACTIVE read-only measurement analysts only when current evidence exposes independent real parent/child bottleneck problems; each active lane owns exactly one such problem. Prefer the one accepted runtime capture shared by the active wave and never start duplicate competing benchmark processes merely to use concurrency. A slot is released only after that lane's number is in benchmark, proof is in evidence, matching plan checklist state and actual-status pointer are current, and Main can make the next decision; only then may the next waiting lane be dispatched. Production/test writing in the shared worktree remains sequential.
- Use existing runtime timing, benchmark output, and profiles first. If and only if one required P1-A operation timing is missing, Main assigns exactly one visible sequential instrumentation-writer lane outside the read-only measurement analysts; that lane is the sole source writer for the branch. Before editing it refreshes the required graph and records file-detail plus impact on the exact owner. It adds only the smallest missing timing instrumentation, and the canonical full build must PASS before any instrumented timing is consumed or ranked.
- The conditional P1-A writer branch records the exact writer, owner/owned bytes, graph/file-detail/impact, build, runtime/instrumentation identity, overhead, denominator/comparability, and output equivalence in `E1-P1A-INSTR1`; all comparisons are like-for-like. It then ends with exactly one disposition: carry the instrumentation with exact ownership into the first refreshed P2-A attempt, or remove the exact owned bytes, run the canonical full build again, and re-establish and remeasure the accepted timing basis. An unbuilt, mixed-instrumentation, or half-removed state cannot enter benchmark ranking or actual status.
- Measurement instrumentation is not a phase, deliverable, implementation slice, optimization attempt, harness project, Architect decision, Supervisor gate, progress claim, or separate commit/report system.
- Temporary debug/profile material stays under repository-local `E:\Anvien\.tmp`. Do not create a durable per-run report tree or a second cost map beside the four standard ledgers.
- The reusable A00x overlay/instrumented candidate build path is `scripts/build-a00x-benchmark.ps1`. It accepts the attempt ID, overlay manifest, output directory, and expected overlay/mapped-source/candidate-source hash contract as explicit parameters; carries the canonical vendor, Ladybug native, CGO, offline Go environment and build flags; and always writes the executable, pinned native DLL, and machine-readable provenance beneath repository-local `.tmp`.
- `scripts/build-a00x-benchmark.ps1` does not edit, copy, wrap through, or replace `scripts/full-build.ps1` or `anvien-launcher/build.ps1`, and it does not write canonical runtime/launcher/server/web outputs, register protocols, run `analyze`, or mutate a target repository. The canonical scripts remain the normal full-build authority; the reusable script exists only to freeze an A00x benchmark candidate whose overlay/instrumentation cannot be represented by canonical source alone.
- When the reusable script and Go/native build contract are unchanged, later A00x attempts invoke it with their own explicit overlay/output/hash inputs and must not repeat general build-interface discovery or audit. The frozen provenance packet is the identity proof for that invocation.
- If a later A00x introduces a genuinely different feature/input and the reusable script fails, first classify the exact compile/link/runtime-contract error from that script invocation. Refresh the current attempt plan, then change only the narrow reusable-script parameter, guard, environment, tag, generated-input, or provenance behavior required by that evidenced A00x difference. Do not restart a broad audit of the two canonical scripts, alter production code to evade the build failure, or change canonical build interfaces unless the latest Owner explicitly changes this rule.
- A reusable-script failure is measurement-support evidence only: it creates no benchmark value, no no-KEEP attempt, no baseline/streak/checklist change, and no production disposition. After a script change, validate that change once; unchanged later invocations reuse the accepted script/native contract.
- A lane report, when needed, records only lane/work/result/checkpoint/next owner. It cannot become an audit gate; report-about-report, evidence-about-evidence, routine hash audit, and documentation review loops are forbidden.
- Production code precedes tests. The canonical full build precedes post-change runtime validation for every P2-A attempt; the separate conditional P1-A instrumentation rule above also requires a full-build PASS before instrumented timing use and, on removal, another full build before the accepted basis is remeasured.
- Before graph-based owner work, refresh the graph as required by repository rules; immediately before editing the measured owner, record file-detail and impact. HIGH/CRITICAL is a scope warning, not an edit prohibition.
- Preserve user, historical, protected, shared, and quarantined artifacts. Rollback is limited to exact attempt-owned bytes; broad reset, checkout, stash, cleanup, alternate worktree, network, install, push, and cross-run hidden caches are forbidden.
- Never reduce workload, outputs, validation, persistence, readers, or failure paths to manufacture a speedup. Do not change public output/schema/format unless the measured owner and affected contract are explicitly brought into the refreshed attempt and equivalence remains exact.
- `P3-A` is the one final whole-candidate Supervisor boundary in addition to the mandatory per-attempt Supervisor checks. `P3-B` performs exact cleanup without another review. `P3-C` performs its own final detect, creates exactly one local P3-C closure commit, and hands that closure commit to Child 07; earlier accepted progress commits remain valid rollback checkpoints.
- If `P3-B` changes accepted production or test bytes, the final `P3-A` verdict is invalidated and execution returns to a new exact-owner P2-A attempt with its own Architect/Planner/Coder/Supervisor chain. The repaired state returns to the same final Supervisor boundary, never a second final boundary or commit boundary.
