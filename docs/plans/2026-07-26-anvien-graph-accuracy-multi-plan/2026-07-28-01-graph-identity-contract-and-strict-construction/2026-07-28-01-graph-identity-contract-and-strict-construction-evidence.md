# Anvien Graph Identity Contract and Strict Construction Evidence Ledger

## Metadata

- Date: `2026-07-28`
- Plan: `docs/plans/2026-07-26-anvien-graph-accuracy-multi-plan/2026-07-28-01-graph-identity-contract-and-strict-construction/2026-07-28-01-graph-identity-contract-and-strict-construction-plan.md`
- Evidence: `docs/plans/2026-07-26-anvien-graph-accuracy-multi-plan/2026-07-28-01-graph-identity-contract-and-strict-construction/2026-07-28-01-graph-identity-contract-and-strict-construction-evidence.md`
- Benchmark: `docs/plans/2026-07-26-anvien-graph-accuracy-multi-plan/2026-07-28-01-graph-identity-contract-and-strict-construction/2026-07-28-01-graph-identity-contract-and-strict-construction-benchmark.md`
- Actual status: `docs/plans/2026-07-26-anvien-graph-accuracy-multi-plan/2026-07-28-01-graph-identity-contract-and-strict-construction/2026-07-28-01-graph-identity-contract-and-strict-construction-actual-status.md`
- Roadmap: `docs/plans/2026-07-26-anvien-graph-accuracy-multi-plan/2026-07-26-anvien-graph-accuracy-roadmap.md`

## Evidence Rules

- The original problem report is the problem origin and bounded oracle source.
- Architecture recommendations inside that report are DRAFT and do not become implementation evidence without current source proof and plan acceptance.
- The causal synthesis and bounded Supervisor PASS verify the finding; they are not the originating report or an implementation design authority.
- Current source and runtime evidence determine implementation ownership.
- A pending evidence ID is a declared target, not proof.
- Every implementation slice closes only when all exact evidence IDs named by its plan Acceptance are recorded.
- Counts and timings belong in the benchmark ledger; commands, source facts, and verdicts belong here.

### Evidence ID Naming

Use `E<phase>-<item>-<kind><n>`. Each ID has one meaning and is not reused.

## E0 - P0 Evidence

Matching plan item: `P0-A`

- `E0-P0A-REPORT1` — recorded: `reports/problem/2026-07-26-tinh-chinh-cac-van-de-cua-anvien` establishes the bounded identity baseline: four `time`/`now` source facts survive ScopeIR while two graph nodes remain. Only the finding and `2/4 -> 4/4` target are used here.
- `E0-P0A-VERIFY1` — recorded: `reports/Supervisor/260726_165317_graph_root_cause_causal_synthesis.md` classifies range/scope-free graph identity plus duplicate-node replacement as the bounded first graph divergence; `reports/Supervisor/rp_supervisor_260726_170048_by_gpt-5-6-sol_final_bounded_investigation.md` passes that bounded investigation only.
- `E0-P0A-GRAPH1` — recorded: the pre-ledger refresh at commit `7042dda8bfc02ee42b40c3a1c0ede89138439481` reported `1,557` scanned files, `676` parsed/parsed-code files, `0` failed files, `85,110` graph nodes, and `123,978` relationships; the required post-ledger refresh with the same command also reports `1,557/676/0`, while the graph inventory is `85,114` nodes and `123,982` relationships. `anvien status`/`.anvien/meta.json` agree on the commit and `stale=false`; the observed `+4/+4` inventory delta occurred across a documentation-only worktree diff, while `git diff --exit-code -- internal` confirms no production-source change. Per-document attribution of that delta was not measured.
- `E0-P0A-SOURCE1` — recorded: `internal/resolution/indexes.go:814-823` (`graphIDForDef`) derives a graph ID from label, cleaned file path, qualified/name, and optional arity; it does not consume `DefinitionFact.ID`, range, lexical scope, or a distinct meaning lane.
- `E0-P0A-SOURCE2` — recorded: `internal/scopeir/range.go:3-8` exposes only four integers; `internal/providers/tsjs/nodes.go:18-26` uses one-based lines and unchanged tree-sitter columns; no shared encoding, interval, or selection-range contract is declared. `internal/scopeir/position_index.go:51-52,95-104` treats the end coordinate as included, so P1-B must ratify semantics before changing a shared range type.
- `E0-P0A-SOURCE3` — recorded and reclassified: `internal/scopeir/definition_index.go:7-17` is first-writer-wins for duplicate `DefinitionFact.ID`, but repository search plus source-flow inspection found no production caller. Its only callers are `internal/scopeir/scope_indexes_test.go` and `internal/scopeir/legacy_p7_scope_extractor_conversion_test.go`; it is preserve-only, not the bounded production loss owner.
- `E0-P0A-SOURCE4` — recorded: `internal/graph/types.go:96-104` (`Graph.AddNode`) silently replaces an existing payload for a duplicate ID; `Graph.init` at `:270-281` also rebuilds an index without rejecting duplicate IDs already present in `Nodes`.
- `E0-P0A-SOURCE5` — recorded: the normal path is `analyze.parseFiles` (`internal/analyze/analyze.go:799-857`) → provider ScopeIR → `BuildCrossFileBinding/buildWorkspace` (`internal/resolution/resolve.go:23-48`, `internal/resolution/indexes.go:72-199`) → `ResolveBoundInto` (`internal/resolution/resolve.go:50-90`) → `emitDefinitionNodes`/`emitter.emitNode` (`internal/resolution/emit.go:31-34,136-203`) → `Graph.AddNode`; no hidden `BuildDefinitionIndex` stage is present and no orchestration edit is proven for P0.
- `E0-P0A-SOURCE6` — recorded: `DefinitionFact` (`internal/scopeir/facts.go:3-28`) has one full `Range`, `OwnerID`, `Label`, and optional `Visibility`, but no selection range, lexical-scope field, or explicit meaning mask. TSJS `addDefinition` (`internal/providers/tsjs/definitions.go:79-131`) stores the construct range and scope binding/owned-definition links, while `nodeRange` stores no identifier-token range.
- `E0-P0A-SOURCE7` — recorded: TSJS scope construction (`internal/providers/tsjs/scopes.go:54-121`) creates deterministic range-derived scope IDs and `innermostScopeID` links definitions to lexical scopes through `ScopeFact.OwnedDefIDs`; `ScopeIR.NormalizeInPlace`/`compareDefinition` sort facts but do not deduplicate them (`internal/scopeir/ir.go:69-110`, `internal/scopeir/sort_keys.go:23-35`). The source has lexical-owner input even though graph projection omits it.
- `E0-P0A-SOURCE8` — recorded: `emitDefinitionNodes` (`internal/resolution/emit.go:136-203`) emits one attempted node and `DEFINES` edge per `defsByFile` occurrence, but node properties retain only start/end lines (not columns, selection range, provider ID, or scope). `emitter.emitRelationship` (`:36-49`) is a separate relationship merge pipeline and remains outside the node-collision owner.
- `E0-P0A-SOURCE9` — recorded: existing tests assert legacy semantics that must be classified before any implementation change: `Graph.AddNode` replacement in `internal/graph/types_test.go:91-105`, `BuildDefinitionIndex` first-writer behavior in `internal/scopeir/scope_indexes_test.go:9-45` and `legacy_p7_scope_extractor_conversion_test.go:64-85`, inclusive position-index boundaries in `scope_indexes_test.go:63-93`, and intentional semantic relationship merging in `internal/resolution/resolution_test.go` (`TestResolveMergesDuplicateSemanticEdgesAndCountsUnresolved`). These are sibling contracts, not proof that the bounded identity behavior is correct.
- `E0-P0A-FD1` — recorded: fresh `anvien file-detail internal/resolution/indexes.go --repo E:\Anvien --json` reports `46` related files, `192` symbols, risk `high`, and `stale=false/changedSinceAnalyze=false`.
- `E0-P0A-FD2` — recorded: fresh file-detail for `internal/scopeir/range.go` reports `227` related files, `6` symbols, risk `medium`, and `stale=false/changedSinceAnalyze=false`.
- `E0-P0A-FD3` — recorded: fresh file-detail for `internal/scopeir/definition_index.go` reports `225` related files, `14` symbols, risk `medium`, and `stale=false/changedSinceAnalyze=false`.
- `E0-P0A-FD4` — recorded: fresh file-detail for `internal/graph/types.go` reports `238` related files, `92` symbols, risk `high`, and `stale=false/changedSinceAnalyze=false`.
- `E0-P0A-FD5` — recorded: fresh file-detail for `internal/resolution/emit.go` reports `42` related files, `75` symbols, risk `high`, and `stale=false/changedSinceAnalyze=false`.
- `E0-P0A-FD6` — recorded: fresh file-detail for `internal/scopeir/facts.go` reports `231` related files, `119` symbols, risk `medium`, and `stale=false/changedSinceAnalyze=false`.
- `E0-P0A-FD7` — recorded: fresh file-detail for `internal/scopeir/ir.go` reports `229` related files, `29` symbols, risk `high`, and `stale=false/changedSinceAnalyze=false`.
- `E0-P0A-FD8` — recorded: fresh file-detail for `internal/scopeir/sort_keys.go` reports `226` related files, `18` symbols, risk `high`, and `stale=false/changedSinceAnalyze=false`.
- `E0-P0A-FD9` — recorded: fresh file-detail for `internal/providers/tsjs/definitions.go` reports `16` related files, `30` symbols, risk `high`, and `stale=false/changedSinceAnalyze=false`.
- `E0-P0A-FD10` — recorded: fresh file-detail for `internal/providers/tsjs/nodes.go` reports `20` related files, `42` symbols, risk `high`, and `stale=false/changedSinceAnalyze=false`.
- `E0-P0A-FD11` — recorded: fresh file-detail for `internal/providers/tsjs/scopes.go` reports `17` related files, `33` symbols, risk `high`, and `stale=false/changedSinceAnalyze=false`.
- `E0-P0A-FD12` — recorded: fresh file-detail for `internal/providers/tsjs/extract.go` reports `23` related files, `33` symbols, risk `high`, and `stale=false/changedSinceAnalyze=false`.
- `E0-P0A-FD13` — recorded: fresh file-detail for `internal/resolution/resolve.go` reports `50` related files, `40` symbols, risk `high`, and `stale=false/changedSinceAnalyze=false`.
- `E0-P0A-FD14` — recorded: fresh file-detail for `internal/resolution/types.go` reports `31` related files, `64` symbols, risk `high`, and `stale=false/changedSinceAnalyze=false`.
- `E0-P0A-FD15` — recorded: fresh file-detail for `internal/analyze/analyze.go` reports `182` related files, `220` symbols, risk `high`, and `stale=false/changedSinceAnalyze=false`.
- `E0-P0A-IMPACT1` — recorded exact upstream impacts with `--include-tests`: `graphIDForDef` is `CRITICAL`, `23` symbols/`13` files/`3` modules/`18` processes (`backend 6`, `backend_test 17`); `buildWorkspace` is `CRITICAL`, `41` symbols/`17` files/`8` modules/`28` processes (`backend 7`, `backend_test 33`, `cli_launcher 1`); `Range` is `CRITICAL`, `854` symbols/`143` files/`22` modules/`75` processes (`backend 503`, `backend_test 351`); `DefinitionFact` is `CRITICAL`, `586` symbols/`80` files/`23` modules/`63` processes (`backend 181`, `backend_test 405`).
- `E0-P0A-IMPACT2` — recorded supporting impacts: `Graph.AddNode` is `CRITICAL`, `296` symbols/`70` files/`11` modules/`82` processes (`backend 36`, `backend_test 179`, `api_test 81`); `emitNode` is `CRITICAL`, `6` symbols/`5` files/`2` modules/`19` processes; `emitDefinitionNodes` is `CRITICAL`, `24` symbols/`9` files/`7` modules/`35` processes; `BuildDefinitionIndex` is `LOW`, `2` test symbols/`2` test files/`0` production symbols; TSJS `nodeRange` is `MEDIUM`, `8` symbols/`4` files; `ScopeIR.NormalizeInPlace` and `compareDefinition` are `LOW` with `3` and `4` impacted symbols respectively. Each CRITICAL/HIGH value is recorded as a scope warning, not an edit prohibition.
- `E0-P0A-BOUNDARY1` — recorded: `E:\cheapapp.org` is an in-place validation target; target source/worktree is preserve-only and only normal target-repository output is allowed.
- `E0-P0A-BOUNDARY2` — recorded from independent target-boundary checks: target HEAD/branch is `a869876ab6262dacde6cd5d432d099a91852a646`/`master`; worktree has `13` pre-existing entries (`7` tracked modifications, `6` untracked, `0` staged); oracle source hash is `A9247972D44D0A74C411FB7CF83D681FEBA371C7A85EEB83F8986AFD6446E40C`; existing target graph hash/size is `DD3A3A987E95C91787E7C70EF68BA5D2F953D24A32D35DEFE38B8E8E606FF090`/`315,569,880` bytes with `1,359` files, `84,807` nodes, and `114,125` relationships. The four bounded declarations are `time@207` in `parseTime`, `time@214` in the `latestIso` reducer callback, `now@262` in `buildEmailOperationsReport`, and `now@501` in `readEmailOperationsReport`; P0 did not run target analyze.
- `E0-P0A-STATUS1` — recorded: the actual-status ledger distinguishes source facts, production owners, preserve-only utilities, and the external target boundary; P0 is now Supervisor-accepted, P1-A is open as documentation-only, and production implementation remains gated by the ordered P1 slices.
- `E0-P0A-STATUS2` — recorded: the invariant family map and phase touch map are updated for identity, ranges/selection, lexical owner/meaning, occurrence conservation, collision/mutation, projection/endpoints, and sibling relationship/persistence surfaces; forbidden fallbacks are explicit.
- `E0-P0A-REVIEW1` — recorded: `reports/Supervisor/rp_supervisor_260810_095511_by_gpt-5-codex_child01_p0a.md` independently reviews the current P0-A source, file-detail, impact, target-boundary, ledger, and diff evidence and returns `PASS`. The report accepts only the P0 classification/documentation gate; it does not claim production repair or runtime `4/4`.

## E1 - P1 Evidence

Matching plan items: `P1-A`, `P1-B`, `P1-C`, `P1-D`, `P1-E`

| Slice | Exact evidence ID | Required proof | Status |
|-------|-------------------|----------------|--------|
| P1-A | `E1-P1A-SOURCE1` | recorded: P0 source trace binds the contract to `DefinitionFact`/`ScopeFact` inputs, the production `defsByFile` occurrence denominator, `graphIDForDef`, `emitDefinitionNodes`, and `Graph.AddNode`; the problem report's DRAFT topology/hash/file-layout proposal remains excluded | recorded |
| P1-A | `E1-P1A-CONTRACT1` | recorded: `docs/contracts/graph-accuracy-contract.md` now marks the campaign active after the accepted correction/P0 gates and ratifies only the source-backed occurrence denominator, identity inputs, collision owner, endpoint traceability, and separate relationship boundary; it prescribes no new node topology, hash algorithm, field name, or file layout. Fresh file-detail is `stale=false/changedSinceAnalyze=false`, `0` related files; file impact is `LOW`, `0` impacted symbols/files/processes. The document audit reports 75 declared Child evidence IDs, 0 missing references, 0 missing required paths, 5 ordered P1 slices, and a documentation-only five-file diff. | recorded |
| P1-A | `E1-P1A-REVIEW1` | recorded: `reports/Supervisor/rp_supervisor_260810_100646_by_gpt-5-codex_child01_p1a_contract.md` independently verifies the source trace, five-file documentation diff, contract blast radius, ledger consistency, ownership boundary, and absence of a newly prescribed architecture; verdict `PASS` | recorded |
| P1-A | `E1-P1A-COMMIT1` | recorded: the accepted P1-A contract/ledger/report work is committed immediately after this evidence row with message `docs(plan): ratify child 01 identity contract`; the final commit hash is reported from Git because a commit cannot contain its own final hash | recorded |
| P1-B | `E1-P1B-IMPACT1` | recorded at implementation HEAD `8cbf6006`: mandatory `anvien analyze --force --json` reports `1,559` scanned, `676` parsed-code, `0` failed, `85,132` nodes, and `124,000` relationships. Full compact file-detail evidence reports `stale=false/changedSinceAnalyze=false` and `231`, `227`, `20`, and `16` related files (excluding the target) for `internal/scopeir/facts.go`, `internal/scopeir/range.go`, `internal/providers/tsjs/nodes.go`, and `internal/providers/tsjs/definitions.go`. Exact upstream impacts with tests are: `DefinitionFact` CRITICAL `586` symbols / `80` files / `23` modules / `63` processes; `Range` CRITICAL `854/143/22/75`; TSJS `nodeRange` MEDIUM `8/4/1/0`; TSJS `collector.addDefinition` LOW `0` upstream symbols / `1` selected file / `0` modules / `0` processes. CRITICAL is retained as a blast-radius warning. The selected owner boundary remains the existing P1-B candidates; `buildWorkspace.scopeByDef`, `PositionIndex`, range containment, other providers, identity, collision, and persistence remain inspect/preserve-only. | recorded |
| P1-B | `E1-P1B-SOURCE1` | recorded: `DefinitionFact` now carries an additive optional `SelectionRange *Range` at the shared ScopeIR fact owner, and TSJS `collector.addDefinition` records the declaring `nameNode` range while retaining the existing construct `Range`. `nodeRange` now explicitly documents the source-proven TSJS contract: one-based lines, zero-based UTF-8 byte columns, exclusive end points. Existing lexical/meaning inputs remain at their current owners (`ScopeFact.OwnedDefIDs`/bindings, `Label`, and `OwnerID`); no redundant topology or field was added. `Range`, `PositionIndex`, TSJS scope containment, other providers, the Child 03 binding-pattern branch, identity, collision, projection, persistence, and readers are unchanged. | recorded |
| P1-B | `E1-P1B-BUILD1` | recorded: the first `powershell -NoProfile -ExecutionPolicy Bypass -File .\\scripts\\full-build.ps1` attempt rebuilt the packaged Go runtime, then failed at launcher-web `npm run build` because `anvien-web/node_modules` was absent (`TS2688` for `vite/client` and `node`). `npm ci` restored the committed lockfile dependency set without a tracked-file change. The complete retry PASSed after production code and the focused test file existed: packaged Go runtime, web `tsc -b && vite build`, launcher/server, CLI `1.2.8`, and built-CLI analyze (`1,560` scanned, `677` parsed-code, `0` failed, `85,193` nodes, `124,090` relationships). A later retry after test-only assertion strengthening stopped before compile because three editor-owned `anvien mcp` processes execute through the global npm junction and lock `anvien\\bin\\anvien.exe`; `anvien doctor processes --json` says they are expected editor integrations, so none was killed. Production/build inputs were unchanged after the PASS; the final test-only delta was compiled and executed through the boundary binary and Go QA below. | recorded |
| P1-B | `E1-P1B-TEST1` | recorded: final focused Go 1.26.3 matrix PASS (`go test ./internal/providers/tsjs -run '^Test(ExtractDefinitionPositionInputs|ExtractDefinitionOwnerMeaningAndLexicalInputs|P1BPreservesBindingPatternBoundary)$' -count=1 -v`); `go test ./internal/providers/tsjs ./internal/scopeir ./internal/parser -count=1` PASS. Raw `go test ./... -count=1` exited `1` only at five intentionally non-standalone/compile-negative corpus packages (`go-map-range`, `go-method-enrichment`, `sample-code`, `go-make-builtin`, `go-type-assertion`) while all reported `internal/...` packages PASSed. The exact product regression generated with `go list -e ./...`, excluding only `*/anvien/test/fixtures/*`, PASSed `61/61` packages. The read-only review gaps were closed by same-lexeme `Shared` interface/value labels, exact-one lexical owner plus parent/binding assertions, and owner/meaning/selection JSON round-trip checks. | recorded |
| P1-B | `E1-P1B-RUNTIME1` | recorded: Go 1.26.3 compiled the real TSJS package boundary into repo-local `.tmp/p1b-tsjs-boundary.test.exe`. The first invocation exited `2` before behavior because PowerShell split dotted `-test.v`; with exact quoted arguments, the rebuilt executable PASSed `TestExtractDefinitionPositionInputs`, `TestExtractDefinitionOwnerMeaningAndLexicalInputs`, and `TestP1BPreservesBindingPatternBoundary`. This exercises parser UTF-8 bytes → TSJS AST/`addDefinition` → normalized ScopeIR/JSON at the nearest owned built boundary, covering LF/CRLF, Unicode byte columns, tabs, exclusive endpoints, construct versus selection ranges, same-lexeme provider labels, exact nested lexical ownership/bindings, member owner ID, round-trip preservation, and the Child 03 binding-pattern boundary. After Supervisor PASS, the compiled boundary executable and superseded `.tmp/p1b-anvien` preflight traces were removed; unrelated `.tmp` artifacts were preserved. | recorded |
| P1-B | `E1-P1B-REVIEW1` | recorded: first independent review `reports/Supervisor/rp_supervisor_260810_115437_by_gpt-5-codex_child01_p1b.md` cleared production/build/runtime/tests but REJECTed one contradictory Detailed Findings ledger narrative. Only that actual-status narrative changed. History-closure re-review `reports/Supervisor/rp_supervisor_260810_120042_by_gpt-5-codex_child01_p1b_resubmission.md` verifies 12 consistency assertions, unchanged production/test hashes, `git diff --check`, and closed P1-C/P1-D/P1-E gates; verdict `PASS`. This accepts only the P1-B Supervisor gate; detect-changes and isolated commit remain pending. | recorded |
| P1-B | `E1-P1B-DETECT1` | recorded after mandatory fresh graph refresh on the final accepted candidate: `anvien analyze --force --json` PASSed with `1,564` scanned, `677` parsed-code, `0` failed, `85,247` nodes, and `124,149` relationships. `anvien detect-changes --repo E:\Anvien --scope all --json` exited `0` with overall risk `low`, `50` changed symbols in `7` changed files, `0` affected symbols, `6` affected-file summaries, and no affected process. Changed layers are backend `34` and docs `16`; changed functional areas are providers `34` and documentation `16`; file-layer changed-file risk is `high` for the already-reviewed provider owner. Five changed resolution-gap occurrences/entities are fully classified (`3` analyzer-gap, `2` non-actionable; `2` unresolved access, `1` unresolved call, `2` builtin type references), while `changedSourceNodesWithGaps=0`, `totalResolutionGapCount=0`, `degradedNodes=0`, and `nodesWithGaps=0`. The seven graph-visible changed files are exactly the four Child ledgers plus `internal/providers/tsjs/definitions.go`, `internal/providers/tsjs/nodes.go`, and `internal/scopeir/facts.go`; the trace was captured under repo-local `.tmp` only for verification and removed after ledger recording. | recorded |
| P1-B | `E1-P1B-COMMIT1` | recorded: isolated commit `6b3a3fc4480c7f4c2291d8004207f50b52d91d21` (`fix(scopeir): preserve TSJS definition selection ranges`) contains the accepted P1-B production/test/ledger/report boundary; the post-commit worktree was clean before P1-C pre-flight opened | recorded |
| P1-C | `E1-P1C-IMPACT1` | recorded at clean implementation HEAD `6b3a3fc4480c7f4c2291d8004207f50b52d91d21`: mandatory `anvien analyze --force --json` exited `0`; `.anvien/meta.json` and `anvien status` agree on current commit/stale=false with `1,564` files, `85,248` nodes, and `124,150` relationships. Fresh full file-detail reports `stale=false/changedSinceAnalyze=false`, `46` related files for `internal/resolution/indexes.go`, and `42` for inspect-only `internal/resolution/emit.go`. Exact upstream impacts with tests are `graphIDForDef` CRITICAL `23` symbols / `13` files / `3` modules / `18` processes; `buildWorkspace` CRITICAL `41/17/7/28`; inspect-only `emitDefinitionNodes` CRITICAL `24/9/6/35`; inspect-only `emitter.emitNode` CRITICAL `6/5/2/19`. Source inspection confirms all 13 production definition providers create deterministic provider occurrence IDs from repo-relative file path, declaration start line/column, label, and name; scanner supplies normalized repo-relative paths; lexical membership is separately retained by `OwnedDefIDs -> scopeByDef`. The active path remains `ScopeIR -> buildWorkspace -> graphIDForDef/defsByFile -> emitDefinitionNodes`; `internal/graphidentity/**` and `internal/graph/declaration_occurrences.go` do not exist or participate at this HEAD. The editable owner remains only `graphIDForDef`/`buildWorkspace` in `internal/resolution/indexes.go`; projection, mutation, standalone `BuildDefinitionIndex`, providers, relationship merge, persistence, readers, and target remain inspect/preserve-only. CRITICAL is retained as a blast-radius warning, not an edit ban. | recorded |
| P1-C | `E1-P1C-SOURCE1` | recorded: `internal/resolution/indexes.go` remains the only production owner changed. `buildWorkspace` passes the existing `scopeByDef[DefinitionFact.ID]` lexical membership into `graphIDForDef`; the graph identity is an unambiguous length-prefixed tuple of normalized repository-relative file, semantic/qualified name, optional arity, provider occurrence ID, lexical scope ID, and owner ID, with the provider label retained as the graph-ID meaning prefix. Nil arity remains distinct from zero. No hash, random/absolute/order input, two-tier topology, new package, provider rewrite, projection edit, mutation policy, relationship change, persistence/readers, or target operation was added. | recorded |
| P1-C | `E1-P1C-BUILD1` | recorded: `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\full-build.ps1` exited `0` after `214.1s`. The repo-native full build installed dependencies, built the packaged Go/Ladybug runtime, built the web (`tsc -b && vite build`), built launcher/server assets, installed CLI `1.2.8`, and completed built-CLI self-analyze with `1,566` scanned, `679` parsed-code, `0` failed, `95,539` nodes, and `134,405` relationships. Vite emitted only existing chunk-size/dynamic-import warnings; no build failure occurred. | recorded |
| P1-C | `E1-P1C-TEST1` | recorded: production code preceded QA updates. `identity_occurrence_test.go` plus independently authored `testdata/p1c_identity_repo/src/identity.ts` prove two `time`, two `now`, same-lexeme interface/value meaning lanes, lexical-scope and owner sensitivity, nil/zero arity separation, path-separator normalization, reordered fact determinism, no name-only merge without provider evidence, `defsByFile` occurrence conservation, and relationship endpoint existence. `go test ./internal/resolution -run '^TestP1CIdentity' -count=1 -v` PASSed `4/4`; final `go test ./internal/resolution -count=1` PASSed. The first full-package run exposed only legacy literal-ID assertions; after fresh Anvien QA file-detail/impact (all changed test functions LOW/0 upstream; `buildGraphSignature` LOW `2` symbols/`2` files/`1` test module/`0` processes), those tests resolve the intended semantic nodes while identity injectivity stays owned by the new P1-C tests. One retry was shell-timeout-only and the unchanged command then completed PASS. The first post-build 61-package regression then exposed the same stale literal-ID assumption in `internal/providers/provider_parity_test.go`; emitted heritage relationships were present. After a fresh graph, file-detail reported `117` related files / low file risk and exact test impact was LOW with `0` upstream symbols/processes. The QA-only assertion now resolves actual semantic graph nodes; its 12-language focused matrix and full `internal/providers` package PASS. The exact product regression generated by `go list -e ./...`, excluding only the measured 11 non-standalone `*/anvien/test/fixtures/*` corpus packages, PASSed `61/61` packages on rerun. | recorded |
| P1-C | `E1-P1C-ORACLE1` | recorded: after the full build, direct packaged runtime `.\anvien\bin\anvien.exe analyze E:\Anvien\.tmp\p1c-runtime-260811 --force --json` parsed the copied stable fixture `1/1` with `0` failed files and produced `20` nodes / `19` relationships. The tracked fixture and runtime copy SHA-256 both equal `327F6BD02FCB341078902FE4302CAB03A2077DDFE98B379164C17E96B547A078`. Read-only graph inspection found exactly `10` source definitions, `10` `DEFINES` relationships, `10` unique definition IDs, two distinct `time` nodes at lines `5/10`, two distinct `now` nodes at lines `15/20`, separate `Shared` Function/Interface meaning lanes, and `0` missing relationship endpoints. This is the nearest normal built-runtime P1-C boundary; it does not run or claim the P1-E target oracle. | recorded |
| P1-C | `E1-P1C-REVIEW1` | recorded: first zero-trust report `reports/Supervisor/rp_supervisor_260811_090631_by_gpt-5-codex_child01_p1c.md` independently clears production, current-source runtime oracle, focused/package/provider QA, `61/61` product regression, impact boundary, and later-slice preservation but returns `REJECT` for contradictory living actual-status prose. Only that ledger invariant changed. History-closure report `reports/Supervisor/rp_supervisor_260811_092330_by_gpt-5-codex_child01_p1c_history_closure.md` verifies unchanged production/direct-test/provider-test SHA-256 values, `50/50` consistency assertions, `git diff --check`, pending detect/commit rows, and closed P1-D/P1-E/target boundaries; verdict `PASS`. This accepts only the P1-C Supervisor gate. Nine exact repo-local debug artifacts were then removed while historical `.tmp/p1c0*` artifacts were preserved; detect-changes and isolated commit remain pending. | recorded |
| P1-C | `E1-P1C-DETECT1` | recorded after exact cleanup and mandatory fresh analyze (`1,570` scanned, `679` parsed-code, `0` failed, `95,575` nodes, `134,442` relationships): staged `anvien detect-changes --repo E:\Anvien --scope all --json` exits `0` with overall risk `medium`, `223` changed symbols in all `19` accepted paths, file-layer changed-file risk `high`, and one affected five-step process (`Main -> CleanPath`) whose only changed step is `buildWorkspace` at step `4`. Changed app layers are backend `26`, backend-test `139`, and docs `58`; changed functional areas are documentation `28`, providers `3`, reporting `30`, and resolution `162`. The exact 19-file inventory is six documentation/ledger files, `internal/providers/provider_parity_test.go`, eight resolution production/QA files, the P1-C fixture, and three coder/Supervisor reports. Eleven changed gap occurrences/entities are fully classified: actionability `9` analyzer-gap / `2` non-actionable; classification `9` in-repo unresolved / `2` builtin; fact families `1` access / `6` call / `4` type-reference; kinds `1` unresolved access / `6` unresolved call / `4` unresolved type reference; target roles `6` callable / `1` member / `4` type; targets `Date.now=8`, `def.ID=1`, `len=1`, `string=1`. They are changed-site inventory only: `changedSourceNodesWithGaps=0`, `totalResolutionGapCount=0`, `degradedNodes=0`, and `nodesWithGaps=0`. Semantic app-layer and functional-area status is complete across all `95,575` graph nodes with `0` missing fields/sources. Full JSON was retained under repo-local `.tmp` only through ledger verification and removed before commit. | recorded |
| P1-C | `E1-P1C-COMMIT1` | recorded: the accepted P1-C production/QA/fixture/ledger/report boundary is committed immediately after this evidence row with message `fix(resolution): conserve declaration occurrence identities`; the final hash is reported from Git because a commit cannot contain its own final hash | recorded |
| P1-D | `E1-P1D-IMPACT1` | exact collision path, callers, and legitimate-operation impact | pending |
| P1-D | `E1-P1D-SOURCE1` | evidence-scoped collision/loss correction | pending |
| P1-D | `E1-P1D-BUILD1` | full build | pending |
| P1-D | `E1-P1D-TEST1` | conflict, enrichment, occurrence, and endpoint tests | pending |
| P1-D | `E1-P1D-COLLISION1` | zero silent collision/skip/replacement proof | pending |
| P1-D | `E1-P1D-REVIEW1` | unconditional Supervisor PASS | pending |
| P1-D | `E1-P1D-DETECT1` | Anvien detect-changes | pending |
| P1-D | `E1-P1D-COMMIT1` | isolated slice commit | pending |
| P1-E | `E1-P1E-BUILD1` | final full build before integration validation | pending |
| P1-E | `E1-P1E-DETERMINISM1` | matched repeated-run canonical identity comparison | pending |
| P1-E | `E1-P1E-INTEGRITY1` | occurrence conservation, collision, range, and endpoint integrity result | pending |
| P1-E | `E1-P1E-TARGET1` | bounded target `time`/`now` result `4/4` | pending |
| P1-E | `E1-P1E-BOUNDARY1` | target source/worktree preservation proof | pending |
| P1-E | `E1-P1E-REVIEW1` | unconditional Supervisor PASS | pending |
| P1-E | `E1-P1E-DETECT1` | Anvien detect-changes | pending |
| P1-E | `E1-P1E-COMMIT1` | isolated validation-slice commit | pending |

## Closure Evidence

| Evidence ID | Required proof | Status |
|-------------|----------------|--------|
| `E2-PNA-SUPERVISOR1` | independent acceptance of complete Child 01 source/diff/runtime/evidence/benchmark | pending |
| `E2-PNB-CLEANUP1` | dead-work inventory, removal, and Supervisor cleanup PASS | pending |
| `E2-PNC-DETECT1` | final detect-changes result for the accepted Child scope | pending |
| `E2-PNC-HANDOFF1` | exact accepted identity/range facts and evidence used to refresh Child 02 | pending |
| `E2-PNC-COMMIT1` | final closure commit and known worktree state | pending |
