# Anvien TypeScript Binding-Pattern Extraction Evidence Ledger

## Metadata

- Date: `2026-07-28`
- Plan: `docs/plans/2026-07-26-anvien-graph-accuracy-multi-plan/2026-07-28-03-typescript-binding-pattern-extraction/2026-07-28-03-typescript-binding-pattern-extraction-plan.md`
- Evidence: `docs/plans/2026-07-26-anvien-graph-accuracy-multi-plan/2026-07-28-03-typescript-binding-pattern-extraction/2026-07-28-03-typescript-binding-pattern-extraction-evidence.md`
- Benchmark: `docs/plans/2026-07-26-anvien-graph-accuracy-multi-plan/2026-07-28-03-typescript-binding-pattern-extraction/2026-07-28-03-typescript-binding-pattern-extraction-benchmark.md`
- Actual status: `docs/plans/2026-07-26-anvien-graph-accuracy-multi-plan/2026-07-28-03-typescript-binding-pattern-extraction/2026-07-28-03-typescript-binding-pattern-extraction-actual-status.md`
- Roadmap: `docs/plans/2026-07-26-anvien-graph-accuracy-multi-plan/2026-07-26-anvien-graph-accuracy-roadmap.md`

## Evidence Rules

- The problem-origin report establishes why the multi-plan exists. It explicitly labels its proposed architecture DRAFT; proposed implementation is not accepted evidence.
- The causal synthesis and final Supervisor report verify the bounded investigation only. They do not approve remediation or establish global TypeScript accuracy.
- Current production facts require fresh source, graph, file-detail, impact, runtime, and test evidence. Historical file paths are investigation leads, not current ownership proof.
- A pending evidence ID is a reserved target, not proof.
- Each implementation slice requires the complete evidence set named in the plan: impact, source change, full build, behavior test, real boundary, Supervisor, detect-changes, and commit.
- Long counts and before/after metrics belong in the benchmark ledger.
- No target source or target-side report may be used as a plan artifact.

### Evidence ID Naming

Evidence IDs use `E<phase>-<item>-<kind><n>`. IDs are never reused for a different fact. Exact IDs, not broad section names, must be cited by actual status and benchmark rows.

## E0 - P0 Evidence

Matching plan item: `P0-A`

### Completed documentation-authority evidence

- `E0-P0A-HANDOFF1`: recorded by orchestration/main in `reports/Investigation/rp_main_260814_020654_child02_pnc_closure_handoff.md`. Accepted Child 02 evidence guarantees exact Graph JSON/Ladybug Definition field and record preservation, optional SelectionRange all-or-none/NULL semantics, real numeric zero preservation, exact `DEFINES` endpoint conservation, explicit `CodeEmbedding.label` persistence/consumption, exact affected readers C09-C16 `8/8`, and repeated analyze/failure/recovery `7/7`. Source commits are P2-B `4d456446fcc49aed0c6d489aa9c63e00d030b53c`, P2-C `927a676653963e8001d7789291010d5b819bac83`, P2-D `35939e7e6a621593d3d3065b9493a97c2c9a4f25`, P2-E `593e77a3f36c78447864a906a75c05e0d89530cc`, Pn-A `e47acfad927425621c3f9048d0a23eed513444a5`, and Pn-B `9b65f3ef3f3f377d0dcb4a9e0cd5eca444cf51c6`. Explicit non-claims: Child 02 does not prove current binding-leaf extraction, binding paths, variable/parameter/catch/loop contexts, unsupported diagnostics, binding graph/reference outcomes, affected Child 03 owners, or the target `6/6`. P0-A must refresh all current graph/source/file-detail/impact facts before production work.
- `E0-P0A-RULE1`: on 2026-08-10, `E:\Anvien\AGENTS.md` was read in full before editing. It requires planner use for plan work, current graph refresh before graph evidence, file-detail and impact before relevant code edits, code before tests, full build before validation, Supervisor acceptance, detect-changes, and per-slice commits.
- `E0-P0A-SKILL1`: the planner skill and all four standard templates were read in full before rewriting this plan set. The rewritten files retain plan, evidence, benchmark, and actual-status roles with exact checklist/evidence mapping.
- `E0-P0A-ORIGIN1`: `reports/problem/2026-07-26-tinh-chinh-cac-van-de-cua-anvien` was read in full. It identifies six missing destructured bindings and proposes recursive pattern semantics, but explicitly states that its architecture is DRAFT. This ledger uses its defect description and acceptance target, not its unapproved architecture.
- `E0-P0A-VERIFY1`: `reports/Supervisor/260726_165317_graph_root_cause_causal_synthesis.md` was read in full. Bounded finding C2 confirms six legal array-binding names existed in the TS AST and were absent from graph definitions because the investigated variable-declarator path returned before fact creation. The report expressly provides no remediation authorization.
- `E0-P0A-VERIFY2`: `reports/Supervisor/rp_supervisor_260726_170048_by_gpt-5-6-sol_final_bounded_investigation.md` was read in full. Its PASS accepts the bounded causal record only, including the six missing bindings; it does not approve a fix or claim global accuracy.
- `E0-P0A-ANVIEN1`: before these eight ledger rewrites, `anvien status` reported the self-index up to date at commit `238aec0`. Concept queries were run for binding-pattern and export ownership; their broad ranking did not establish an exact binding owner. They are therefore not used to close P0, and graph freshness must be renewed after documentation changes.
- `E0-P0A-SCOPE1`: the corrected Child 03 scope retains binding extraction, declaration contexts, graph/reference projection, and the `6/6` target gate. It excludes unrelated export, module, ambient, artifact, and consumer work.

### Required current-state evidence

| Evidence ID | Required proof | Status |
|-------------|----------------|--------|
| `E0-P0A-GRAPH1` | fresh analyze and current HEAD/source basis after plan correction | accepted; temporal basis preserved |
| `E0-P0A-SRC1` | full current-source inventory of binding facts, all declaration contexts, diagnostic exits, projection, resolution, and relevant tests | accepted; exact manifest independently recomputed |
| `E0-P0A-FD1` | exact `file-detail` outputs and related-file counts for every frozen owner | accepted `27/27` |
| `E0-P0A-IMPACT1` | exact upstream impacts and blast-radius report for every candidate function/method/exported symbol | accepted `15/15` files and `39/39` symbols |
| `E0-P0A-STATUS1` | completed current status matrix, touch map, R2 refresh, and Final P0 Decision | accepted at P0 capture; P0-A later committed as `17bd4584` |
| `E0-P0A-REVIEW1` | fresh independent Supervisor review of the corrected manifest and P0-A boundary | accepted `PASS`; report/hash recorded below |
| `E0-P0A-DETECT1` | fresh graph basis and `anvien detect-changes --repo E:\Anvien --scope all` for the isolated docs/evidence boundary | accepted; docs-only LOW-risk result recorded below |
| `E0-P0A-COMMIT1` | isolated P0-A documentation/evidence commit and post-commit worktree/index state | recorded by the immediate commit boundary; final hash reported by Git externally |
| `E0-P0A-REVIEW2` | fresh Supervisor re-review of the byte-clean QA report actually staged for commit | accepted `PASS`; report/hash recorded below |

### P0-A current-state candidate (2026-08-14)

- `E0-P0A-GRAPH1`: at entry HEAD `181b8cb800f5fe34fa6fe85ddd359f514ead9fb0` on `master`, `anvien --help` exited `0`. The first `anvien analyze --force` wrapper timed out after `5.025s` with wrapper exit `124` and no stdout; process/lock inspection proved PID `23376` was the live analyze process, it exited normally, its lock released, and meta publication was observed. That attempt is not called PASS. The sole complete invocation capture, and the only graph evidence used here, was `anvien analyze --force`, exit `0`, scanned `1,629`, parsed code `688`, failed `0`, nodes `97,340`, relationships `136,614`, artifact `E:\Anvien\.anvien\graph.json`, indexed/current commit `181b8cb800f5fe34fa6fe85ddd359f514ead9fb0`. One planned `anvien status` then reported indexed `2026-08-14 02:34:41 +07`, up to date. No further analyze was run.
- `E0-P0A-SRC1`: full-read Set A/B/C exact path manifest, per-path responsibility/mode/rationale, canonical sorted path list, and reproducible partition checks are durable in the QA report section `Durable Set A/B/C manifest and partition checks`. The authorized correction pass used one fresh analyze temporal basis (`1,631/688/0`, `97,369` nodes, `136,643` relationships); prior accepted file-detail/impact metrics remain explicitly preserved at their original capture boundary. Exact checks: `|A|=62`, `|B|=27` (`15` production + `12` exact test owners), `|C|=35`, duplicates `0`, `B∩C=0`, `A\(B∪C)=0`, `(B∪C)\A=0`, mandatory leads assigned `27/27`, unassigned `0`. The provisional `40` reconciliation lists all 13 removed rows and their final C classification. Current source proves: `variable_declarator` accepts only identifier names and silently returns for patterns; no recursive pattern walker exists; `BindingFact` exists, but neither it nor the other current ScopeIR contracts carry pattern path/rest/default/provenance or an extraction-diagnostic contract; parameter paths may have a separate TypeBindingFact, but no parameter-specific DefinitionFact/local-binding record with pattern metadata, and catch scope/binding handling is missing; declaration-form loop identifiers can emit in an enclosing function/module scope while loop patterns are rejected; assignment-form loops create no declarations, but plain-identifier loop writes and assignment-destructuring write/reference behavior are not implemented or fully proven; Definition emission is independent of type-inference success but both paths independently reject non-identifiers; imports are a separate pipeline to preserve; accepted definitions project one-for-one to nodes/`DEFINES`; lexical resolution walks inner-to-parent scope and ambiguity fails; omitted leaves therefore create no Definition/local binding and downstream gaps remain; graphaccuracy readers consume generic existing graph/diagnostic truth and do not reconstruct omitted leaves.
- `E0-P0A-FD1`: exact `anvien file-detail <path> --repo E:\Anvien --json` completed for all `27/27` frozen owners, exit `0`, stale `false`, changed-since-analyze `false`, indexed/current commit equal to entry HEAD. The report records every related-file and relationship metric. Production file-detail risk distribution is high `14`, medium `1`; exact test-owner risk is low `12`.
- `E0-P0A-IMPACT1`: exact upstream file impact completed for all `15/15` production owners with stale `0`; severity distribution `CRITICAL 9`, `HIGH 2`, `MEDIUM 1`, `LOW 3`. Final upstream symbol proof used only canonical UIDs obtained from the owning file-detail: `39/39` found, stale `0`, exit nonzero `0`, ambiguous/not-found/unresolved `0`; severity `CRITICAL 14`, `LOW 25`. The provisional symbol count `38` was an arithmetic error before canonical execution: the reconciled categories are TS/JS `15` + ScopeIR `11` + import `2` + resolution/projection `7` + audit readers `4` = `39`. Rejected exploratory attempts—bare-name ambiguity, `collector.*` not-found, an empty-UID CLI argument error, and two wrapper timeouts—are disclosed in the QA report and are not evidence. HIGH/CRITICAL are blast-radius warnings, not prohibitions.
- `E0-P0A-STATUS1`: at its P0 capture boundary, R2 and correction row `R2-C1` pointed to the exact manifest/checks in the QA report and P3-A stayed closed pending review/commit. That historical gate was later satisfied by the accepted P0-A commit `17bd45845a218dccfa7cb09bde8195a14693bf50`; P3-A subsequently opened and is now in repair after `E3-P3A-REVIEW1` REJECT.

All five P0-A current-state IDs were independently Supervisor-accepted with no unresolved owner boundary. The isolated documentation/evidence step later closed at commit `17bd45845a218dccfa7cb09bde8195a14693bf50`. This paragraph records the P0-A transition only; it does not itself accept later P3-A work, whose separate acceptance is `E3-P3A-REVIEW3`.

### Independent P0-A Supervisor acceptance

- `E0-P0A-REVIEW1`: fresh zero-trust Supervisor re-review returned `PASS` in `reports/Supervisor/rp_supervisor_260814_045821_by_gpt-5_child03_p0a_inventory_rereview.md` (SHA-256 `9833B915407B5D4C9680C306F8E53C8A209B522818201462C2AE8D4495952CDC`, `11,327` bytes). It independently verified `62` existing unique manifest rows with complete metadata, Set B `27` (`15` production + `12` tests), Set C `35`, zero duplicates/overlap/union gaps, all 13 provisional-40 removals mapped to C exactly once, preserved prior graph/file-detail/impact evidence and temporal separation, unchanged prior REJECT provenance, and the exact six-path pre-report dirty boundary with staged `0`. No production implementation, build, test, runtime, target, or later-slice acceptance is implied.

### P0-A detect/commit closure

| Evidence ID | Required proof | Status |
|-------------|----------------|--------|
| `E0-P0A-DETECT1` | fresh graph basis and `anvien detect-changes --repo E:\Anvien --scope all` for the isolated docs/evidence boundary | accepted; staged confirmation follows |
| `E0-P0A-COMMIT1` | isolated P0-A documentation/evidence commit and post-commit worktree/index state | accepted and committed as `17bd45845a218dccfa7cb09bde8195a14693bf50` |

- `E0-P0A-DETECT1`: after `anvien --help` exited `0`, the final fresh `anvien analyze --force` exited `0` at the complete staged-boundary temporal basis `1,633` scanned / `688` parsed-code / `0` failed, graph `97,388` nodes / `136,662` relationships, artifact `E:\Anvien\.anvien\graph.json`. Final `anvien detect-changes --repo E:\Anvien --scope all --json` exited `0` with `8` changed and `8` affected docs/reports, `62` changed sections, LOW risk, `0` affected processes, `0` affected flows, `0` changed ResolutionGap entities, and `0` degraded nodes; changed app layer is docs (`62`) and functional areas are documentation (`21`) plus reporting (`41`). An earlier pre-clean-report capture (`1,632/688/0`, `4/4`, `21`) is retained only as temporal history. This is documentation/evidence impact only; no production, test, target, runtime, build, or behavior claim is made.
- `E0-P0A-REVIEW2`: after removing three report-only trailing whitespace characters to satisfy `git diff --check`, a fresh Supervisor review independently recomputed the exact QA artifact at SHA-256 `5B2A567E965521C680B9254873C8049DA69EB3A3F8C21767AF5C13B215DF4857`, `36,873` bytes, and returned `PASS` in `reports/Supervisor/rp_supervisor_260814_051244_by_gpt-5_child03_p0a_clean_report.md` (SHA-256 `023AD68F321B9C7755E52C56FCDDD41F761BD1CAF0DE38583443207AE0D96C43`, `6,483` bytes). It reverified `62/27/35` membership, canonical equality, zero partition defects, and the staged docs-only boundary. No implementation or later-slice acceptance is implied.

## E3 - P3 Evidence

Rows below preserve the complete review and commit history. P3-A through P3-B2A retain their accepted isolated boundaries; P3-C is committed at `656a0445ff3e25b6225b994cdaf7cf1b35eb665c`; P3-C2 is committed at `8784c6c21da842b188f136b95ec97ab8df9f20e8`. Aggregate `E3-PNA-REVIEW1` independently accepts all seven slices and closes the complete current Child 03 binding-pattern invariant. Removed historical artifacts are not present and not relied upon under latest Owner authority; their absence is not an evidence-integrity gate. `Pn-B` is the sole open lane.

### P3-A — binding contract and recursive walker

| Evidence ID | Required proof | Status |
|-------------|----------------|--------|
| `E3-P3A-IMPACT1` | fresh file-detail/impact and one-responsibility ownership for exact editable owners | accepted by `E3-P3A-REVIEW3`; HIGH/CRITICAL blast radius recorded |
| `E3-P3A-SRC1` | production fact/walker and structured unsupported-diagnostic change | accepted at the P3-A helper/contract boundary by `E3-P3A-REVIEW3` |
| `E3-P3A-BUILD1` | full build after production and focused tests | clean-gated direct build PASS; source hashes remained unchanged through final review |
| `E3-P3A-TEST1` | deterministic general pattern matrix; one fact per legal leaf | post-build focused/regression tests PASS; accepted by `E3-P3A-REVIEW3` |
| `E3-P3A-BOUNDARY1` | real ScopeIR extraction output and diagnostic count | legal/malformed matrix PASS; accepted by `E3-P3A-REVIEW3` |
| `E3-P3A-REVIEW1` | first independent Supervisor review | `REJECT`; immutable first-candidate history |
| `E3-P3A-DETECT1` | repair detect captured before roadmap reconciliation | historical `292` changed symbols / `9` tracked files; not acceptance evidence |
| `E3-P3A-REVIEW2` | second independent Supervisor review of the repaired candidate | `REJECT`; sole residual blocker was stale/incomplete `E3-P3A-DETECT1` |
| `E3-P3A-DETECT2` | fresh durable detect-resubmission package after settled roadmap/ledger state | accepted `294` changed symbols / `10` tracked files plus direct untracked-walker manifest |
| `E3-P3A-REVIEW3` | final independent Supervisor review of source, history, and `E3-P3A-DETECT2` | `PASS`; no P3-B or later-slice acceptance |
| `E3-P3A-PLAN1` | post-PASS planner refresh and document blast-radius gate | PASS; five plan documents LOW with zero affected files/processes/flows |
| `E3-P3A-DETECT3` | final orchestration-owned pre-commit detect after post-PASS ledger refresh | PASS: `296` changed symbols / `10` changed files / `10` affected files |
| `E3-P3A-COMMIT1` | isolated accepted slice commit | this commit boundary; exact hash reported by Git/handoff |

The `E3-P3A-DETECT1` and `E3-P3A-REVIEW1` paragraphs immediately below are retained as historical checkpoint wording from before the second and third reviews. Their current classifications are controlled by the table above and the later `REVIEW2`/`DETECT2`/`REVIEW3` entries; they are not current acceptance claims.

- `E3-P3A-IMPACT1`: fresh graph basis after the repair ledger/source state is `anvien analyze --force` exit `0`, `1,638` scanned / `689` parsed-code / `0` failed, graph `97,876` nodes / `137,485` relationships, indexed/current commit `df2efe7811553e6a3024792ad8aaab04a1c99fc0`, and all six file-detail/impact commands exit `0` with `stale=false`. File-detail summaries are walker `64` symbols / `29` inbound / `47` outbound / `110` local / `79` unresolved (high); `facts.go 164/923/22/148/5` (medium); `ir.go 47/952/34/75/138` (high); `sort_keys.go 96/246/102/41/109` (high); `extract_test.go 171/13/112/62/0` (low); `scopeir_test.go 83/0/47/9/0` (low). Upstream impact is walker CRITICAL `33` impacts / `18` files / `9` direct; facts CRITICAL `311/75/102`; ir CRITICAL `114/29/49`; sort_keys HIGH `23/3/20`; both authorized test owners LOW `0/0/0`. These are blast-radius warnings; no inspect-only owner was edited.
- `E3-P3A-SRC1`: current source adds only `internal/providers/tsjs/binding_patterns.go` plus the three exact ScopeIR contract owners. The walker recognizes legal `undefined` pattern leaves, carries array/object parent context for rest legality, rejects private keys, invalid object-rest and assignment-pattern wrappers, guards malformed subtrees with `HasError()`, and emits structured diagnostics. New ScopeIR collections deep-clone their nested selection/path/index state in `Normalized` and `NormalizeOwned`; deterministic comparators cover all new fields. Authorized tests add the four legal `undefined` cases, nested array-rest control, four exact malformed/context-invalid fixtures, and independent clone/determinism inputs. No declaration-context, graph/resolution/persistence/import/export/target wiring is present.
- `E3-P3A-BUILD1`: root `package.json` maps `npm run full-build` to `powershell -ExecutionPolicy Bypass -File .\scripts\full-build.ps1`; source inspection confirms CLI package/build/install/version, launcher Web/CLI/server build, and final analyze. The valid direct gate inspected six artifacts (repo CLI/DLL, launcher/server, global CLI/DLL), doctor processes `[]`, lock `free`, Restart Manager initial/final holder counts `0` for all six, discovered process tree `[]`, killed set `[]`, and exclusive-open `6/6`. Direct `npm run full-build` exited `0` after `150.7s`; `anvien version` was `1.2.8`; build-time analyze was `1,638/689/0`, graph `97,876/137,485`. A transient post-build global-artifact exclusive probe failed, then immediate Restart Manager/doctor/lock rescan and retry exclusive-open returned zero holders and `6/6`; no retry build was needed.
- `E3-P3A-TEST1`: post-build `go test -count=1 ./internal/providers/tsjs ./internal/scopeir` exited `0` (`tsjs 0.348s`, `scopeir 0.841s`); focused rejected-invariant tests `go test -count=1 -run 'TestExtractBindingPattern|TestBindingCollections' ./internal/providers/tsjs ./internal/scopeir` exited `0` (`tsjs 0.277s`, `scopeir 0.834s`); identifier/import/boundary regression `go test -count=1 -run '^(TestExtractTypeScriptScopeIR|TestExtractJavaScriptScopeIR|TestP1BPreservesBindingPatternBoundary)$' ./internal/providers/tsjs` exited `0` (`0.285s`).
- `E3-P3A-BOUNDARY1`: nearest real package-local ScopeIR matrix has `5/5` primary leaves and `2/2` nested leaves; `[a,,b]` preserves indexes `0/2`; aliases/defaults/rest/computed/static paths, wrapper ranges, identifier-token selections, and separate construct/pattern provenance are deterministic. Legal `[]`, `{}`, `[,,]` produce zero leaves/zero diagnostics. The four legal `undefined` snippets each produce one leaf with expected name/path/rest flag. The four exact malformed/context-invalid snippets each produce zero leaves plus exactly one `DiagnosticUnsupportedBindingNode` carrying site/node/reason/path/provenance; invalid-rest and missing-pattern controls likewise produce one structured diagnostic. Clone/determinism tests prove independent top-level and nested ownership and byte-identical normalized marshal order.
- `E3-P3A-DETECT1`: after the ledger snapshot, final `anvien analyze --force` and `anvien detect-changes --repo E:\Anvien --scope all` were run. The raw stdout artifact (including the literal footer delimiter `---` and `**Next:** ...`) is `reports/coder/rp_coder_260814_092104_by_gpt-5_child03_p3a_repair_detect_changes_raw.txt`; the structured JSON prefix/parser artifact is `reports/coder/rp_coder_260814_092104_by_gpt-5_child03_p3a_repair_detect_changes.json`. The command exited `0` with `292` changed symbols, `9` changed/affected files, `2` affected process steps, summary risk `medium`, file-layer risk `high`, and ResolutionGap delta `67` entities / `69` occurrences (`2` analyzer-gap / `65` non-actionable), current resolution health `0` gaps / `0` degraded nodes. Raw/JSON byte sizes and SHA-256 values are recorded after final capture in the durable repair coder report; the raw artifact remains authoritative and the JSON artifact is only the structured parser boundary. The untracked walker is explicitly covered by Git/source hashes because detect-changes enumerates tracked graph files only.
- `E3-P3A-REVIEW1`: independent review returned `REJECT` in `reports/Supervisor/rp_supervisor_260814_085244_by_gpt-5_child03_p3a.md` (`20,888` bytes, SHA-256 `873182C966A0EC4ACBF77938AC9BFF00715FDE025B1A78C07D9CBE67FC8A54EE`). The first coder report and detect artifacts remain immutable historical candidate evidence but do not cover the repair. `E3-P3A-COMMIT1` remains pending; no self-acceptance, stage, or commit is claimed.

The first-candidate report/artifacts remain immutable historical evidence only: their `236` changed-symbol / `5` tracked-file snapshot, old footer wording, and omitted untracked walker do not cover this repair. The repair capture above was later classified as historical `292/9` evidence by `E3-P3A-REVIEW2`; it is not the accepted resubmission package.

- `E3-P3A-REVIEW2`: the second independent review returned `REJECT` in `reports/Supervisor/rp_supervisor_260814_105416_by_gpt-5_child03_p3a_repair_rereview.md` (`19,952` bytes, SHA-256 `0D5157481A2899CC6CB45F36FFCF5953B37AAB90F7F247EA9B0BEAEEAE8EE309`). It independently cleared the source/test/docs/cleanup findings and rejected only the stale `292/9` detect boundary plus missing explicit current traceability for the untracked walker.
- `E3-P3A-DETECT2`: the fresh detect-resubmission package was captured after the settled tracked semantic state. Its durable report is `reports/coder/rp_coder_260814_112440_by_gpt-5_child03_p3a_detect_refresh.md` (`13,989` bytes, SHA-256 `BC0DD591ED82E3884AC99E947D37BC81DF8FB4DA61D9E4E25E4E4226C7D3D69B`); raw stdout is `reports/coder/rp_coder_260814_112440_by_gpt-5_child03_p3a_detect_refresh_raw.txt` (`536,736` bytes, SHA-256 `55B113DEC90CE96E62E3D62628E23D21A5A32083FE2B30E21EE1FB62C748195B`); the byte-identical JSON prefix is `reports/coder/rp_coder_260814_112440_by_gpt-5_child03_p3a_detect_refresh.json` (`536,579` bytes, SHA-256 `7400B8D5DEF55A5B1EEF2B8C52A91B829134D7E2A11FF0B04756A11B534CA7BA`); and the direct walker manifest is `reports/coder/rp_coder_260814_112440_by_gpt-5_child03_p3a_detect_refresh_walker_manifest.json` (`8,237` bytes, SHA-256 `2F7E3D17F5AE828918774F366679DC56E231165452FC969E0F0A35BA54DD2852`). The package records `294` unique changed symbols and identical `10/10` changed/affected tracked paths, `2` affected processes, the complete `67/69` gap aggregates, exact raw footer/parser boundary, and the untracked walker at `11,423` bytes / SHA-256 `2A9CA2BCA90182CEA69676C1906FFCE9F16A888205BE2F77C44A2C2CB40CBDB0` without falsely describing it as detector-enumerated.
- `E3-P3A-REVIEW3`: the final independent review returned `PASS` in `reports/Supervisor/rp_supervisor_260814_134848_by_gpt-5_child03_p3a_detect_resubmission.md` (`19,430` bytes, SHA-256 `25CE23B8C485CDAE975301CE4483BB8AF20878D08E78987A616131DFA7CC4D13`). It independently reproduced `294/10/10`, verified all source/test and artifact hashes, closed both prior REJECT histories, and confirmed no declaration-context, graph/resolution/persistence, target, P3-B, or later-slice wiring. Full build/tests were not repeated because all six source/test hashes still matched the clean-gated PASS boundary and intervening commits touched only the Owner-controlled orchestration skill.
- `E3-P3A-PLAN1`: after reading the planner skill and all four templates, orchestration ran `anvien --help` and a fresh `anvien analyze --force` at current/indexed HEAD `80639abd55fc6a943b1894a420bc73d3401fe4f0`; the refresh exited `0` with `1,645/689/0` scanned/parsed/failed and graph `97,971/137,580`. Full `file-detail` recorded the roadmap's `28` outbound plan links and one inbound roadmap link for each Child 03 ledger. Upstream file impact for the roadmap and all four ledgers was `LOW` with `0` impacted items, affected files, processes, flows, or tests. This proves only the post-PASS plan-governance edit boundary; it is not additional source acceptance.
- `E3-P3A-DETECT3`: after the post-PASS planner refresh, `anvien analyze --force` exited `0` at current/indexed HEAD `80639abd55fc6a943b1894a420bc73d3401fe4f0` with `1,645/689/0` scanned/parsed/failed and graph `97,971/137,580`. A full `anvien detect-changes --repo E:\Anvien --scope all` and a no-write `--json` confirmation both exited `0` with `296` changed symbols, `10` changed files, `10` affected files, summary risk `medium`, and file-layer risk `high`. The exact tracked path set remains the roadmap, all four Child 03 ledgers, `extract_test.go`, `facts.go`, `ir.go`, `scopeir_test.go`, and `sort_keys.go`; per-file changed-symbol counts are `2/9/6/8/4/62/45/50/32/78`. App-layer counts are backend `173`, backend-test `94`, docs `29`; functional-area counts are providers `267`, documentation `29`. The only affected processes are `proc_241_normalizeinplace` and `proc_560_normalizeinplace`, one changed `NormalizeInPlace` step each in `ir.go`. Gap aggregates remain `67` entities / `69` occurrences (`2` analyzer-gap, `65` non-actionable; `63` builtin, `2` in-repo unresolved, `2` standard-library), while current gaps / nodes with gaps / degraded nodes are `0/0/0`. The `+2` symbols versus `E3-P3A-DETECT2` are documentation-only results of the accepted planner refresh. The untracked walker remains direct-source traced at `11,423` bytes / SHA-256 `2A9CA2BCA90182CEA69676C1906FFCE9F16A888205BE2F77C44A2C2CB40CBDB0`; it is not falsely described as detector-enumerated. No P3-B or later-slice path appears.
- `E3-P3A-COMMIT1`: this isolated commit boundary contains the five plan documents, four production owners, two authorized test owners, and the three current machine-readable detect artifacts. It excludes Owner-only skill history and all review/handoff Markdown artifacts. Git and the post-commit orchestration handoff report the exact hash; P3-B remains locked pending Owner transfer.

The `236/5` first-candidate pair and `292/9` repair pair remain immutable historical evidence only. `E3-P3A-DETECT2` is the accepted resubmission proof; `E3-P3A-DETECT3` is the separate final pre-commit gate.

### P3-B — variable-declaration contexts

| Evidence ID | Required proof | Status |
|-------------|----------------|--------|
| `E3-P3B-IMPACT1` | exact variable-context file/symbol impact | accepted invariant; source-cleared and locked by `E3-P3B-REVIEW1`, retained unchanged by `E3-P3B-REVIEW2` |
| `E3-P3B-SRC1` | production variable-context integration, independent of type inference | accepted invariant; source-cleared and locked by `E3-P3B-REVIEW1`, retained unchanged by `E3-P3B-REVIEW2` |
| `E3-P3B-BUILD1` | full build | accepted invariant; fresh Supervisor build PASS and hash-locked through `E3-P3B-REVIEW2` |
| `E3-P3B-TEST1` | variable matrix, identifier regression, type-inference failure, assignment and import controls | accepted invariant; focused/targeted/ScopeIR/product regressions cleared and hash-locked through `E3-P3B-REVIEW2` |
| `E3-P3B-BOUNDARY1` | real ScopeIR variable facts with exact ranges/scopes/paths | accepted invariant; exact real-boundary behavior cleared and hash-locked through `E3-P3B-REVIEW2` |
| `E3-P3B-REVIEW1` | first independent Supervisor review | `REJECT`; immutable history, only `P3B-LEDGER-CURRENTNESS-1` blocked acceptance while every source/test/build invariant was locked |
| `E3-P3B-REVIEW2` | focused independent re-review of the repaired ledger-currentness invariant | `PASS`; no residual same-invariant surface and no later-slice acceptance |
| `E3-P3B-DETECT1` | staged pre-commit Anvien detect-changes result | accepted: `222` changed symbols / `13` changed files / `13` affected files; zero affected processes/flows; current health `0/0/0` |
| `E3-P3B-COMMIT1` | isolated accepted slice commit | accepted and committed as `17254549a13ad81a560c18fbcc6ab8fe3ce5f111` |

- `E3-P3B-IMPACT1`: baseline is branch `master`, HEAD/current/indexed commit `e54e706945aa3bdd450a9ac8462e626b199e288c`, initially clean worktree, and fresh opening `anvien analyze --force` exit `0` with `1,639` scanned / `689` parsed code / `0` failed, graph `97,887` nodes / `137,496` relationships, `stale=false`. Exact `file-detail` completed for the initial three candidate owners with `changedSinceAnalyze=false`: `definitions.go` has `51` symbols / `16` related files / HIGH relationship risk; `extract.go` has `34` symbols / `23` related files / HIGH relationship risk / one linked flow / five linked tests; `extract_test.go` has `171` symbols / `24` related files / LOW risk. Upstream file impact is `definitions.go` MEDIUM (`5` impacted symbols / `2` files / `0` flows), `extract.go` CRITICAL (`21` impacted symbols / `10` files / `1` flow), and `extract_test.go` LOW (`0/0/0`). Canonical exact-UID impact is `collector.emitDefinitionKind` LOW (`0` impacted symbols/files/processes), struct `collector` CRITICAL (`4` impacted symbols / `3` files / `8` process records through `newCollector` -> `Extract` -> analyzer/SFC), and `collector.result` LOW (`0` impacted symbols/files/processes). CRITICAL is a blast-radius warning, not an edit prohibition. Full source proves `definitions.go` cannot return accepted `BindingLeaves`/`ExtractionDiagnostics` by itself because collector storage and the only `ScopeIR` result projection live in `extract.go`; the approved production boundary therefore permits only two collector fields plus `result()` plumbing there and forbids traversal/refactor changes. After the historical stale-oracle failure, a second fresh analyze exited `0` at `1,639/689/0`, graph `98,521` nodes / `138,234` relationships, `stale=false`. `definition_position_inputs_test.go` file-detail reports `60` symbols, LOW risk, zero inbound refs, and unchanged/current graph state; upstream file impact is LOW `0/0/0`. Exact-UID impact for `TestP1BPreservesBindingPatternBoundary` is LOW with `0` impacted symbols/files/modules/processes/flows. Orchestration therefore opened only that function in the fourth file; no helper, sibling test, or new production owner is authorized.
- `E3-P3B-SRC1`: the production candidate changes only `internal/providers/tsjs/definitions.go` and the approved minimal plumbing in `internal/providers/tsjs/extract.go`. The preserved identifier branch still uses `addDefinition` unchanged. A non-identifier `variable_declarator` now invokes the accepted P3-A walker exactly once, stores its leaves/diagnostics, and emits one `NodeVariable` DefinitionFact, one OwnedDefID, and one local BindingFact for every returned legal leaf using the leaf's exact range/selection and lexical scope. It emits no declared/return type and never calls the separate `emitVariableTypeBinding` path for a pattern. `extract.go` adds only the two collector slices and maps them to `ScopeIR.BindingLeaves` / `ExtractionDiagnostics`; `Extract`, `walkKind`, traversal, parameters, catch, loops, assignment/reference logic, imports, P3-A contracts, and downstream graph code are unchanged.
- `E3-P3B-BUILD1`: before the first build, `anvien doctor locks --repo E:\Anvien --json` reported the analyze lock free. `anvien doctor processes --json` found five editor-owned MCP children executing the exact global `anvien.exe` artifact replaced by `npm install -g .` plus five `cmd.exe` parents; those ten build holders were terminated, then doctor showed only an unrelated Playwright test server and the lock remained free. Every later build gate reconfirmed the same free lock and no global `anvien.exe` holder. After the authorized stale-oracle bytes changed, the final gate again showed lock `free`, no global executable holder, and only the unrelated VS Code-owned Playwright test server. Final-byte root `npm run full-build` exited `0` after `101.1s`: CLI runtime/build/global install PASS, `anvien version` `1.2.8`, launcher/Web production build PASS (`2,943` modules), and final analyze PASS at `1,639` scanned / `689` parsed code / `0` failed with graph `98,530` nodes / `138,251` relationships. Earlier successful builds, including the prior `108.4s` three-file candidate build, were superseded by later source/test corrections. npm allow-scripts and Vite chunk/import messages were non-failing warnings. Timings are validation evidence, not P3-B benchmarks.
- `E3-P3B-TEST1`: the debug history is retained rather than hidden. The first focused attempt failed at compile because the new helper name collided with an existing package-test helper, then a P3-B-specific rename fixed it; the next attempt exposed equal-span module/function scope selection and drove an adapter-local non-module tie-break without editing `scopes.go`; the next attempt exposed two incorrect nested wrapper-range expectations and corrected only the test oracle to the accepted P3-A construct-range contract. The first targeted regression then exited `1`: seven selected P3-A/identifier tests passed, and only `TestP1BPreservesBindingPatternBoundary` failed because its then-locked historical oracle required zero destructured definitions. That attempt remains historical non-PASS evidence. After exact-function authorization and the final full build, `go test -count=1 -v -run '^TestP1BPreservesBindingPatternBoundary$' ./internal/providers/tsjs` PASS `1/1`; focused `go test -count=1 -v -run '^TestExtractVariableBindingPattern' ./internal/providers/tsjs` PASS `4/4`; targeted `go test -count=1 -v -run '^(TestExtractBindingPattern.*|TestExtractTypeScriptScopeIR|TestExtractJavaScriptScopeIR|TestP1BPreservesBindingPatternBoundary)$' ./internal/providers/tsjs` PASS `8/8`. Raw `go test -count=1 ./...` exited `1` only in five known compile-negative/non-standalone analyzer fixture packages (`go-map-range`, `go-method-enrichment`, `sample-code`, `go-make-builtin`, `go-type-assertion`) and is not PASS evidence. The repo-declared product package command from `anvien/package.json`, `go test ./cmd/... ./internal/... -count=1`, then exited `0` after `136.4s` for all `61/61` listed packages, including TSJS and ScopeIR.
- `E3-P3B-BOUNDARY1`: the passing real `Extract`/ScopeIR matrix proves array, object, nested, rest, and default paths for `7/7` leaves. Every leaf has exactly one DefinitionFact, one local BindingFact, one OwnedDefID, exact leaf/wrapper Range, exact identifier SelectionRange, `BindingContextVariable`, and the expected function scope even when module/function spans tie. The explicit inference-miss case emits `1/1` declaration/binding leaf with zero invented type facts. Identifier-only facts retain identical IDs/ranges/selections across controls; assignment destructuring adds `0` declarations; import facts have byte-identical delta `0`; the invalid-rest case emits one structured diagnostic and zero fake declarations. The transitioned definition-position oracle independently proves the top-level `left` `NodeVariable` fact has file path `src/binding-pattern.ts`, Range and SelectionRange both one-based-line/zero-based-byte/exclusive `[1:8,1:12)`, selection text `left`, exactly one module lexical owner, one OwnedDefID occurrence, and exactly one `BindingLocal` pointing to the same DefID. `git diff --check` passes; the manifest contains only the four ledgers plus exact two production/two test owners; no untracked or P3-B/debug `.tmp` artifact exists. This boundary was independently source/test-cleared and locked by `E3-P3B-REVIEW1`; `E3-P3B-REVIEW2` closes the only separate ledger-currentness blocker without changing its hashes.
- `E3-P3B-REVIEW1`: the first independent review returned `REJECT` in `reports/Supervisor/rp_supervisor_260814_170841_by_gpt-5_child03_p3b.md` (`17,373` bytes, SHA-256 `CD689543FE2ED6038C1AF2F1C5B7453889A06EE0F55E43AF1328844814D09CAD`). It independently source-cleared the exact two-production/two-test boundary, reran a holder-clean full build, passed the stale-oracle `1/1`, focused `4/4`, targeted `8/8`, ScopeIR `3/3`, and product `61/61` gates, and locked all impact/source/build/test/boundary plus plan/evidence/benchmark invariants. Its sole blocker was stale present-state wording in actual status, named `P3B-LEDGER-CURRENTNESS-1`; no cleared semantic invariant was rejected or reopened.
- The focused ledger repair is durable in `reports/coder/rp_coder_260814_171751_by_gpt-5_child03_p3b_ledger_currentness.md` (`9,109` bytes, SHA-256 `63E94BC8FBC51D8DB5F642606F45993D14BED99A0783EF4B6274598013BCAC16`). It changed only the two rejected present-state lines and appended R14. The repaired actual-status checkpoint before planner closure was `33,333` bytes / SHA-256 `EB6BC60251C4F9FB36A7F0D5D2C10DC178C75AE48828FEA83FEEC822949EF062`; all locked Go and companion-ledger hashes remained exact.
- `E3-P3B-REVIEW2`: the focused independent re-review returned `PASS` in `reports/Supervisor/rp_supervisor_260814_172406_by_gpt-5_child03_p3b_ledger_rereview.md` (`12,094` bytes, SHA-256 `55C5014E60716D3D52AE8F29B351F2DC193879F32641AF9E4D8683CF5C1AC447`). It reconstructed the rejected ledger checkpoint exactly, verified the repair-only delta and append-only R14, compared all four current ledgers, reverified every locked source/test/report hash, and found no residual same-invariant surface. Its scope accepts P3-B for orchestration planner/detect/commit closure only; parameter, catch, loop, assignment-write/reference, graph, resolution, persistence, target, and all later slices remain unaccepted and locked.
- `E3-P3B-DETECT1`: orchestration first ran `anvien --help`, confirmed the analyze lock free, and completed a fresh `anvien analyze --force` at current/indexed HEAD `e54e706945aa3bdd450a9ac8462e626b199e288c`: `1,643` scanned / `689` parsed code / `0` failed, graph `98,583` nodes / `138,304` relationships. Full `file-detail` plus upstream file impact for the roadmap and all four Child 03 ledgers were current/non-stale and LOW with zero affected files, processes, flows, or tests; the roadmap has `28` outbound plan links and each ledger has one inbound roadmap link. A provisional pre-stage detect enumerated the nine already tracked changed paths as `173/9/9`; it is retained only as temporal history and is not final evidence. After staging the exact 13-path manifest, final full and JSON `anvien detect-changes --repo E:\Anvien --scope all` confirmations exited `0` with `222` changed symbols and identical `13/13` changed/affected paths, summary risk LOW, and file-layer risk HIGH. Per-file changed-symbol counts are roadmap `2`, actual status `9`, benchmark `2`, evidence `4`, plan `4`, definition-position test `12`, `definitions.go` `69`, `extract.go` `19`, `extract_test.go` `52`, coder candidate report `12`, ledger-repair report `10`, first Supervisor report `13`, and focused Supervisor PASS report `14`. App-layer counts are backend `88`, backend-test `64`, docs `70`; functional areas are providers `152`, documentation `21`, and reporting `49`. Affected processes and flows are zero. ResolutionGap delta is `66` entities / `66` occurrences (`58` analyzer-gap / `8` non-actionable; `58` in-repo unresolved / `8` builtin), while current gaps / nodes with gaps / degraded nodes are `0/0/0`. This final staged result enumerates all 13 commit paths, including the four durable reports; their direct SHA-256 values remain the byte-integrity authority.
- `E3-P3B-COMMIT1`: isolated commit `17254549a13ad81a560c18fbcc6ab8fe3ce5f111` contains the roadmap, all four Child 03 ledgers, exactly two production owners, exactly two test owners, and the four durable P3-B reports. It excludes skill files, P3-A artifacts, target/runtime output, and every later-slice path. The staged manifest was exactly `13/13` with unstaged count `0`. Standard `git diff --cached --check` reported only `new blank line at EOF` for the two immutable Supervisor reports; changing either would have invalidated accepted byte/hash provenance, so both remained exact. `git -c core.whitespace=-blank-at-eof diff --cached --check` exited `0`, proving no other staged whitespace error, and no active non-sample commit hook existed. The current Owner transfer now opens only P3-B1.

### P3-B1 — parameter contexts

| Evidence ID | Required proof | Status |
|-------------|----------------|--------|
| `E3-P3B1-IMPACT1` | exact parameter-owner and dependent-oracle impact | accepted/locked through `E3-P3B1-REVIEW2`; exact two-file repair plus six dependent files only |
| `E3-P3B1-SRC1` | production parameter-context integration | accepted by `E3-P3B1-REVIEW2`; direct formal-parameter ownership closes syntax-role ambiguity |
| `E3-P3B1-BUILD1` | full build | accepted; independent final-byte holder-clean build PASS `108.660s` |
| `E3-P3B1-TEST1` | function/method/constructor/arrow parameter matrix, type-only-label exclusion, and shadowing controls | accepted; former-failure `1/1`, parameter/parity `5/5`, dependent `4/4`, preservation `10/10`, package/product gates PASS |
| `E3-P3B1-BOUNDARY1` | real parameter ScopeIR output with zero type-only false facts | accepted; actual parameter `1/1/1/1`, all 15 type-only labels zero facts/diagnostics |
| `E3-P3B1-REVIEW1` | first independent Supervisor review | `REJECT`; immutable history, sole blocker `P3B1-PARAMETER-CONTEXT-DISAMBIGUATION-1` |
| `E3-P3B1-REVIEW2` | focused independent re-review of the repaired invariant on final bytes | `PASS`; accepts P3-B1 only for orchestration planner/detect/commit closure |
| `E3-P3B1-DETECT1` | pre-commit Anvien detect-changes result | accepted: `309` changed symbols / `17` changed files / `13` affected files; zero affected processes/flows; current health `0/0/0` |
| `E3-P3B1-COMMIT1` | isolated accepted slice commit | accepted, committed, and pushed as `01f160e6e28ad74c1f379ce5ea47e643a5a14652` |

Opening basis (2026-08-14): predecessor gate `E3-P3B-COMMIT1` is satisfied by `17254549a13ad81a560c18fbcc6ab8fe3ce5f111`. Current HEAD `ea1e27710966eb54a4533255dc80a2ac0c2120d8` differs only by the Owner-owned orchestration-skill commit. A fresh graph at that HEAD records `1,643/689/0` scanned/parsed/failed and `98,584/138,305` nodes/relationships; all five plan documents are current, LOW impact, and affect `0` files/flows/tests. This is opening/governance evidence only, not fulfillment of any pending P3-B1 evidence ID.

- `E3-P3B1-IMPACT1` candidate boundary: at current/indexed HEAD `06229bea5735e75c8d8a476c738f627bf93def8d`, fresh graph evidence is non-stale. `definitions.go` file-detail reports `68` symbols, HIGH relationship risk, and unchanged/current graph state; upstream file impact is MEDIUM with `7` impacted symbols across `2` files (`definitions.go` `6`, preserve-only `types.go` `1`) and no process/flow, while exact canonical-UID `collector.emitDefinitionKind` impact is LOW `0/0/0`. `extract_test.go` reports `223` symbols and LOW file impact `0/0/0`; its TSJS parity golden is LOW with zero symbols, relationships, affected files, processes, or flows. After the candidate introduced legal parameter facts, the repo-native product regression failed only four deterministic downstream expected-output assertions. Fresh file-detail reports Vue test owner `88` symbols, graph-parity owner `34`, and resolution test owner `304`, all LOW/current/non-stale; upstream file impact for all three is LOW with zero affected files/processes/flows. Exact-UID impact for `TestResolveVueGraphParityCounts`, `TestResolveTypeScriptGraphBaselineCountsAreReconciled`, `TestResolveTypeScriptGraphSignatureFixture`, and the test-only `graphCountBaseline` contract is LOW with `0` impacted symbols/files/modules/processes. The Vue ScopeIR, resolution baseline-count, and resolution graph-signature goldens each have zero symbols/relationships and LOW file impact `0`. The original conversion evidence independently proves that `resolutionOnly` `24` nodes / `54` relationships / `18` `DEFINES` came from the two recorded TypeScript baseline scripts; those values and their source string are preserve-only. This evidence opens one Vue count assertion, the minimal graph-parity Go-node reconciliation contract/assertion, and three exact artifacts; production Vue/resolution, sibling tests, graph projection, and P3-C remain locked.
- Pre-adjustment validation history: holder-clean full build and focused P3-B1/parity, P3-A/P3-B preservation, and TSJS/ScopeIR tests passed. Repo-native `go test ./cmd/... ./internal/... -count=1` is explicitly non-PASS evidence because Vue parity gained parameter Definitions `repo`, `user`, and `value` plus `DEFINES 10 -> 13`, while the resolution fixture gained six parameter Variable nodes and six `DEFINES` edges. No out-of-boundary file was edited before Orchestration updated the plan. Any build/test result predating the dependent-oracle final bytes is superseded and cannot close `E3-P3B1-BUILD1` or `E3-P3B1-TEST1`.
- `E3-P3B1-REVIEW1`: the first independent review returned `REJECT` in `reports/Supervisor/rp_supervisor_260814_222415_by_gpt-5_child03_p3b1_parameter_contexts.md` (`19,388` bytes, SHA-256 `93A9427D84405ADD763140EEC7241FB1A795E796B41DDCCD4F786523F2E40184`). It independently passed the holder-clean full build (`110.7s`), focused `4/4`, former failures `4/4`, P3-A/P3-B preservation `10/10`, four-package and repo-native product gates, exact JSON deltas, historical baseline, Git boundary, cleanup, and all already opened impact evidence. It rejected only `P3B1-PARAMETER-CONTEXT-DISAMBIGUATION-1`: pinned TypeScript grammar aliases labeled tuple members to `required_parameter`/`optional_parameter`, while the candidate globally dispatches those kinds and accepts a nearest callable ancestor, creating false parameter BindingLeaf/Definition/OwnedDefID/local Binding facts for type-only labels.
- Historical repair authority after `E3-P3B1-REVIEW1`, satisfied by `E3-P3B1-REVIEW2`: edit only `internal/providers/tsjs/definitions.go` and `internal/providers/tsjs/extract_test.go`; prove membership in the owning callable's own formal-parameter surface; preserve TypeScript unparenthesized arrows and explicit `this`; and add a real-`Extract` tuple/function-type former-failure oracle asserting actual parameters emit once while every type-only label emits zero BindingLeaves, Variable Definitions, OwnedDefIDs, local Bindings, and diagnostics. At that checkpoint, the six dependent candidate/oracle files were byte-locked, final-byte build/behavior gates required refresh, and detect/commit plus every later slice remained pending and locked.
- `E3-P3B1-REVIEW2`: focused independent re-review returned `PASS` in `reports/Supervisor/rp_supervisor_260814_233151_by_gpt-5_child03_p3b1_parameter_contexts_review2.md` (`15,253` bytes, SHA-256 `0A0240FB56235EF9BE24F1C02E379DD83C4E7D2B8434F157B1203374B9774299`). Source review proved direct `formal_parameters` ownership, runtime-callable kind, and exact callable `parameters` field node-ID identity exclude labeled tuples and nested function/constructor/type signatures without a name special case. Independent final-byte gates passed: fresh graph `1,646/689/0`, `100,738/140,534`; holder-clean full build `108.660s`; former-failure `1/1`; parameter/parity `5/5`; locked Vue/resolution `4/4`; P3-A/P3-B preservation `10/10`; four packages; and repo-native command/internal regression `151.608s`. All governance, repair-owner, prior-report, and six dependent hashes matched; staged remained `0`; `git diff --check` passed; no repair temp existed. No P3-B2 or later-slice acceptance is implied.
- `E3-P3B1-DETECT1`: orchestration completed full and JSON `anvien detect-changes --repo E:\Anvien --scope all` confirmations on the staged P3-B1 boundary. Both exited `0` on graph basis `1,647` scanned / `689` parsed code / `0` failed, `100,756` nodes / `140,552` relationships, with identical `309` changed symbols, `17` changed files, and `13` affected files. The exact staged manifest is five governance files, eight implementation/test/oracle files, and four durable coder/Supervisor reports; unstaged and untracked counts were `0`. Summary risk is LOW and file-layer risk HIGH. App-layer counts are backend `93`, backend-test `137`, and docs `79`; functional-area counts are providers `220`, resolution `10`, documentation `22`, and reporting `57`. Affected processes and flows are `0/0`. ResolutionGap delta is `65` entities / `65` occurrences (`53` analyzer-gap / `12` non-actionable; `53` in-repo unresolved / `12` builtin), while current gaps / nodes with gaps / degraded nodes are `0/0/0`. Per-file changed-symbol counts are roadmap `2`, actual status `9`, benchmark `2`, evidence `5`, plan `4`, `definitions.go` `93`, `extract_test.go` `125`, Vue test `2`, graph-parity test `10`, the four durable reports `12/13/15/17`, and all four golden artifacts `0`. Direct hash verification reconfirmed both repair owners, all six byte-locked dependents, both coder reports, and both Supervisor reports before the ledger refresh; cached diff-check passed.
- `E3-P3B1-COMMIT1`: main created isolated commit `01f160e6e28ad74c1f379ce5ea47e643a5a14652` with parent `06229bea5735e75c8d8a476c738f627bf93def8d`, exact `17` paths, and worktree clean after commit. Commit subject is `feat(tsjs): integrate parameter binding patterns`; the required orchestration/working-rules/Anvien-upgrade sentence is present in its body. Exactly one `git push origin master` exited `0`, advancing remote `master` from `ea1e2771` to `01f160e6`; post-push local HEAD and `origin/master` are identical. This closes P3-B1 only and opens P3-B2; no later-slice acceptance is implied.

### P3-B2 — catch contexts

| Evidence ID | Required proof | Status |
|-------------|----------------|--------|
| `E3-P3B2-IMPACT1` | exact catch-owner impact | accepted as the implementation-boundary gate only; three files / three existing symbols selected, no code acceptance |
| `E3-P3B2-SRC1` | production catch-context integration | accepted by `E3-P3B2-REVIEW1`; exact two-production/one-test boundary |
| `E3-P3B2-BUILD1` | full build | accepted; independent holder-clean final-byte build PASS |
| `E3-P3B2-TEST1` | catch pattern, scope, shadowing, and duplicate-declaration controls | accepted; focused and preservation matrices PASS |
| `E3-P3B2-BOUNDARY1` | real catch ScopeIR output | accepted; exact catch scope and one-to-one fact conservation PASS |
| `E3-P3B2-REVIEW1` | independent Supervisor PASS | `PASS`; accepts P3-B2 only for orchestration closure |
| `E3-P3B2-DETECT1` | pre-commit Anvien detect-changes result | accepted: `242` changed symbols / `11` changed files / `11` affected files; zero affected processes/flows; current health `0/0/0` |
| `E3-P3B2-COMMIT1` | isolated accepted slice commit | accepted, committed, and pushed as `19247b4eb58a4e01a6256f3d63bbb59839644d64` |

- `E3-P3B2-IMPACT1`: visible coder discovery stopped before code and produced `reports/coder/rp_coder_260815_002037_by_gpt-5_child03_p3b2_catch_contexts.md` (`11,579` bytes; SHA-256 `77E2EC40404015338B75573EEB7D0303BC0EE90279B4AFBF77676E47161022D1`). Fresh graph basis at HEAD/indexed `01f160e6e28ad74c1f379ce5ea47e643a5a14652` is `1,647/689/0`, `100,756/140,552`, stale `false`. Full source proves scope collection/build runs before definition emission, current scopes are module/class/function only, and correct catch-local ownership therefore requires one explicit `catch_clause -> ScopeBlock` candidate rather than an enclosing module/function fallback. Pinned TypeScript `v0.23.2` and JavaScript `v0.25.0` grammars define optional `catch_clause.parameter` as identifier/array/object pattern, so `catch {}` is the required zero-fact/zero-diagnostic control. File-detail/impact selects `definitions.go` (`95` symbols, `22` related excluding self, HIGH relationship risk; MEDIUM `8` direct / `2` files / `0` process/flow), `scopes.go` (`35`, `18`, HIGH; MEDIUM `8` / `2` / `0`), and `extract_test.go` (`345`, `24`, LOW; LOW `0/0/0`). Exact canonical-UID impact for `collector.emitDefinitionKind`, `collector.collectScopeCandidateForKind`, and `TestExtractParameterBindingPatternsPreserveShadowingAndSiblingContexts` is LOW with `0` impacted symbols/modules/processes. Orchestration independently reread `scopes.go`/`extract.go` and reproduced the current file/impact facts before authorizing the plan update. HIGH/MEDIUM are scoped blast-radius warnings, not edit prohibitions. No production/test/governance/temp/stage/commit/push/detect/Supervisor action was performed by that coder checkpoint; `SRC1`, `BUILD1`, `TEST1`, and `BOUNDARY1` were pending at that historical boundary.

- `E3-P3B2-SRC1`: the final implementation is confined to `internal/providers/tsjs/definitions.go`, `internal/providers/tsjs/scopes.go`, and `internal/providers/tsjs/extract_test.go`. `definitions.go` adds one `catch_clause` dispatch plus private catch-only scope-resolution/emission helpers; `scopes.go` adds only `catch_clause -> ScopeBlock` in `collectScopeCandidateForKind`; tests add the focused real-`Extract` matrix and update only the authorized stale `caught` expectation. Final hashes are definitions `0A8CFB00F47116994585AC42072C826284C62175169547C36924E99EC0C4A162`, scopes `845907EAA89F1F228F08560D04BDE1C450A0200A79E4B1041C5BC55190CDB2B4`, and extract test `C7089F90B4DF58D83734F60C4C436D1A5AB2CEAE3091CA850B74A80102E1401E`. The catch adapter uses the accepted walker once, requires the exact catch scope, emits one Variable Definition/OwnedDefID/local Binding per leaf, returns before the walker for optional catch, and adds no type/fallback/exception/assignment/loop/graph behavior.
- `E3-P3B2-BUILD1`: the coder final-byte `npm run full-build` passed in `115.2s`. Independent Supervisor review then terminated six build-artifact MCP holders, proved holder count `0` and exclusive-open `6/6`, and reran the authoritative final-byte full build successfully in approximately `92.8s`. Packaging/runtime, global install/version `1.2.8`, launcher, TypeScript/Vite production build (`2,943` modules), and final analyze all passed; the review graph was `1,649/689/0`, `100,985/140,892`. Timings are validation evidence, not a performance benchmark.
- `E3-P3B2-TEST1`: independent post-build validation passed focused real-catch `4/4`, accepted P3-A/P3-B/P3-B1 preservation `14/14`, TSJS scope/parity `6/6`, ScopeIR tree/normalization, the four-package TSJS/ScopeIR/Vue/resolution boundary, and `go test ./cmd/... ./internal/... -count=1` for every listed repository-native package (approximately `161.9s`). No failure remained.
- `E3-P3B2-BOUNDARY1`: the real parser plus production `tsjs.Extract` boundary proves seven TypeScript pattern leaves and three JavaScript control leaves with exact ranges, identifier selections, typed paths, rest/default modifiers, catch provenance, exact-range `ScopeBlock` parentage, and body-call `InScope`. Every accepted leaf has exactly one Definition, one global/exact-catch OwnedDefID, and one BindingLocal using the same DefID; optional catch has one scope and zero leaves/diagnostics/definitions/owner/binding facts. Outer/catch same-name DefIDs remain distinct and exclusively owned, while assignment `written` emits zero BindingLeaves and zero Variable Definitions. No catch type fact is invented.
- `E3-P3B2-REVIEW1`: independent zero-trust review returned `PASS` in `reports/Supervisor/rp_supervisor_260815_012555_by_gpt-5_child03_p3b2_catch_contexts.md` (`14,774` bytes; SHA-256 `E80DA8AEB480678708F4ED02FDBED455D21206D3F0C79221E296286A605116D4`). It source-cleared all three touched owners, independently verified grammar/traversal/scope contracts, reran the full build and complete focused/preservation/product matrix, and found no residual same-invariant surface. Its scope accepts P3-B2 only for planner refresh, `E3-P3B2-DETECT1`, isolated `E3-P3B2-COMMIT1`, and immediate push; P3-B2A and every later slice remain locked. Orchestration's planner gate then refreshed the graph to `1,650/689/0`, `101,001/140,908`; the roadmap and all four Child 03 ledgers were current/non-stale, LOW impact, and affected `0` files/processes/flows/tests.
- `E3-P3B2-DETECT1`: orchestration staged the exact `11/11` accepted manifest with unstaged/untracked counts `0`, refreshed the graph at HEAD/indexed `01f160e6e28ad74c1f379ce5ea47e643a5a14652` to `1,650` scanned / `689` parsed code / `0` failed and `101,001` nodes / `140,908` relationships, then ran full and JSON `anvien detect-changes --repo E:\Anvien --scope all` confirmations. Both exited `0` with identical `242` changed symbols, `11` changed files, and `11` affected files; summary risk is LOW and file-layer risk HIGH. Per-file changed-symbol counts are roadmap `2`, actual status `9`, benchmark `2`, evidence `5`, plan `4`, definitions `57`, extract test `111`, scopes `3`, impact report `18`, implementation report `16`, and Supervisor report `15`. App-layer counts are backend `60`, backend-test `111`, and docs `71`; functional-area counts are providers `171`, documentation `22`, and reporting `49`. Affected processes/flows are `0/0`. ResolutionGap delta is `46` entities / `46` occurrences (`40` analyzer-gap / `6` non-actionable; `40` in-repo unresolved / `6` builtin), while current gaps / nodes with gaps / degraded nodes are `0/0/0`. Cached diff-check passed, and all accepted source/test/report hashes remained exact before detect.
- `E3-P3B2-COMMIT1`: main created isolated commit `19247b4eb58a4e01a6256f3d63bbb59839644d64` with parent `01f160e6e28ad74c1f379ce5ea47e643a5a14652`. The exact `11` paths are the roadmap, all four Child 03 ledgers, `definitions.go`, `scopes.go`, `extract_test.go`, two coder reports, and the Supervisor PASS report; no later-slice, skill, target, temp, or unrelated path is present. Commit subject is `feat(tsjs): integrate catch binding patterns`, and the required orchestration/working-rules/Anvien-upgrade sentence is present in its body. Exactly one `git push origin master` exited `0`, advancing remote `master` from `01f160e6` to `19247b4e`; post-push local HEAD and `origin/master` are identical and the worktree is clean. This closes P3-B2 only and opens P3-B2A for fresh owner impact; no later-slice acceptance is implied.

### P3-B2A — loop declaration contexts

| Evidence ID | Required proof | Status |
|-------------|----------------|--------|
| `E3-P3B2A-IMPACT1` | exact loop-owner impact | accepted as the implementation-boundary gate; exact four files/four existing symbols selected, no code acceptance |
| `E3-P3B2A-SRC1` | production `for-of`/`for-in` declaration integration | accepted by `E3-P3B2A-REVIEW2`; exact four-file candidate with two-file focused repair |
| `E3-P3B2A-BUILD1` | full build | accepted; independent holder-clean final-byte full build PASS |
| `E3-P3B2A-TEST1` | declaration/assignment loop matrix, scope, path, and sibling controls | accepted; loop `5/5`, wrapper controls `9/9`, preservation matrices and four-package boundary PASS |
| `E3-P3B2A-BOUNDARY1` | real loop ScopeIR output and zero false declarations for assignment forms | accepted; representable targets emit one truthful write, exact member false read is absent, bracket controls remain zero-fact |
| `E3-P3B2A-REVIEW1` | first independent Supervisor review | `REJECT`; sole blocker `P3B2A-ASSIGNMENT-TARGET-WRAPPER-CLOSURE-1` |
| `E3-P3B2A-REVIEW2` | focused independent re-review of the repaired wrapper invariant on final bytes | `PASS`; accepts P3-B2A only for orchestration planner/detect/commit/push closure |
| `E3-P3B2A-DETECT1` | pre-commit Anvien detect-changes result | accepted: `348` changed symbols / `14` changed files / `14` affected files; zero affected processes/flows; current health `0/0/0` |
| `E3-P3B2A-COMMIT1` | isolated accepted slice commit | accepted; commit `55aa5344b5c53561055cb756bfd9a3d61a199433` with exact `14`-path boundary, pushed once; post-push local/remote matched before later Owner-only skill commits |

Opening basis (2026-08-15): predecessor gate `E3-P3B2-COMMIT1` is satisfied by pushed commit `19247b4eb58a4e01a6256f3d63bbb59839644d64`. A fresh post-push graph at that HEAD is `1,650/689/0`, `101,001/140,908`; roadmap/plan/evidence/actual-status transition owners are current, LOW impact, and affect `0` files/processes/flows. This opened only read-only P3-B2A loop-owner discovery; implementation remained locked until the checkpoint below.

- `E3-P3B2A-IMPACT1`: the visible coder task stopped before code and produced `reports/coder/rp_coder_260815_021253_by_gpt-5_child03_p3b2a_loop_contexts.md` (`18,519` bytes; SHA-256 `5D29AFBABB96A0549705A445D1E2262154F37A442947B01F102A5E75DC673612`). Fresh graph evidence at current/indexed HEAD `19247b4eb58a4e01a6256f3d63bbb59839644d64` is `1,650/689/0`, `101,001/140,908`, stale `false`. Pinned TypeScript `v0.23.2` and JavaScript `v0.25.0` grammar plus ten real-parser controls prove `for-in` and `for-of` both use `for_in_statement`; non-empty `kind` distinguishes declaration forms, assignment forms have no `kind`, and `left` is direct identifier/pattern/member syntax rather than a `variable_declarator`. Current source proves declarations need `emitDefinitionKind`, lexical `let|const` ownership needs one exact `collectScopeCandidateForKind` loop case, and no-`kind` assignment targets need `emitReferenceKind` write handling because identifier/pattern writes are absent and a member target is currently misclassified as read. File-detail/impact selects `definitions.go` (`106` symbols / `22` related / HIGH; MEDIUM `9` direct / `2` files / `0` process-flow), `scopes.go` (`35/18` / HIGH; MEDIUM `9/2/0`), `references.go` (`33/15` / HIGH; LOW `1/1/0`), and `extract_test.go` (`455/24` / LOW; LOW `0/0/0`). Exact canonical-UID impact for `collector.emitDefinitionKind`, `collector.collectScopeCandidateForKind`, `collector.emitReferenceKind`, and `TestExtractParameterBindingPatternsPreserveShadowingAndSiblingContexts` is LOW `0/0/0`. HIGH relationship risk is a narrow-scope warning, not an edit prohibition. Orchestration accepts this evidence only to authorize the exact four-file implementation boundary; no source/build/test/boundary/review/detect/commit/push or P3-C acceptance is implied.

- `E3-P3B2A-REVIEW1`: independent zero-trust review returned `REJECT` in `reports/Supervisor/rp_supervisor_260815_082939_by_gpt-5_child03_p3b2a_loop_contexts.md` (`15,125` bytes; SHA-256 `6D9CD9B381D851762A65A42B0DE2170DB5F242A8F1555F54E91C967DF494A628`). Fresh graph evidence was `1,652/689/0`, `101,421/141,443`; exact file/symbol impact and the complete production diff were checked before test reliance. Source and production-parser probes cleared declaration discrimination, one-to-one leaf ownership, `var` versus lexical scope, direct target-only recursion, direct member suppression, and preservation of nested-receiver/computed-key/default-initializer reads. The sole blocker `P3B2A-ASSIGNMENT-TARGET-WRAPPER-CLOSURE-1` is that TS/JS `parenthesized_expression` and TypeScript `non_null_expression` wrap already-representable identifier/member assignment leaves but are absent from both write recursion and exact-member containment, producing zero write or a retained false read. `subscript_expression` is not a blocker because truthful computed access is not representable by the current dot-member contract and contract expansion is out of scope. Repair is restricted to `references.go` and `extract_test.go`; `definitions.go`, `scopes.go`, all cleared semantics, governance, and both existing coder reports are locked. The independent full build/regression matrix was intentionally not run on rejected bytes and must run once on final repair bytes before `E3-P3B2A-REVIEW2`.

- `E3-P3B2A-SRC1`: final production/test scope is exactly `internal/providers/tsjs/definitions.go`, `scopes.go`, `references.go`, and `extract_test.go`. Declaration form dispatches only supported non-empty `var|let|const` kinds, consumes the accepted walker once, emits one Variable Definition/OwnedDefID/local Binding per legal leaf, and owns `var` in the nearest function/module versus `let|const` in the exact loop `ScopeBlock`. Assignment form emits zero declarations/leaves/scopes/diagnostics and routes only representable target leaves to `AccessWrite`. The focused repair changes only `references.go` and the focused real-parser oracle: `unwrapLoopAssignmentTarget` unwraps `parenthesized_expression` and `non_null_expression` before both write dispatch and exact-member containment. It adds no bracket/computed-access contract and changes no general reference path. Final hashes are references `9F7C61DA0CD8B2F9EDFE0D1300740E6EB45149BC0D134132520E1CBD6961B019`, extract test `9BB57405BC90A830B354D624A93F7B7B24D57071EEFFC864A803649A1E193293`, locked definitions `4936F9DD0012F787E0BF007DA8070527C945E28D0DC0B1A0BA083A49D45350D7`, and locked scopes `54132D72927F721E32336C075A553DCF8947CC05180240CF0B2AF6F8A7345509`.
- `E3-P3B2A-BUILD1`: coder final-byte `npm run full-build` passed; independent Supervisor then cleared build holders/locks and reran the authoritative final-byte full build with exit `0`. Packaging, npm install, launcher/web build, CLI version, and final analyze completed. Supervisor build graph context was `1,989` scanned / `799` parsed code / `0` failed, `121,010` nodes / `163,054` relationships. The higher inventory includes concurrent Owner-owned skill content and is validation context only, not a P3-B2A benchmark.
- `E3-P3B2A-TEST1`: independent post-build validation passed focused loop tests `5/5` with wrapper/bracket subcases `9/9`, P3-A/P3-B/P3-B1 preservation `14/14`, P3-B2 catch preservation `4/4`, TSJS scope/parity `6/6`, selected ScopeIR tree/normalization `3/3`, and the four-package TSJS/ScopeIR/Vue/resolution boundary. `go test ./cmd/... ./internal/... -count=1` exited `1` only in `TestSkillHelpPrintsGeneratedSkillSelectionGuide` and `TestAnalyzeCommandGeneratesAIContextByDefault`; both failures are caused by the explicitly excluded Owner-owned `internal/aicontext/skills/ui-ux-pro-max-skill/` tree and do not touch or contradict a P3-B2A path or invariant. The command is recorded as non-PASS and is not used as acceptance evidence.
- `E3-P3B2A-BOUNDARY1`: real parser plus production `tsjs.Extract` proves TypeScript/JavaScript parenthesized identifiers and members plus TypeScript non-null identifiers/members emit exactly one underlying write, with zero false exact-member read. Nested `target.nested` remains a legitimate receiver read while only outer `value` is written. All assignment controls emit zero BindingLeaves, Definitions, loop `ScopeBlock`s, and diagnostics. Direct object/array/pair/rest/default/member recursion remains exact; property keys and default initializers are not promoted to writes. TypeScript/JavaScript bracket targets emit zero invented access fact because the current dot-member `AccessFact` contract cannot represent computed access truthfully.
- `E3-P3B2A-REVIEW2`: focused independent review returned `PASS` in `reports/Supervisor/rp_supervisor_260815_091529_by_gpt-5_child03_p3b2a_loop_contexts_review2.md` (`13,197` bytes; SHA-256 `80BC32BD13A34E23CA1F518FC20B1C5E04CB1882516D82763728E1DF2E552C31`). It closes only `P3B2A-ASSIGNMENT-TARGET-WRAPPER-CLOSURE-1`, locks every declaration/scope/direct-target/prior-context invariant, and authorizes planner refresh, final detect, isolated commit, and immediate push. Before planner edits, `HEAD == origin/master == 19247b4eb58a4e01a6256f3d63bbb59839644d64`, the exact authorized manifest was `13` paths including REVIEW2, staged count was `0`, diff-check passed, and the excluded Owner tree remained separate. Orchestration's post-PASS planner gate refreshed the graph to `1,990/799/0`, `121,029/163,073`; roadmap plus all four Child 03 ledgers were current/non-stale, LOW impact, and affected `0` files/processes/flows/tests. Roadmap retains `28` outbound plan links and each ledger one inbound roadmap link.
- `E3-P3B2A-DETECT1`: after planner refresh and staging the exact `14`-path candidate, the latest successful graph refresh explicitly excluded the concurrent Owner skill package and exited `0` at `1,800` scanned / `727` parsed code / `0` failed with graph `110,865` nodes / `151,831` relationships. The inventory differs from the Supervisor's full-worktree review context because the Owner package is external state and is not a P3-B2A benchmark or source delta. `anvien detect-changes --repo E:\Anvien --scope all --json` was also run and truthfully enumerated five additional unstaged Owner-skill paths; it is not the isolated commit result. The authoritative final boundary is full/JSON `anvien detect-changes --repo E:\Anvien --scope staged` with exit `0`: `348` changed symbols, `14` changed files, and `14` affected files; summary risk LOW and file-layer risk HIGH. Changed app layers are backend `131`, backend-test `108`, docs `109`; functional areas are providers `239`, documentation `23`, reporting `86`; affected processes/flows are `0/0`. ResolutionGap delta is `92` entities / `92` occurrences (`83` analyzer-gap / `9` non-actionable), while current gaps / nodes with gaps / degraded nodes are `0/0/0`. Staged count was `14`, and the exact changed-path list equals the candidate manifest; the excluded Owner skill tree is absent. This is the final pre-commit detect boundary; `E3-P3B2A-COMMIT1` remains pending until the isolated commit and immediate push.
- `E3-P3B2A-COMMIT1`: main created `55aa5344b5c53561055cb756bfd9a3d61a199433` with parent `19247b4eb58a4e01a6256f3d63bbb59839644d64`, exact `14` paths, and the required commit-body sentence `Updated the multi-agent orchestration skill and working-rules skill; the Anvien upgrade is in progress.` Exactly one `git push origin master` exited `0`; post-push local/remote matched at the closure boundary. Later local commits are Owner-owned changes under the external `internal/aicontext/skills/**` tree and are excluded from Child 03 evidence. This closes P3-B2A and opens only read-only P3-C impact discovery; no P3-C implementation or later slice is accepted here.

### P3-C — graph projection and lexical resolution

| Evidence ID | Required proof | Status |
|-------------|----------------|--------|
| `E3-P3C-IMPACT1` | exact extraction-to-graph/reference blast radius and affected-persistence inventory | accepted by `E3-P3C-REVIEW2`; fresh excluded graph, current file-detail, and canonical-UID impact `22/22` |
| `E3-P3C-SRC1` | smallest production projection/resolution change; no unproved adapter expansion | accepted by `E3-P3C-REVIEW2`; exact owner-file/range/local-Binding validation completes before mutation |
| `E3-P3C-BUILD1` | full build | accepted; unchanged canonical final-byte full build PASS, exit `0` |
| `E3-P3C-TEST1` | occurrence conservation, endpoints, lexical shadowing, assignment/import controls, and gap regression | accepted; focused P3-C, uncached six-package regression, `go vet`, and fresh hostile sentinel proof PASS |
| `E3-P3C-BOUNDARY1` | real graph or persisted-graph boundary showing corrected binding facts | accepted; `7/7` occurrences/`DEFINES`, five reads plus one write, zero normal gaps, parity and fail-before-mutation PASS |
| `E3-P3C-REVIEW1` | independent Supervisor verdict | `REJECT`; exact owner-scope blocker retained in immutable report |
| `E3-P3C-REVIEW2` | focused independent review of exact owner-scope repair while preserving cleared invariants | `PASS`; no residual same-invariant surface, no P3-C2 acceptance |
| `E3-P3C-PLAN1` | post-PASS planner refresh plus current document file-detail/impact | accepted; excluded graph current, five documents LOW with zero affected files/processes/flows/tests |
| `E3-P3C-DETECT1` | pre-commit Anvien detect-changes result | accepted staged boundary: `378` changed symbols / `14` changed files / `14` affected files; `15` process/flow records; current health `0/0/0` |
| `E3-P3C-COMMIT1` | isolated accepted slice commit | this exact commit boundary; hash reported by Git/handoff |

- `E3-P3C-REVIEW1`: independent review returned `REJECT` in `reports/Supervisor/rp_supervisor_260815_204754_by_gpt-5_e3_p3c_review1.md` (`19,444` bytes; SHA-256 `A924F5FE1BFC93E9CB7C2712E240840BBCE748DE8B7620654E203B9C6ED1DFCB`). The well-formed candidate passed `7/7` occurrence/`DEFINES` projection, lexical reads/write, shadowing, import conservation, Graph JSON/Ladybug parity, and zero normal gaps. The sole root blocker is coordinated owner drift: moving both `OwnedDefID` and its matching local Binding to a wrong-file/non-owning scope is accepted before mutation and then yields unresolved output; gap integrity is a consequence of the same failure. Repair is restricted to `internal/resolution/resolve.go` plus `internal/resolution/p3c_binding_occurrence_test.go`, with the locked persistence test and every cleared invariant preserved. No detect, commit, P3-C2, or later-slice acceptance is implied.

- `E3-P3C-IMPACT1`: focused REVIEW2 refreshed the graph with both forbidden-tree exclusions and passed at `1,106` scanned / `624` parsed code / `0` failed, graph `80,147` nodes / `119,090` relationships. Current `resolve.go` file-detail is `113` symbols / `88` inbound / `168` outbound / `21` linked flows / `31` linked tests, `risk=high`, `stale=false`. Canonical-UID upstream impact completed for all `22/22` changed/new top-level production symbols and new struct fields: `21` are CRITICAL and one is LOW. `ResolveBoundInto` and `buildBindingOccurrenceIndex` reach the known `33`-process boundary; the focused test owner remains LOW. HIGH/CRITICAL are scoped blast-radius warnings, not edit prohibitions.
- `E3-P3C-SRC1`: final production remains confined to `internal/resolution/resolve.go`. `buildBindingOccurrenceIndex` executes before caller graph selection, `graph.New`, emitter creation, or emission. It requires exact leaf-to-Variable-definition equality by normalized file/name/full range/selection, one lexical owner, normalized leaf/definition/owner-IR/owner-scope file agreement, owner containment of both full ranges and every present selection range, exactly one matching `(BindingLocal, name, defID)` inside that validated owner, and no duplicate accepted defID. Binding resolution still uses `resolveScopedName` plus validated-set membership only. Final production hash is `C1FF5C515D401ECAD4FBF93C271DF4AC19101B2F5410D4174F7F598502BBC96A`; focused test hash is `9BAE2F63575C313B5F5F8EF4C265360BCB38F8D2F616CDCDC8942C57A080CE7F`; locked persistence hash remains `C704B45FD350F2A1B064D79E78B4DC99F6378D44358B4E86149A09FA38D4A850`.
- `E3-P3C-BUILD1`: the unchanged canonical `scripts/full-build.ps1` passed on final bytes with exit `0` after one holder/lock cleanup. Packaged runtime, npm audit (`0` vulnerabilities), global install, CLI version `1.2.8`, launcher/native build, and Web production build (`2,943` modules) passed. Its built-in unexcluded analyze passed at `1,807/729/0` but is explicitly discarded as P3-C graph evidence; no graph command followed it.
- `E3-P3C-TEST1`: independent final-byte focused `^TestP3C` validation passed the well-formed projection test, all four fail-closed subtests, and the locked persistence parity test. The uncached six-package resolution/Ladybug/graph/schema/TSJS/ScopeIR regression passed `6/6`; `go vet` emitted no diagnostics; the coordinated wrong-file owner/local-Binding hostile test passed again after vet.
- `E3-P3C-BOUNDARY1`: every accepted leaf conserves one distinct Variable node and one `DEFINES` endpoint (`7/7`); five reads plus one assignment write resolve to exact lexical endpoints; inner/outer shadowing stays distinct; assignment creates no eighth definition; import metrics remain `1/1/1`; well-formed gaps remain zero; Graph JSON/Ladybug CSV/load parity remains exact with zero skipped/fallback/warning outcomes. The hostile control removes the real owner facts, inserts both owner and matching local Binding into a different-file module scope, and proves error, nil result graph, unchanged node/relationship counts, and byte-identical sentinel JSON.
- `E3-P3C-REVIEW2`: focused independent review returned `PASS` in `reports/Supervisor/rp_supervisor_260820_131548_by_gpt-5_e3_p3c_review2.md` (`16,180` bytes; SHA-256 `9F00227F5DF888559106FA1B0E98332FEC876A939637839F823A3042DA673798`). It reviewed live `master` at `a569b8674fefdaa757cf7fdf63f454caf7925215`; delegated anchor `a36b42c8c71d2fa7de551b5bd00a8c026123f8c6` is an ancestor, and the sole intervening commit is excluded-tree-only with no reviewed-byte change. The current Coder handoff remains exact at `11,209` bytes / SHA-256 `9E50FA115150BB3F6CF0A7E3B12B185F26BDE5BE72B8302892F688B6227FF535`. All ten mandatory invariants pass and residual unverified same-invariant surfaces are none. Acceptance is P3-C only for planner, detect, and isolated commit closure; target/P3-C2 remains locked.
- `E3-P3C-PLAN1`: after the one post-PASS planner refresh, `anvien analyze . --force --exclude "internal/aicontext/skills/**" --exclude ".claude/skills/**"` exited `0` at current/indexed HEAD `a569b8674fefdaa757cf7fdf63f454caf7925215`: `1,108` scanned / `624` parsed code / `0` failed, graph `80,168` nodes / `119,111` relationships. Current file-detail reports the roadmap LOW/non-stale/unchanged with `28` outbound plan links; each Child 03 plan/evidence/benchmark/actual-status ledger is LOW/non-stale/unchanged with exactly one inbound roadmap link. Upstream file impact for all five documents is LOW with `0` impacted items, affected files, processes, flows, or tests. This proves only the living-ledger governance boundary and does not reopen accepted code/test gates.
- `E3-P3C-DETECT1`: after exact `14/14` staging with no unstaged path outside the two forbidden trees, the final graph refresh again exited `0` at `1,108/624/0`, `80,168/119,111`. Full staged detect plus JSON and flow confirmations exited `0` with `378` unique changed symbols, `14` changed files, and `14` affected files; the changed and affected path sets both equal the exact staged manifest. Summary and file-layer risk are HIGH, reflecting the already reviewed resolution blast radius rather than a prohibition. Changed app layers are backend `133`, backend-test `160`, docs `85`; changed functional areas are documentation `25`, reporting `60`, resolution `230`, storage `63`. The flow surface contains `15` affected process/flow records, all with changed steps in `ResolveBoundInto` or `resolveAccess`; affected app-layer count is backend `15`, split across resolution `11` and mixed `4`. ResolutionGap delta is `76` entities / `76` occurrences (`56` analyzer-gap / `20` non-actionable; `56` in-repo unresolved / `11` builtin / `9` standard-library), while current gaps / nodes with gaps / degraded nodes are `0/0/0`. `--scope staged` is authoritative; forbidden Owner trees are not part of this evidence.
- `E3-P3C-COMMIT1`: this ledger is part of the exact isolated `14`-path P3-C commit boundary together with the other four living documents, one production owner, two focused/persistence tests, two Investigation handoffs, two Coder reports, and two Supervisor reports. It excludes both Owner skill trees, target/P3-C2 paths, runtime output, and unrelated work. The exact commit hash is reported by Git/handoff after commit success; P3-C2 opens automatically only then.

### P3-C2 — real-target acceptance

| Evidence ID | Required proof | Status |
|-------------|----------------|--------|
| `E3-P3C2-ORACLE1` | independent six-site oracle with names, source locations, paths, ranges, scopes, and expected targets | accepted `PASS`; exact metadata-only six-site oracle |
| `E3-P3C2-TARGET1` | real target analyze result: bindings `6/6`, downstream sites `6/6`, binding-caused gaps `0` | accepted `PASS`; direct/persisted chain `6/6`, endpoints `6/6`, gaps `0` |
| `E3-P3C2-BOUNDARY1` | target pre/post source boundary, operational-output record, and contamination check | accepted `PASS`; exact recovered status provenance and pre/post equality |
| `E3-P3C2-REVIEW1` | independent Supervisor PASS | `PASS`; accepts P3-C2 only for post-PASS planner/detect/isolated-commit closure |
| `E3-P3C2-DETECT1` | Anvien-side change/boundary check before evidence commit | accepted staged boundary: `648` changed symbols / `17` changed files / `17` affected files; `16` affected processes; current health `0/0/0` |
| `E3-P3C2-COMMIT1` | isolated Anvien-side evidence/ledger commit; no target artifact committed | this exact `17`-path commit boundary; hash reported by Git/handoff after success |

- `E3-P3C2-ORACLE1`: the final QA artifact is `reports/QA/rp_qa_260820_143739_by_gpt-5_p3c2_target_binding_validation.md` (`35,045` bytes / `425` lines / SHA-256 `CCC8CD8659CD616105609CA0CF8BD633BE7B68B7C739F0339F257A954F22CBD5`). It records the six exact locals `messageRows`, `attemptRows`, `eventRows`, `providerEventRows`, `readinessRows`, and `suppressionRows`; typed `array-index(0..5)` paths; exact declaration/selection ranges; Function owner `readEmailOperationsReport` at `[497:7,634:1)`; and six exact downstream spans. The oracle contains metadata only and copies no target source body.
- `E3-P3C2-TARGET1`: the authorized probe files remain exact at SHA-256 `C68E520AF4F220AD2E9E4A9B71941EB9B816C3EAFC15D6F7B44212305BB7BBA7` and `DA3B500FB161411A4D134DDF11935A83ECB1955F08C3087B5C78D4B4073C4F9A`; the Coder report is `FC25935961463336A554503F9D5045B3F117BE5DC7ED7E61FEBF88F062927A5F`. Direct probe stdout is `26,555` bytes / SHA-256 `8E382A3ADDEE3542505A0B98A8A9A57EC41606133D72E86455A1CF090F0D8F21`: six ordered variable-context BindingLeaves, typed paths `0..5`, six exact matching Variable Definitions/nonempty DefIDs, one exact lexical owner with each DefID once in `OwnedDefIDs`, and one exact owner-local `BindingLocal` each. Normal target analyze passed `1359/887/0`, graph `90,899/121,868`; persisted Variables/`DEFINES`/exact lexical `ACCESSES` are `6/6/6`, both bounded gap queries return `0`, and accepted assignment/import/shadowing plus Graph JSON/Ladybug parity remain frozen at the accepted P3-C hashes.
- `E3-P3C2-BOUNDARY1`: recovery report `reports/Investigation/rp_main_260820_161744_p3c2_status_boundary_recovery.md` is `16,512` bytes / SHA-256 `F44293C45E142742D63D71A400D975D7E48101D7C1A91F69D18739FDE1A27700`; Main verification is `6,637` bytes / SHA-256 `24EA2A47420DC1C65D9DA3B67339AFE622B83A5F2EDCB9C09F36F3B37F8A9B5B`. The exact porcelain-v2 recipe preserves Git order and joins two branch headers plus `13` entries as BOM-less UTF-8 with LF separators and no trailing LF/NUL: `1,833` bytes / SHA-256 `FE3573ADB361095CF36BAD90D56912FB2EC1F92FFF0628BE19DB12E4F27E4E2E`, exactly `7 tracked + 6 untracked`. Target HEAD `a869876ab6262dacde6cd5d432d099a91852a646`, tracked-diff Git object `941c3e00be7357de8393d959b91ca93be72e64fb`, and untracked manifest `6` files / `573,963` bytes / SHA-256 `886DE642875AA7D58F5F8357054927AADA49CAE65604120AB28AA788F0E81B97` are exact pre/post; all ten match/equality fields are `true`. The older `6 tracked` phrase is retained as a non-byte-backed handoff miscount, and no target report/fixture/probe/debug artifact exists.
- `E3-P3C2-REVIEW1`: independent Supervisor returned `PASS` in `reports/Supervisor/rp_supervisor_260820_170542_by_gpt-5_e3_p3c2_review1.md` (`25,161` bytes / `221` lines / SHA-256 `A787DE62678ABCAF999100903CF58B3B8D62A6D21DE9D9F99F331BEE2C46FF02`). The review directly parsed original/recovery rollouts, rechecked every mandatory artifact/source/probe hash, closed all ten invariants, and found no residual same-invariant surface or target contamination. At verdict, Anvien HEAD/parent were `656a0445ff3e25b6225b994cdaf7cf1b35eb665c` / `a569b8674fefdaa757cf7fdf63f454caf7925215`, the P3-C commit manifest was exactly `14` paths with no forbidden-tree path, and tracked/staged/unstaged rows were `0/0/0`. Authorization is limited to one post-PASS planner refresh, final `E3-P3C2-DETECT1`, and isolated `E3-P3C2-COMMIT1`; `Pn-A` remains locked until commit success.
- Post-PASS planner/impact refresh: after the five living documents were aligned to `ORACLE1/TARGET1/BOUNDARY1/REVIEW1`, `anvien analyze . --force --exclude "internal/aicontext/skills/**" --exclude ".claude/skills/**"` exited `0` at current/indexed HEAD `656a0445ff3e25b6225b994cdaf7cf1b35eb665c`: `1,120` scanned / `626` parsed code / `0` failed, graph `80,799` nodes / `120,058` relationships. Current `file-detail` reports the roadmap LOW/non-stale/unchanged with `28` outbound plan links; each Child 03 plan/evidence/benchmark/actual-status ledger is LOW/non-stale/unchanged with exactly one inbound roadmap link. Upstream file impact for all five documents is LOW with `0` impacted items, affected files, processes, flows, or tests. This proves only the accepted living-ledger governance boundary; it does not rerun or broaden target behavior.
- `E3-P3C2-DETECT1`: after staging the exact `17`-path P3-C2 manifest with unstaged/untracked counts `0/0`, the excluded self-graph passed at `1,120/626/0`, `80,799/120,058`. Full and JSON `anvien detect-changes --repo E:\Anvien --scope staged` confirmations exited `0` with `648` changed symbols, `17` changed files, and `17` affected files. Summary risk is `CRITICAL` and file-layer risk is `HIGH`; these are blast-radius warnings concentrated in the accepted new metadata-probe command, not edit prohibitions. App-layer counts are CLI launcher `405`, backend test `78`, and docs `165`; functional areas are unknown `483`, reporting `143`, and documentation `22`. The `16` affected process records all contain changed steps in `cmd/binding-contract-probe/main.go`; no plan/report/test path contributes a process change. ResolutionGap delta is `244` entities / `249` occurrences (`118` analyzer-gap / `126` non-actionable; `118` in-repo unresolved / `77` standard-library / `49` builtin), while current gaps / nodes with gaps / degraded nodes are `0/0/0`. The changed and affected path sets both equal the exact staged manifest; both forbidden trees and every target path are absent.
- `E3-P3C2-COMMIT1`: this ledger is part of the exact isolated `17`-path P3-C2 boundary together with the roadmap and other three Child 03 ledgers, two probe source/test files, seven Investigation provenance/handoff reports, one QA report, one Coder report, and one Supervisor report. It excludes both forbidden skill trees, target files/output, P3-C production/tests already committed at `656a0445`, runtime output, and unrelated work. Git and the post-commit handoff report the exact commit hash; no push is authorized. `Pn-A` opens automatically only after commit success.

### Pn-B — dead-work cleanup

| Evidence ID | Required proof | Status |
|-------------|----------------|--------|
| `E3-PNB-CLEAN1` | cleanup candidate inventory and exact dead-work deletion | accepted; `107` entries reconciled, `11/11` dead paths absent, retained set `72/72` |
| `E3-PNB-REVIEW1` | independent cleanup Supervisor review | `PASS`; `34,757` bytes / `455` LF lines / SHA-256 `533A957569BB929FFFD8C269BACAB781C14CB0568B4F465DE67E5D8C81A6943D` |
| `E3-PNB-PLAN1` | excluded graph refresh plus five-document file-detail/upstream-impact gate | accepted governance basis: `1,124/626/0`, `80,879/120,138`; all five documents LOW and `0` affected files/processes/flows/tests |
| `E3-PNB-DETECT1` | exact staged Pn-B detect-changes result | `PASS`: `75` changed documentation/reporting sections / `8` changed files / `8` affected files; LOW risk; zero process/flow/gap/health impact |
| `E3-PNB-COMMIT1` | isolated Pn-B commit and post-commit boundary | reserved Main-owned gate; pending after detect |

- `E3-PNB-CLEAN1`: the cleanup Coder candidate is `reports/coder/rp_coder_260820_182937_by_gpt-5_e3_pnb_cleanup.md` (`28,632` bytes / `412` LF lines / SHA-256 `D63362A7B382F8382875E71718DDC580B34A21DEBCA71BC327509E56DAC1E8D4`). It records `40=10+30` lifecycle commits, tracked union `71=49+22`, denominator `107=50+22+11+24`, exact dead absence `11/11` totaling `37,756` pre-delete bytes, `.tmp=738`, Child 03 name-match `0`, two shared hashes, protected `34/34`, and no build/test/target/graph rerun under cleanup-only authority.
- `E3-PNB-REVIEW1`: the independent report `reports/Supervisor/rp_supervisor_260820_185106_by_gpt-5_e3_pnb_review1.md` is `PASS` at `34,757/455/533A957569BB929FFFD8C269BACAB781C14CB0568B4F465DE67E5D8C81A6943D`; it independently verified denominator arithmetic, absence/containment/ignore state, shared/protected hashes, Git isolation, and no current dead artifact missed. Historical `24` absent rows are informational only and were not reconstructed.
- The isolated Pn-B boundary contains the five living documents, the cleanup Coder report, the Supervisor report, and the explicitly authorized concurrent Main handoff provenance. The handoff is excluded from the cleanup denominator and candidate evidence but is retained as Main-owned rotation provenance. No production/test/probe/target/forbidden-tree path is included; no push is authorized.
- `E3-PNB-DETECT1`: after exact eight-path staging, full and JSON `anvien detect-changes --repo E:\Anvien --scope staged` confirmations exited `0` with `75` changed sections, `8` changed files, and `8` affected files. Summary/file risk is LOW; app-layer count is docs `75`; functional areas are documentation `23` and reporting `52`; affected processes/flows are `0/0`; ResolutionGap delta is `0/0`; current gaps / nodes with gaps / degraded nodes are `0/0/0`; semantic fields are complete. The changed and affected path sets both equal the exact isolated manifest, with no production/test/probe/target/forbidden-tree path.

## Closure Evidence

| Evidence ID | Required proof | Status |
|-------------|----------------|--------|
| `E3-PNA-REVIEW1` | final Supervisor review across all seven slices and current source/evidence reality | `PASS`; aggregate invariant closed with no residual same-invariant surface |
| `E3-PNB-CLEAN1` | dead child-created artifacts removed; valid evidence retained | `PASS`; exact `107` denominator and `11/11` absence |
| `E3-PNB-REVIEW1` | Supervisor PASS for cleanup | `PASS`; no residual same-invariant surface |
| `E3-PNB-PLAN1` | planner refresh and document file-detail/impact gate | accepted; excluded graph and all five document impacts are LOW/zero |
| `E3-PNB-DETECT1` | final staged detect-changes result for Pn-B | `PASS`: `75/8/8`, LOW, zero processes/flows/gap/health impact |
| `E3-PNB-COMMIT1` | isolated Pn-B commit | pending Main-owned closure |
| `E3-PNC-DETECT1` | final detect-changes result for implementation scope | pending |
| `E3-PNC-COMMIT1` | final known commit/worktree state | pending |
| `E3-PNC-HANDOFF1` | Child 04 actual-status refreshed from accepted Child 03 results and exact opening condition recorded | pending |

`E3-PNA-REVIEW1`: independent Supervisor returned `PASS` in `reports/Supervisor/rp_supervisor_260820_175806_by_gpt-5_e3_pna_review1.md` (`31,921` bytes / `366` LF lines / SHA-256 `7B8F0D1292C0CA754862AC59C229C6E224C8CD88CBDDAA9BDEE961D193C2847D`). Main independently re-read the complete report, recomputed its identity, verified all seven commits are ancestors with exact manifests `14/13/17/11/14/14/17`, confirmed current `HEAD/parent` `8784c6c21da842b188f136b95ec97ab8df9f20e8` / `656a0445ff3e25b6225b994cdaf7cf1b35eb665c`, and rechecked the frozen QA/recovery/Main-verification/probe hashes. The review source-clears the full recursive leaf/context/projection/persistence/reference invariant, preserves every substantive REJECT-to-PASS closure, and finds no residual same-invariant surface. Historical artifacts removed by Owner cleanup are recorded only as not present/not relied upon; they are not a blocker or a reconstruction requirement. This closes Pn-A and opens only Pn-B; Pn-C and Child 04 remain locked.

`E3-PNB-REVIEW1`: the cleanup invariant is independently `PASS` at `reports/Supervisor/rp_supervisor_260820_185106_by_gpt-5_e3_pnb_review1.md` (`34,757` bytes / `455` LF lines / SHA-256 `533A957569BB929FFFD8C269BACAB781C14CB0568B4F465DE67E5D8C81A6943D`). Pn-B is accepted pending the isolated eight-path Main commit; Pn-C remains locked until Git confirms that commit.
