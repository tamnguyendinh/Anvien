services:
  avmatrix-server:
    image: ${SERVER_IMAGE:?Set SERVER_IMAGE in .env or your shell environment}
    container_name: ${SERVER_CONTAINER_NAME:-avmatrix-server}
    # Map the server to the same host port the web UI expects by default
    # (http://127.0.0.1:4848). The browser runs on the host, so the UI's
    # built-in default works without any reconfiguration.
    ports:
      - '${SERVER_HOST_PORT:-4848}:4848'
    volumes:
      # Persist the global registry, indexes, and cloned repos across runs.
      - avmatrix-data:/data/avmatrix
      # Optional: mount a host workspace so `avmatrix index <path>` can see
      # repos you already have on disk. The default points at an empty
      # `./workspace/` sibling that compose will create on first start —
      # it intentionally does NOT bind-mount the repo root, which would
      # expose `.git`, `.env`, and CI secrets to the container.
      # Override with `WORKSPACE_DIR=/abs/path/to/your/repos`.
      - ${WORKSPACE_DIR:-./workspace}:/workspace:ro
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-fsS', 'http://127.0.0.1:4848/api/info']
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

  avmatrix-web:
    image: ${WEB_IMAGE:?Set WEB_IMAGE in .env or your shell environment}
    container_name: ${WEB_CONTAINER_NAME:-avmatrix-web}
    ports:
      - '${WEB_HOST_PORT:-4173}:4173'
    depends_on:
      avmatrix-server:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://127.0.0.1:4173/']
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  avmatrix-data:
