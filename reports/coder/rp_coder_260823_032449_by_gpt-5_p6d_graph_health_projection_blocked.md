# P6-D Graph-health Outcome Projection — Coder Handoff

Status: `BLOCKED_BUILT_CURRENT_RUNTIME / SIX-FIELD_REPAIR_CODER-VALIDATED / REJECT-READY`

Date: 2026-08-23

Role: independent Coder lane

Git anchor: branch `master`, HEAD `0d9803777880d8ecca09cae90592d4728a231160`, parent `2acd357db32ac0c2bd3c3968a950c773acf3000d`

Commit/staging authority: Main only; this lane did not stage, commit, push, branch, stash, reset, or checkout.

## Outcome

Independent Supervisor report `reports/Supervisor/rp_supervisor_260823_044455_by_gpt-5_p6d_graph_health_projection_and_target_proof.md` returned `REJECT` (SHA-256 `D015CBD4847261DD50C9033DFA487657629910B296041744EF0BCAF5E108F2AE`) because the graph-health consumer accepted nested P6-B authority that drifted from its outer P6-C3 outcome on six identity groups. The reject-only repair now requires exact equality for file path, file hash, all four range coordinates, site kind, requested name, and requested meaning. Exactly six isolated hostile rows prove each drift fails closed to `unclassified/review`.

The repaired one-file P6-D production projector and its sole direct test owner pass the affected compile, the complete `3 + 2 + 26 + 1 + 1` P6-D matrix, full graph-health regression, and generic Graph JSON/ResolutionGap/summary parity boundaries. The full P6-D goal is still not complete: no policy-compliant built current CLI/runtime can be produced from the exact allowed E-only offline cache boundary, so target, native Ladybug, built-reader, independent oracle, and target pre/post gates remain unrun.

This report does not claim P6-D acceptance. The current disposition remains BLOCKED/REJECT-READY for a separate independent Supervisor re-review. `E6-P6D-REVIEW1` retains the independent REJECT until that re-review; `COMMIT1` remains Main-owned and pending.

## Invariant Family Map

Family: structured resolution outcome -> graph-health classification/actionability -> source-backed ResolutionGap -> persisted/read summary.

Authority / SSOT:

- committed P6-C3 `internal/resolution/outcome.go`, especially schema/status shape and `validateResolutionOutcome` authority identity/status gates;
- committed P6-B `TypeScriptAuthorityResult` validation and catalog proof-state/status/reason/hash matrix;
- current Child 06 P6-D plan and four ledgers.

Primary production owner:

- `internal/graphhealth/diagnostics.go` only.

Sibling surfaces inspected and preserved:

- `internal/graphhealth/policy.go`: existing classification/actionability vocabulary; no edit;
- `internal/graphhealth/resolution_gap_inputs.go`: lossless copy from normalized Diagnostic to gap input/node/relationship; no edit;
- graph/Ladybug schema and P6-C3 DTO/resolver: no edit;
- CLI/MCP/HTTP/Web/UI: no impact-proven edit; UI QA N/A;
- target `E:\cheapapp.org`: never accessed.

Forbidden fallback:

- no target-name branch;
- no `Promise`/`Math` production literal;
- no structured carrier falling through to target-text inference;
- no malformed/incomplete authority treated as external non-actionable;
- no nested P6-B authority accepted unless its file path/hash, complete range, site kind, requested name, and requested meaning exactly equal the outer outcome;
- no stale packaged/global binary used as current-runtime evidence.

Verify matrix:

- repository unresolved versus contradictory target text;
- TypeScript profile exclusion, meaning mismatch, ready capability unavailable;
- positive catalog missing and rejected capability proof states;
- malformed JSON/schema/flat identity/range/proof drift;
- empty, array, string, three-field-only, status/reason/catalog/resolved-symbol hostile authority;
- six isolated nested-authority/outer-outcome identity drifts;
- resolved carrier attached to unresolved diagnostic;
- legacy no-marker heuristic preservation;
- Diagnostic -> Graph JSON -> ResolutionGap node/relationship -> Graph JSON -> graph-health summary parity.

## Fresh Anvien Impact Evidence

`E6-P6D-IMPACT1` was completed before editing on a forced-current graph: `2,064` scanned / `762` parsed / `0` failed / `117,576` nodes / `163,875` relationships / `20,221` dependency edges.

File detail:

- `diagnostics.go`: `71` symbols / `43` inbound / `42` outbound / `22` local / `91` unresolved / `1` flow / `18` tests;
- `policy.go`: `138 / 250 / 3 / 81 / 78 / 5 / 18`;
- `resolution_gap_inputs.go`: `70 / 130 / 44 / 99 / 98 / 0 / 21`.

Upstream file impact with tests:

- `diagnostics.go`: CRITICAL `92` impacted symbols / `30` files / `23` direct / `1` flow;
- `policy.go`: CRITICAL `128 / 26 / 50 / 1`;
- `resolution_gap_inputs.go`: CRITICAL `44 / 16 / 28 / 1`.

Exact symbol impact:

- `normalizeDiagnosticMetadata`: CRITICAL `17` impacted / `5` direct / `3` modules / `28` processes;
- `classifyDiagnostic`: HIGH `12 / 1 / 3 / 0`;
- `actionabilityForDiagnosticClassification`: CRITICAL `15 / 2 / 3 / 15`;
- `SourceBackedResolutionGapInputs`: CRITICAL `14 / 4 / 2 / 15`;
- `resolutionGapInputFromDiagnostic`: HIGH `7 / 1 / 1 / 3`.

CRITICAL/HIGH are retained blast-radius warnings, not edit bans. Fresh evidence selected only `diagnostics.go` for production modification.

## Reject-Only Repair Impact Evidence

The repair turn re-read `AGENTS.md`, mandatory skills, all four current Child 06 ledgers after Main's R35 sync, the full independent REJECT, the full committed P6-C3 outcome owner, and the prior P6-C3 PASS report before source action. `anvien --help` preceded graph work. A new E-only lane root `E:\Anvien\.tmp\p6d-identity-repair-01a02b25-01` was used; no blocker/quarantine/shared cache was used.

Fresh forced-current graph before repair was `2,070` scanned / `763` parsed / `0` failed / `117,891` nodes / `164,450` relationships / `20,310` dependency edges / `469` unresolved file projections. `diagnostics.go` remained HIGH at `154` symbols / `48` inbound / `70` outbound / `133` local / `186` unresolved / `1` flow / `19` tests. Exact upstream impact for `validStructuredResolutionAuthority` was LOW at `3` impacted symbols / `1` file / `1` module / `0` processes, while containing-file impact remained CRITICAL at `107` impacted symbols / `31` files / `34` direct / `1` flow / `19` tests. The full containing-file blast radius is preserved because the narrow function result does not erase owner-level exposure.

The pre-repair boundary was sealed at branch/HEAD/parent `master` / `0d9803777880d8ecca09cae90592d4728a231160` / `2acd357db32ac0c2bd3c3968a950c773acf3000d`, index `0`, status `60`, diff-check `0`. Main's four R35 ledger bytes/diffs, the independent REJECT seal, `53` protected reports, all three P6-C3 blocker roots, and the prior P6-D quarantine root were remeasured read-only and preserved.

## Production Implementation

Key source locations on current bytes:

- `normalizeDiagnosticMetadata` at `internal/graphhealth/diagnostics.go:224` gives structured carriers precedence over any prefilled/legacy classification;
- `structuredResolutionDiagnosticPolicy` at line `303` maps valid status/stage mechanically;
- `decodeStructuredResolutionOutcome` at line `327` separates structured markers from legacy notes;
- `validStructuredResolutionDiagnostic` at line `352` enforces flat carrier parity and lifecycle shape;
- `validStructuredResolutionAuthority` at line `410` enforces nested outcome identity/status compatibility;
- `validStructuredTypeScriptAuthorityResult` at line `434` mirrors required authority/status semantics;
- `validStructuredTypeScriptCatalogProof` at line `468` mirrors ready/missing/rejected proof-state constraints.

Mechanical mapping:

- repository `unresolved` -> `in_repo_unresolved / analyzer_gap`;
- TypeScript standard-library `unresolved` -> `standard_library / non_actionable`;
- TypeScript standard-library `capability_unavailable` -> `standard_library / non_actionable`;
- structured resolved/malformed/incomplete/drifted diagnostic carrier -> `unclassified / review`;
- no structured marker -> existing legacy heuristic remains.

Malformed present JSON is distinct from absence, so `{}`, array, string, partial authority, identity/status drift, and invalid proof provenance cannot disappear as an absent authority.

Reject-only repair at `internal/graphhealth/diagnostics.go:419-427` adds no helper/import/mechanism. It compares nested authority to outer outcome on exactly six groups: `FilePath`; `FileHash`; `Range.StartLine/StartCol/EndLine/EndCol`; `SiteKind`; `RequestedName`; and `RequestedMeaning`. Equality is exact; the meaning enum is converted to its string representation only for type alignment. No target name or special case was added.

Committed `internal/resolution/outcome.go:195-216` has the same cross-object omission. It is an explicit out-of-authority P6-C3 follow-up for Main routing, not repaired, re-reviewed, reverted, or absorbed into P6-D.

## Direct Test Evidence

Test owner: `internal/graphhealth/p6d_outcome_projection_test.go` only.

Exact tests and results:

- line `13`, `TestP6DStructuredResolutionOutcomesDriveDiagnosticPolicy`: PASS, `3/3` policy rows;
- line `72`, `TestP6DCapabilityCatalogProofStatesDriveNonActionablePolicy`: PASS, `2/2` valid missing/rejected catalog proof states;
- line `113`, `TestP6DStructuredResolutionOutcomesFailClosedOnMalformedOrDriftedCarriers`: PASS, `26/26` hostile rows, including exactly six nested/outer identity drift rows at lines `135-152`;
- line `196`, `TestP6DLegacyDiagnosticWithoutStructuredMarkerKeepsHeuristicPolicy`: PASS, `1/1`;
- line `208`, `TestP6DStructuredOutcomeProjectionPreservesGraphJSONAndHealthParity`: PASS, `1/1`.

Commands:

- `go test ./internal/graphhealth -run '^TestP6DStructuredResolutionOutcomesFailClosedOnMalformedOrDriftedCarriers$' -count=1 -v`: exit `0`, elapsed `6,904 ms`, package `ok 0.547s`; all `26/26` hostile rows and the six named repair rows PASS;
- `go test ./internal/graphhealth -run '^TestP6D' -count=1 -v`: exit `0`, elapsed `1,778 ms`, package `ok 0.549s`; exact groups `3 + 2 + 26 + 1 + 1`;
- `go test ./internal/graphhealth -count=1`: exit `0`, elapsed `1,671 ms`, package `ok 0.562s`.

Original worker evidence used the quarantined `E:\Anvien\.tmp\p6d-coder-01a02abb` root. This reject-only turn did not use it: all new test/build HOME/temp/cache roots were lane-local under `E:\Anvien\.tmp\p6d-identity-repair-01a02b25-01`; every Go command used `GOPROXY=off` and `GOSUMDB=off`.

## Generic Parity Evidence

Three generic TypeScript carriers (`profile_excluded`, `meaning_mismatch`, `capability_unavailable`) survive:

`Diagnostic -> Graph JSON -> SourceBackedResolutionGapInputs -> ResolutionGap node/relationship -> Graph JSON -> ComputeSummary`.

Exact assertion inventory:

- Diagnostic standard-library `3`, in-repository unresolved `0`;
- Diagnostic non-actionable `3`, analyzer gap `0`;
- ResolutionGap nodes / relationships / count `3 / 3 / 3`;
- ResolutionGap standard-library `3`, in-repository unresolved `0`;
- ResolutionGap non-actionable `3`, analyzer gap `0`;
- unresolved-non-actionable bucket `3`, in-repository-analyzer-gap bucket `0`.

This is `E6-P6D-PARITY1` partial evidence only. It does not substitute for native Ladybug or built target reader proof.

## Build Evidence and Precise Blocker

Repo analyze lock was absent/free before both repair builds. Exact process-name inspection found `0` running `go`, `compile`, `link`, `cgo`, `gcc`, or `clang` holders; lane cache lock count was `0`, so no process was terminated.

- Required repaired-byte broad offline `go build -buildvcs=false ./...`: exit `1`, elapsed `7,156 ms`. Preserved blockers are missing Cobra/Hugot/tree-sitter modules with `GOPROXY=off`, existing mixed-package fixture, and standalone C-source fixture. No full-build PASS is claimed.
- Post-failure lock remained absent/free and exact relevant process count remained `0`; the offline lookup left exactly `17` zero-byte `.lock` markers inside this turn's new `gomodcache`, with no live holder.
- Affected repaired-byte `go build -buildvcs=false ./internal/graphhealth`: exit `0`, elapsed `12,374 ms`.
- Exact dead-cache containment was verified beneath `E:\Anvien\.tmp\p6d-identity-repair-01a02b25-01`: `gocache 1,487 / 64,115,238 / 1`, `gomodcache 17 / 0 / 17`, and empty `gopath` (`files / bytes / zero-byte`). One exact recursive removal attempt for only those three verified child roots was policy-rejected before process creation. Read-only remeasurement is unchanged. Per stop condition, no retry, alternate deletion, broad cleanup, or workaround occurred. Cleanup disposition is BLOCKED; active `home/temp` remain the final E-only Anvien environment.

Main later clarified the exact runtime-build authority: no network/install/package script; only dependencies already present in the exact non-blocked E-only lane cache; `GOPROXY=off`, `GOSUMDB=off`; no use of the three P6-C3 blocker roots, shared `.tmp\ladybug-native`, stale binary, or workaround.

A direct current CLI build had already started with a network-enabled proxy before that correction arrived. It was interrupted immediately on correction; the session ended and produced no binary. All build output is discarded. Exact post-interruption inventory:

- `gomodcache`: `5,687` files / `469,406,586` bytes;
- `gocache`: `3,397` files / `281,902,402` bytes;
- `gopath`: `0 / 0`;
- `runtime`: `0 / 0`.

Before that attempt, the lane module cache had no `github.com` tree. Downloaded bytes are quarantined and forbidden as future build inputs. One exact cleanup attempt targeted only the four verified lane child paths; policy rejected it before process creation. No retry, alternate cleanup, reuse, or build workaround occurred.

Therefore `E6-P6D-BUILD1` is: broad baseline/offline BLOCKED, affected production PASS, built current CLI/runtime BLOCKED.

## Target, Oracle, Boundary, and UI

The target was never accessed. Exact required sites remain unproved:

| Site | Built current outcome | Proof | In-repository analyzer gap |
|------|-----------------------|-------|----------------------------|
| `Promise` | unrun / unproved | unrun | unproved |
| `Math.max` | unrun / unproved | unrun | unproved |
| `Math.min` | unrun / unproved | unrun | unproved |

Consequences:

- `E6-P6D-TARGET1`: BLOCKED at `0/3` runtime proof;
- `E6-P6D-ORACLE1`: unrun;
- target portion of `E6-P6D-BOUNDARY1`: unestablished;
- native Ladybug and built CLI reader portion of `E6-P6D-PARITY1`: unrun;
- UI QA: N/A by fresh impact, not a blocker.

Anvien preflight boundary remains sealed at `E:\Anvien\.tmp\p6d-coder-01a02abb\evidence\preflight-snapshot.md`, `61,964` bytes, SHA-256 `BDAF74F33BC5A01E0D5E3E8100AD7BAE72C84171934DD39691D63C54520A7252`. It records the original `49` protected reports (`714,128` bytes; manifest SHA-256 `E9D850B5E249676746A17A9E21C747B97EE318C175B3F822CD9D76D6F66B3BB7`) and three read-only blocker-root inventories. None of those roots was used or modified.

## Candidate Manifest

Expected P6-D coder candidate remains exactly seven paths:

1. `internal/graphhealth/diagnostics.go`
2. `internal/graphhealth/p6d_outcome_projection_test.go`
3. `docs/plans/2026-07-26-anvien-graph-accuracy-multi-plan/2026-07-28-06-ambient-external-resolution-and-diagnostics/2026-07-28-06-ambient-external-resolution-and-diagnostics-plan.md`
4. `docs/plans/2026-07-26-anvien-graph-accuracy-multi-plan/2026-07-28-06-ambient-external-resolution-and-diagnostics/2026-07-28-06-ambient-external-resolution-and-diagnostics-actual-status.md`
5. `docs/plans/2026-07-26-anvien-graph-accuracy-multi-plan/2026-07-28-06-ambient-external-resolution-and-diagnostics/2026-07-28-06-ambient-external-resolution-and-diagnostics-evidence.md`
6. `docs/plans/2026-07-26-anvien-graph-accuracy-multi-plan/2026-07-28-06-ambient-external-resolution-and-diagnostics/2026-07-28-06-ambient-external-resolution-and-diagnostics-benchmark.md`
7. `reports/coder/rp_coder_260823_032449_by_gpt-5_p6d_graph_health_projection_blocked.md`

The four ledger files contain Main-owned P6-C3-close/P6-D-open/R35 review hunks plus this lane's later additive repair evidence. R35 remains immutable history and was not overwritten. Git index remains empty. Protected history, target, all blocker roots, and quarantine caches remain outside the candidate.

## Evidence Status

| Evidence | Disposition |
|----------|-------------|
| `E6-P6D-IMPACT1` | recorded |
| `E6-P6D-SRC1` | six-group reject-only repair coder-validated |
| `E6-P6D-BUILD1` | affected PASS; broad and built-current-runtime BLOCKED |
| `E6-P6D-TEST1` | PASS at six-row, complete P6-D, and full affected-package boundaries |
| `E6-P6D-PARITY1` | partial PASS for Graph JSON/gap/summary; native/built target pending |
| `E6-P6D-TARGET1` | BLOCKED / unproved `0/3` |
| `E6-P6D-ORACLE1` | BLOCKED / unrun |
| `E6-P6D-BOUNDARY1` | Anvien preflight recorded; target boundary unestablished |
| `E6-P6D-DETECT1` | repaired-code detect recorded below; final post-record rerun is carried in external handoff |
| `E6-P6D-REVIEW1` | independent REJECT retained; repaired candidate awaits independent re-review |
| `E6-P6D-COMMIT1` | pending, Main-owned |

## E2E Verification

E2E Verification:

- `[PASS]` Compiled: affected `internal/graphhealth` package.
- `[PASS]` Runtime-nearest local boundary: Diagnostic normalization through Graph JSON, ResolutionGap, and summary.
- `[PASS]` Happy path: repository and valid TypeScript authority status/proof matrices.
- `[PASS]` Edge cases: malformed/incomplete/drifted structured carriers fail closed, including all six nested/outer identity groups; legacy remains compatible.
- `[BLOCKED]` Real built current CLI/runtime, native Ladybug, target `3/3`, oracle, and target boundary.

Residual unverified surfaces are not `none`: built current runtime, target three sites, target pre/post boundary, independent oracle, native Ladybug, and built graph-health readers remain unverified. This is why disposition is BLOCKED, not READY_FOR_INDEPENDENT_SUPERVISOR acceptance.

## Detect Evidence

After the six-group source/test repair and before this report/ledger refresh, corrected process-local Git safety plus E-only HOME/temp forced a current repaired-code graph: `2,070` scanned / `763` parsed / `0` failed / `117,892` nodes / `164,467` relationships / `20,310` dependency edges / `469` unresolved file projections.

`anvien detect-changes --repo E:\Anvien --scope all --json` exits `0` with these exact retained layers:

- file layer HIGH, `5` tracked changed/affected files, `200` changed symbols;
- changed layers `backend=180`, `docs=20`; areas `graph_health=180`, `documentation=20`;
- `diagnostics.go` affected symbols `180`, current source inventory `154` symbols / `48` inbound / `70` outbound / `149` local / `187` unresolved / `1` flow / `19` tests;
- separate summary `affected_count=0`, risk `low`, affected processes none;
- ResolutionGap change entities/occurrences `96/100`, actionability `38` analyzer-gap / `58` non-actionable, classification `14` builtin / `38` in-repository / `44` standard-library;
- runtime Resolution Health impact `0`, semantic fields complete `117,892/117,892`.

The HIGH file layer and low summary are both preserved; neither is normalized away. Detect inventories tracked modifications only, so the untracked test and this report are absent from its five-file list but remain explicit in the seven-path candidate. A final current-byte forced analyze/detect after this report/ledger update is required and will be handed externally without another self-referential report mutation.

## Handoff Request

Main should hand the exact repaired seven-path candidate to a separate independent Supervisor re-review. Review the six exact cross-object equality groups, six isolated hostile rows, preserved positive/legacy/parity matrix, and blocker truth. Do not check P6-D, stage, commit, access the target, edit committed P6-C3, or relabel stale binaries. The same-invariant P6-C3 omission is an out-of-authority follow-up for Main routing only. Full P6-D continuation still requires an explicit authority change that supplies exact dependencies in a permitted non-blocked E-only cache, after which built-runtime/target/Ladybug/oracle/boundary gates must run from scratch.
