# Main Orchestration Rotation Handoff — Anvien Graph Accuracy

## Authority

- Outgoing Main: task `01a02a1d-d2d6-7e02-9827-25718ba1d858`.
- Warmed visible successor: task `01a02a49-2d3c-7f20-b842-3d1a543d4320`, host `local`, saved project `E:\Anvien`, environment `local`, not a worktree.
- Successor PRE-TRANSFER ACK turn: `01a02a49-2fa3-7d00-b4dc-c631ed638823`.
- Successor compact cursor after ACK: `d308c5f9-fa74-42f7-9a27-3f8e4b6a696c:1`.
- Exact internal createdAt: `2026-08-22 23:38:45 +07:00`.
- Outgoing Main absolute rotation deadline: `2026-08-22 23:49:30 +07:00`.
- Successor absolute rotation deadline after official transfer: `2026-08-23 00:49:30 +07:00`.
- Successor required next warmup: by `2026-08-23 00:34:30 +07:00`.
- Locked authoritative workspace remains only `E:\Anvien`.
- `C:\`, alternate worktree/checkouts, and `E:\cheapapp.org` remain forbidden. P6-D, graph-health policy/rendering, and target proof remain locked.
- This report must not be edited after external identity measurement.

## Successor Warmup

- The successor task started at `2026-08-22 23:23:54 +07:00` and completed ACK at `23:23:59 +07:00`, before the required `23:34:30 +07:00` warmup target.
- It returned `UNDERSTOOD / WAITING_FOR_OFFICIAL_TRANSFER`, confirmed the E-only boundary and no action, and emitted no tool marker.
- Outgoing Main retains all authority until the exact official transfer follow-up, then terminates immediately.

## Incoming Handoff Verification And Re-anchor

The incoming report was externally verified and read in full:

- `E:\Anvien\reports\Investigation\rp_main_260822_224235_orchestration_rotation_handoff.md`.
- `12,781` bytes / `137` LF / `0` CR / strict UTF-8 without BOM.
- SHA-256 `3CB2AF54FEBB09FBB556D71AD9C5753EF0255D4EAFEF5D737A2AF8DEDFD8755A`.
- Filesystem createdAt `2026-08-22T22:44:30.1954647+07:00`; lastWrite `2026-08-22T22:44:30.1962909+07:00`.

Main then read in full:

- `E:\Anvien\AGENTS.md`;
- `.agents/skills/working-rules/SKILL.md`;
- `.agents/skills/orchestration/SKILL.md` and the visible-session opening template;
- `.agents/skills/planner/SKILL.md`;
- `.agents/skills/supervisor/SKILL.md`;
- all four current Child 06 ledgers through contiguous bounded reads with no remaining renderer truncation.

`E:\Anvien\anvien\bin\anvien.exe --help`, `file-detail --help`, `impact file --help`, and `impact symbol --help` were read before Main-dependent Anvien work. Main did not edit production, tests, or ledgers. It performed only handoff/report/Git identity checks, independently verified the new coupling boundary, routed exact expansion authority to the existing coder, created this protected handoff, and warmed the required visible successor.

## Accepted Campaign State

- P6-A remains committed at `b98131e44932a7bcac17b487ecb2914535927d01`.
- P6-B remains committed at `5c1584fef7153a7a331c8fedd1ce64176ddc873d`.
- P6-C1 remains committed at `8055f0a6860721e26462572e34469e0d708d4a52`.
- P6-C2 remains independently accepted and committed at `2acd357db32ac0c2bd3c3968a950c773acf3000d`, parent `8055f0a6860721e26462572e34469e0d708d4a52`.
- P6-C3 is the sole open slice. Its first candidate has a sealed independent `REJECT`; the same coder task is actively repairing the two rejected production invariants plus the Owner-authorized validation-unblocker.
- P6-C3 remains unchecked, unstaged, and uncommitted. REVIEW1 and COMMIT1 remain pending.
- P6-D, graph-health policy/rendering, target proof, and `E:\cheapapp.org` remain locked.

## Immutable P6-C3 History

Old coder report:

- `E:\Anvien\reports\coder\rp_coder_260822_205432_by_gpt-5_p6c3_structured_resolution_outcomes.md`.
- `18,206` bytes / `254` LF / `0` CR / strict UTF-8 without BOM.
- SHA-256 `9398C3B5B662EB9B7EA1141DA12BAF93D74C80975C91C36AD01DD2B0B5920FC8`.
- Disposition was only `READY_FOR_INDEPENDENT_SUPERVISOR`, never acceptance.

Sealed independent REJECT:

- `E:\Anvien\reports\Supervisor\rp_supervisor_260822_215636_by_gpt-5_p6c3_structured_resolution_outcomes.md`.
- Verdict `REJECT`.
- `22,565` bytes / `199` LF / `0` CR / strict UTF-8 without BOM.
- SHA-256 `50B5CF6CC0CA256DD134EA89BF8F5471C18289E44BA00B965503804044769B55`.
- Filesystem createdAt/lastWrite `2026-08-22T21:59:51.4617830+07:00`.

The two blocking invariants remain exact:

1. Repository/P5 authority must be terminal before intrinsic classification. A legal repository type named `string` was incorrectly classified as `Intrinsic:go#type#string`.
2. Every visited stable site must finalize exactly one outcome independently of optional carrier emission. A local `Math.missing` call with nil TypeScript authority and compatibility-disabled resolved heritage each produced zero outcomes.

The same-worker production repair remains limited to `internal/resolution/resolve.go` and `internal/resolution/emit.go`; its three hostile regressions were locally green before the validation-unblocker. All earlier status/shape, immutable snapshot, duplicate/conflict, P5 proof, Graph JSON/Ladybug CSV, and native Ladybug clearances remain candidate evidence only until independent re-review.

Full repository build, direct `internal/analyze`, and packaged candidate runtime remain truthfully BLOCKED/invalidated under offline missing modules plus repository baseline fixtures. Never infer PASS.

## Post-Seal Owner Correction And First Stop Boundary

Outgoing Main transferred a binding Owner correction: handle the default `internal/resolution` package-test dependency coupling now, with permission to expand the current phase/slice if necessary, but stop before expansion if source proves more than the evidenced two legacy tests or an outside production/parser/shared owner.

The existing coder resumed from cursor `15923456-b532-4f51-acc6-537a810baf21:433`. Its first correction turn ended safely with no test-coupling edit because source-site evidence disproved the two-file assumption:

- direct parser importers: `internal/resolution/resolution_test.go` and `internal/resolution/type_alias_test.go`;
- indirect callers of parser-backed shared helpers: `internal/resolution/binding_accumulator_test.go`, `graph_parity_test.go`, `identity_occurrence_test.go`, and `p3c_binding_occurrence_test.go`;
- actual parser-dependent closure: at least six test files, not two;
- coupling edit count `0`;
- no new coder report, no stage/commit, and no target/P6-D access.

That turn also truthfully disclosed incomplete cleanup: two lane-created repo-local temp roots and sixteen zero-byte module lock markers remained because cleanup policy rejected recursive removal before process creation. No cleanup mutation occurred. The active turn must remove only externally verified exact E-rooted artifacts or report the exact blocker; Main must not broad-clean them.

## Main Independent Six-File Verification

Main independently refreshed the graph on unchanged code bytes:

- `anvien analyze E:\Anvien --force --json` exited `0`.
- `2,058` scanned / `761` parsed code / `0` failed.
- `117,494` nodes / `163,788` relationships.
- `20,205` dependency edges / `469` unresolved file-projection entries.

Source and graph agreed:

- `parseFixtureWorkspace` upstream impact is MEDIUM: `5` impacted / `3` files / `1` module / `0` processes. Exact callers are three same-file tests plus one test each in `binding_accumulator_test.go` and `graph_parity_test.go`.
- `parseTypeScriptSource` upstream impact is MEDIUM: `8` impacted / `3` files / `1` module / `0` processes. Exact callers are three same-file tests, one helper site in `identity_occurrence_test.go`, and four call sites across two tests in `p3c_binding_occurrence_test.go`.
- Four indirect file impacts: `binding_accumulator_test.go` LOW `0`; `graph_parity_test.go` MEDIUM `6` direct / `1` file; `identity_occurrence_test.go` LOW `3` direct / `1` file; `p3c_binding_occurrence_test.go` LOW `3` direct / `1` file.
- `rg` over the full `internal/resolution` subtree found no seventh caller of `parseFixtureWorkspace`, `parseFixtureWorkspaceForBenchmark`, `parseFixtureWorkspaceWithPool`, `parseTypeScriptSource`, or `parseTypeAliasFixture`.
- HIGH/MEDIUM/CRITICAL are blast-radius warnings, not edit bans.

Main therefore applied the Owner's pre-authorization narrowly to the same coder: exactly these six existing test files plus at most one new dedicated parser-integration test/helper owner. Any seventh caller, second new integration owner, production/parser/shared-contract edit, P6-D/graph-health/target access, or coverage deletion/skip/pass-by-default requires another stop before expansion.

## Active Coder Expansion Lane

- Same coder task: `01a02946-f50c-7a63-ae34-a98cee8bac1a`.
- Active expansion turn: `01a02a47-313a-7a82-849f-cfe3bc8e8512`.
- Expansion turn started at `2026-08-22 23:21:44 +07:00`.
- Latest compact cursor consumed before report sealing: `15923456-b532-4f51-acc6-537a810baf21:507`.
- State: `active/inProgress`, no task error, no attention request, no lane deadline.
- Mandatory ACK was consumed: exact six-file closure, maximum one new integration owner, planner update before coupling edit, no production/P6-D/target/Git transition.
- All four Child 06 ledgers were read in full and planner-updated to record the six-file closure, MEDIUM/LOW impacts, one-owner ceiling, and seventh-owner stop condition. P6-C3/REVIEW1/COMMIT1 remain pending.
- The first post-ledger graph refresh was blocked before graph read by isolated-HOME Git safe-directory handling and made no mutation. The retry used process-local `safe.directory=E:/Anvien`, E-rooted HOME/TEMP, and completed its command marker. No visible compact result existed at seal, so do not infer PASS from the marker.
- At the last explicit coder commentary before seal, coupling edit was still `0`; next stated gate was file-detail for all six files and exact symbol impact for every function/type proposed for movement.
- Do not STOP/restart it because Main rotates. Resume only with compact `wait_threads`, never `read_thread`.

The authorized remaining work is exact:

1. Close six-file and moved-symbol file-detail/impact gates on current bytes; reject `not found = zero`.
2. Implement the minimum test-only separation so default offline `go test ./internal/resolution` no longer pulls parser/tree-sitter/Hugot-only dependencies while every parser-dependent assertion remains reachable through one explicit integration boundary.
3. Preserve all production imports/semantics, parser/shared contracts/statuses, and existing coverage; no deletion, skip, or pass-by-default.
4. Clean only exact lane-created E-rooted temp roots and sixteen exact zero-byte markers.
5. Rerun hostile P6-C3, default full resolution package, dedicated integration command, P6-B/P6-C2/P6-C3/persistence/native regressions, truthful full build, final analyze/detect, and exact cleanup/Git boundary.
6. Create exactly one new coder resubmission report only when `READY_FOR_INDEPENDENT_SUPERVISOR`; no stage/commit/self-PASS.

## Current Git And Candidate Checkpoint

At `2026-08-22T23:37:26.8310801+07:00`:

- branch `master`;
- HEAD `2acd357db32ac0c2bd3c3968a950c773acf3000d`;
- parent `8055f0a6860721e26462572e34469e0d708d4a52`;
- status `63 = 12 tracked + 51 untracked`;
- active living candidate `17` paths: five production, eight tests, and four Child 06 ledgers;
- protected history `46 = 37 Main + 6 Supervisor + 3 coder`;
- index empty; `git diff --check` PASS;
- no new P6-C3 coder report exists.

The exact case-sensitive path-sorted 17-file canonical manifest uses `relative-path|bytes|SHA256`, LF-joined without terminal LF:

- canonical bytes `2,474`;
- SHA-256 `57A1A6265F50ABF7C6E49F4D0CC1C02907C7C806B24831E327DEF6EC7D2D60BE`.

The four current ledger identities at that checkpoint are:

- actual status: `51,419` bytes / SHA-256 `47D090B4950371920B104F4D73517050ED97C146A12F48F28CE4BA2281B4476E`;
- benchmark: `19,509` bytes / SHA-256 `C08348EC80C71230A70CE1057ACC4498A656F66EBF0CBAED4AB96440050486D2`;
- evidence: `94,801` bytes / SHA-256 `8380913172FBA8C820AA88B894B6F863540B43BCF8507F3BFF49EC84D5E9BFC2`;
- plan: `73,899` bytes / SHA-256 `C0AB8D0B43AAF6FA181252AA6446787845A4ECA1AE34146D7B8F0CD2C75D036B`.

This is a rotation checkpoint, not a final candidate seal. The active coder may legitimately change the six authorized test files, create at most one new integration owner, update ledgers, clean exact temp artifacts, and create exactly one new coder report after this measurement. Successor must immediately resnapshot.

## Idle Independent Supervisor

- Reuse only Supervisor task `01a029d2-9561-7923-89c1-09ff0f8d9eaf` after a corrected coder `READY_FOR_INDEPENDENT_SUPERVISOR` handoff.
- The prior review turn is complete and the task is idle; prior final compact cursor remains `e16d6c93-bb62-4bae-9c13-44dbcafff6c1:111`.
- Do not create a second Supervisor task and do not impose a lane deadline.
- Give the Supervisor the two rejected invariants, preserved clearances, six-file validation-unblocker authority, exact new report/candidate seals, cleanup disposition, and truthful build/blocker split.

## Exact Successor Ownership

1. Externally verify/read this sealed Main report, then re-read `AGENTS.md`, `working-rules`, `orchestration`, `planner`, `supervisor`, and all four current Child 06 ledgers. Load additional skills only when Main directly uses them.
2. Re-snapshot Git immediately. Resume compact monitoring of coder task `01a02946-f50c-7a63-ae34-a98cee8bac1a` from the latest cursor supplied in the official transfer message; use `wait_threads`, never `read_thread`.
3. Do not restart coder, impose a lane deadline, open a new coder/planner lane, open P6-D, touch graph-health, or access `E:\cheapapp.org`.
4. Intervene only if the coder crosses the six-file/one-new-owner ceiling, edits production/parser/shared contracts, deletes/skips coverage, broad-cleans, self-accepts, stages/commits, or touches P6-D/target.
5. On coder handoff, externally measure/read the single new sealed coder report, verify the full living candidate/report/Git boundary, and confirm the old coder report plus REJECT remain immutable protected history.
6. Only after corrected `READY_FOR_INDEPENDENT_SUPERVISOR`, follow up the same idle Supervisor task. If REJECT, route only concrete residual invariants to the same coder. If PASS, use planner once, run current accepted-byte explicit-path analyze/detect, stage only the exact accepted manifest, and create the isolated P6-C3 commit.
7. P6-D opens only after the isolated P6-C3 commit. Never access `E:\cheapapp.org` earlier.
8. Warm the next Main by `2026-08-23 00:34:30 +07:00` and transfer by `2026-08-23 00:49:30 +07:00` if the campaign remains active.

Owner correction remains binding: coder/Supervisor lanes have no deadlines; only Main rotates.
