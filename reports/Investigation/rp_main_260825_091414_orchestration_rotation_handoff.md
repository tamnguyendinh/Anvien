# Child 06A Visible Main Orchestration Rotation Handoff — 2026-08-25 09:14:14 +07:00

## Authority

- Outgoing Visible Main: `01a0366e-1f78-77d2-bf10-eed74cae9673`.
- Warmed clean successor: `01a036a0-38ab-73a3-9cca-02849eccf4bf`.
- Nominal transfer/hard deadline: `2026-08-25T09:14:14.223+07:00`.
- Sole workspace: `E:\Anvien`, saved project local, no worktree.
- This file is uncommitted/protected. Detached byte count/SHA-256 is supplied in the exact `OFFICIAL AUTHORITY TRANSFER` message after the final write and must be verified by successor exactly once.
- Guard `01a03339-144d-7383-8cd1-72fcd5e5417c` is an independent governance lane. Main must not command, retarget, enforce, request ACK, wait for, or functionally use Guard. Publish only neutral official-transfer facts; Guard independently verifies and acts.

## Mandatory Successor Startup — FULL RAW, Sequential, Through EOF

Read, without summary substitution, in this exact order: `E:\Anvien\AGENTS.md`; `.agents\skills\working-rules\SKILL.md`; `.agents\skills\orchestration\SKILL.md`; `.agents\skills\governance-rule-guard\SKILL.md`; `.agents\skills\planner\SKILL.md`; planner templates `plan.template.md`, `evidence.template.md`, `benchmark.template.md`, `actual-status.template.md`; the current Child 06A plan, evidence, benchmark, actual-status and `plan-rules.md`; then this handoff. Verify this handoff's detached seal exactly once. Re-anchoring restores context only; it must not rerun accepted capture, graph refresh, impact/source/test work, canonical runtime build, full-build validation, or any PASSed gate.

## Exact Product Goal And Exhaustive State Machine

The product goal is to reduce as much as safely possible the elapsed wall time of real `anvien analyze` graph generation on the same accepted workload while preserving exact correctness, graph/output, persistence, readers, determinism, freshness, failure, transaction/temp and publication equivalence.

Execution is exhaustive, not winner-only: maintain the complete top-level parent list; choose the largest unchecked parent; measure the complete child list inside that parent; choose the largest unchecked child; continue that same child after every `KEEP`; only three consecutive full attempts without `KEEP` at one accepted baseline may terminalize that child as `SYSTEM_CHARACTERISTIC`; then take the next-largest child. Check a parent only after every child is terminal/checked and parent/full-pipeline remeasurement exists. Rerank all parents, take the largest remaining unchecked parent, and repeat until every measured parent and child is processed. Never omit a small row.

Every production attempt is: exact parent + complete child list + selected child timing/denominator/cause/owner/full call path -> fresh Visible Architect -> concrete Planner refresh -> Coder production-first/test-second -> canonical full build -> remeasure selected child, parent and end-to-end -> fresh per-attempt Supervisor -> disposition. `KEEP` requires all three elapsed times lower plus Supervisor `PASS`; otherwise restore/retain accepted state, increment the same child's streak, and return through a fresh Architect/Planner. Exactly one final P3-A whole-candidate Supervisor, P3-B cleanup, P3-C detect, exactly one implementation commit, then direct Child 07 handoff.

## Current Cursor And Numeric Authority

- Formal gates: P0-A and P1-A complete; P2-A open at `PARENT_DRILLDOWN_PENDING` on `B1-P1A-OP001 resolution`.
- Optimization throughput remains `0/30` parents and `0` measured child rows. No A001, Architect, Planner attempt refresh, Coder, Supervisor, KEEP, production optimization or implementation commit exists.
- Immutable uninstrumented reference: process/analyzer/phase `605.732722 / 602.5278811 / 587.2385976 s`.
- Carried-instrumentation P2 basis: `618.4358209 / 615.8656873 / 600.8917213 s`.
- Accepted parent reference: `resolution = 544.8094574000 s`, denominator `{"runs":1}`. Cross-capture arithmetic is not speed evidence.
- Benchmark must contain measured numbers, not logs: for all 17 children, rank, elapsed seconds, parent share, denominator, exact file:function:line, full call path; then same-run parent, child sum, residual. JSON/log/report is trace evidence only.
- Code remains locked until the complete non-overlapping 17-row child table, largest-child cause/source owner/full call path, fresh Architect A001 and Planner refresh all exist.

## Completed Segment 03 Input

- Visible lane/thread: `01a033a5-414c-7721-864f-a8f4642d46e3`.
- PASS artifacts: `E:\Anvien\.tmp\child06a_p2a_op001_resolution_source_map_segment03\source_map.json`, `source_map.md`, `validation.json`.
- Concise report: `E:\Anvien\reports\Investigation\rp_child06a_p2a_op001_segment03_source_map.md`.
- Exactly 17 ordered children and source/call-path/denominator/timer boundaries are validated. No child duration exists there.

## Active Segment 05 Measurement Reality At Transfer

- Visible lane/thread/turn: `01a033a5-4ec3-7e42-8946-0ab9172f6088` / `01a03687-5bb0-7643-b2c1-7e2f90c0e6a0`.
- Artifact root: `E:\Anvien\.tmp\child06a_p2a_op001_resolution_slot_segment05`.
- Overlay/harness exists; production `internal/resolution` remains unchanged. Toolchain is exactly `C:\Program Files\Go\bin\go.exe`, `go1.26.3 windows/amd64`; C: is read/execute-only for this installed executable, while every generated temp/cache/binary/log/artifact remains repo-local under the artifact root.
- First build failed before binary/analyze because isolated Git/VCS stamping returned `error obtaining VCS status`; preserve its exact three logs under build-failure history. This is validation evidence, never benchmark data.
- The corrected build mirrored `internal/cli/package_runtime.go::buildGoRuntimePackage`: vendor, `ladybugdb`, trimpath, stripped ldflags, pinned native CGO environment/runtime, plus measurement-only `-overlay`, artifact output and evidenced `-buildvcs=false`. It produced `anvien-op001-resolution.exe` (`98,462,980` bytes).
- One analyze launch occurred at `2026-08-25T09:09:17+07:00` and exited `1` after `1.0645919 s` before analysis: `not a git repository: E:\Anvien; pass --skip-git to index any folder without a .git directory`. `process.json` records `launchCount=1`; `stdout.json` is empty; no benchmark/profile/child metrics/ranking exists. Do not call this measurement or retry it silently.
- Segment 05 was editing `run.ps1` at `09:11:06`; inspect its exact active transcript/files first. The likely missing preparation is the accepted process-local Git safe-directory configuration used by P1-A; verify rather than infer, then issue a new exact exclusive slot only if plan/rules permit. Never add `--skip-git` because that changes the accepted workload contract.
- Required short report path remains `E:\Anvien\reports\Investigation\rp_child06a_p2a_op001_segment05_measurement_slot.md`; it is not a gate and was absent at last snapshot.

## Build/Full-Build/Benchmark Operational Inventory

- Canonical runtime build is the runtime-binary boundary previously measured at `25.6760326 s`; source contract is `buildGoRuntimePackage`.
- `scripts/full-build.ps1` is the full-build validation boundary: stop exact output holders -> canonical runtime build -> launcher assets -> version/hash -> maintenance `analyze . --force`. Its accepted elapsed `700.1712706 s` is validation only, includes maintenance analyze, and is neither build performance nor P1/P2 product benchmark. Do not rerun it for this drill-down.
- Overlay measurement binary must not overwrite `E:\Anvien\anvien\bin\anvien.exe`; it mirrors canonical build flags/env and adds only overlay/artifact-output necessities.
- Product benchmark is the accepted real analyze invocation with benchmark JSON; its elapsed child/parent/E2E numbers go directly into benchmark. Build/test timings remain evidence unless they are the measured product target.
- Graph refresh is required before graph-based commands; do not refresh now because this read-only parent measurement does not use graph commands and prior accepted graph gates remain valid. Before later code editing, perform the required fresh graph/file-detail/impact. Detect runs exactly once in P3-C.

## Tailored Lane Packet Rule — Every Lane

Never give a lane vague context and ask it to infer the job. For every Measurement, Architect, Planner, Coder or Supervisor dispatch, Main creates a role-specific filled execution worksheet: exact goal and product link; phase/slice/cursor; ownership/authority/boundary/non-goals; exact inputs/files/symbols/benchmark rows/baseline; ordered steps; exact commands and artifact paths; exact result fields to fill; PASS/FAIL/BLOCKED checks; stop/completion conditions; next owner and Main decision after handoff. Measurement packets prefill rows/formulas; Architect packets prefill measurements/cause/owner/call path/invariants; Planner packets prefill the Architect decision and concrete implementation matrix; Coder packets prefill exact edit/build/remeasure boundaries; Supervisor packets prefill candidate diff, three before/after measurements and affected invariants.

## Immediate Successor Action And Branches

1. After startup/seal/Git-live inspection, read actual Segment 05 transcript items and current artifact files/processes. Do not repeat old empty-turn delivery loops.
2. If Segment 05 has already produced a valid 17-row measurement, verify numeric arithmetic/non-overlap/output equivalence quickly and update benchmark with all numeric rows, plan with exactly 17 matching nested unchecked items, evidence `E2-P2A-A001DRILL1`, and actual-status in the same transition. Select #1 and bind cause/owner/full call path.
3. If it only reports the Git preflight failure above, preserve it as failed slot evidence. Give the same visible lane a fully filled correction/new-slot packet only after confirming the accepted P1-A Git safe-directory setup; never use `--skip-git`, never change workload, never silently retry, and keep one functional executor.
4. Once complete child proof exists, immediately open fresh Visible Architect A001 with the entire table and selected #1 packet; then Planner refresh. Production Coder stays locked before all four prerequisites.
5. Continue orchestration actively. Owner reminders/questions are not PAUSE; only explicit PAUSE/STOP halts work.

## Git/Protection/Violations

- Snapshot at `2026-08-25T09:11:17.9139334+07:00`: HEAD `8df19258fbcb18e14841bf0dae036400aa9b22a3`; `20` tracked dirty; `0` staged; `7` untracked before this handoff. Preserve every unrelated/protected dirty/untracked item; this handoff adds one more protected untracked file.
- Preserve all prior violations and actual-status R23-R31, including passive wait-only HIGH, repeat HIGH, successor warm lateness `27.777 s` HIGH, prior hard-transfer miss HIGH, invented E-drive executable restriction HIGH, repeated Guard-command HIGH, underspecified/all-lane packet failures, first overlay build failure and incomplete build-contract correction. Do not erase or downgrade any predecessor violation.
- No hidden functional agents; never `fork_thread`; Main is commander, not worker.

## Handoff Completion Condition

Successor receives authority only from the exact official transfer message. Outgoing Main terminates immediately after that message and UI navigation. The successor continues the same Segment 05 measurement/correction cursor; rotation itself is not functional progress.
