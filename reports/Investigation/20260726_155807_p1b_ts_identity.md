# P1-B TypeScript Extraction, Visibility, Scope, and Identity Comparison

Date: 2026-07-26 15:58 +07
Target: `E:\cheapapp.org`
Target HEAD: `a869876ab6262dacde6cd5d432d099a91852a646`
Graph: `E:\cheapapp.org\.anvien\graph.json`
Graph SHA-256: `DD3A3A987E95C91787E7C70EF68BA5D2F953D24A32D35DEFE38B8E8E606FF090`
Classification: bounded `confirmed wrong`; first-divergence ownership is P2-A follow-up

## Question

For three fixed TypeScript files, does Anvien preserve source binding patterns, same-file local identity, and export visibility in the graph?

This is a fresh source/graph comparison. It does not accept prior reports or synthetic fixtures as authority and does not propose a fix.

## Independent source oracle

The oracle used TypeScript `5.9.3` from `E:\Anvien\node_modules`, parsed the target `tsconfig.json` (`888` root files, zero config errors), and walked the three real target source files. The target declares TypeScript `6.0.3` but has no installed compiler; the syntax and declaration facts below are stable across these versions. The machine-readable capture is `E:\Anvien\.tmp\cheapapp-graph-root-cause-restart\p1b-identity-oracle-output.json`.

## Results

| File | Source facts | Graph facts | Bounded result |
|---|---|---|---|
| `modules/email/server/operations/email-operations-observability.ts` | One array binding pattern at lines 503–509 containing six names (`messageRows`, `attemptRows`, `eventRows`, `providerEventRows`, `readinessRows`, `suppressionRows`); two `time` declarations (lines 207, 214); two `now` declarations (lines 262, 501); 17 exported declarations | Six pattern names have zero matching graph definition nodes; one `time` node (line 207); one `now` node (line 262); all 17 export names exist as definitions but zero definitions have `exported`/`visibility` properties | six missing bindings, two same-name locals collapsed, 17 export flags absent |
| `modules/release-distribution/server/release-distribution-publication-state.ts` | Three exported declarations: type alias line 1, `canPublishRelease` line 7, `canRollbackRelease` line 13 | Three matching definitions; zero `exported`/`visibility` properties | three export flags absent |
| `modules/commercial-config/server/admin-commercial-config/read-admin-commercial-config.ts` | One exported function at line 10 | One matching definition; zero `exported`/`visibility` properties | one export flag absent; function identity/range preserved |

Totals for this fixed slice: `6` omitted pattern bindings, `2` collapsed same-name local pairs (`time`, `now`), and `21` source exports whose definition nodes lack an export/visibility property. All `21` exported names are present as definitions; the defect is metadata/identity, not total declaration disappearance.

## Why these are source-to-graph discrepancies

### Destructured bindings

The source AST contains six legal array binding elements. No graph definition node matches any of the six names. This is not a `file-detail` display cap: the email file is parsed, its graph definition inventory contains 80 non-property definitions, and the six names are absent from the raw graph selection.

### Same-file local identity

Source has two distinct `time` declarations (`parseTime` line 207 and `latestIso` line 214) and two distinct `now` declarations (`buildEmailOperationsReport` line 262 and `readEmailOperationsReport` line 501). The graph contains one `Variable:...:time` node at line 207 and one `Variable:...:now` node at line 262. The source ranges are distinct, so this is a bounded identity collision, not a same-line display grouping.

### Export visibility

All 21 source exports are represented by matching graph definitions, but none of the selected definitions has an `exported` or `visibility` property. `file-detail` therefore cannot infer exported status from the graph for these rows. The source declaration itself is present; the missing fact is visibility propagation.

## Evidence

- `E1-P1B-GRAPH1`: fresh graph identity/hash/counts and up-to-date status.
- `E1-P1B-SRC1`: direct target source/AST oracle for array patterns, local declarations, and export declarations.
- `E1-P1B-CMP1`: raw graph comparison: six pattern names absent; one node each for the two `time`/`now` name pairs; all 21 export names present but metadata absent.
- `E1-P1B-REPORT1`: this report.

## Limits

- The fixed slice does not estimate global TypeScript prevalence.
- Object/default/rest/parameter patterns, export lists/aliases/default exports, and nested binding variants are not globally inventoried here.
- This report identifies bounded wrong source-to-graph facts; P2-A must trace the earliest Anvien owner in `internal/providers/tsjs/definitions.go` and graph identity emission before any remediation discussion.
- No target files, target graph, Anvien production source, or tests were edited.
