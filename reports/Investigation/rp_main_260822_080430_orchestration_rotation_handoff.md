# SEALED MAIN ORCHESTRATION ROTATION HANDOFF

## 1. Authority envelope

- Campaign: Anvien Graph Accuracy.
- Outgoing Main: task `01a026c3-fd59-7ee3-8cbd-27db46f6eae1`.
- Designated successor Main: task `01a026f7-6a62-7d53-9d67-8a6f8500597f`, host local, same saved project and authoritative checkout.
- Successor PRE-TRANSFER ACK is complete: `UNDERSTOOD / WAITING_FOR_OFFICIAL_TRANSFER`. It confirmed the sole boundary `E:\Anvien`, the ban on `C:\`, alternate worktrees, and `E:\cheapapp.org`, plus no action before the official follow-up.
- This report is the immutable handoff payload. Authority transfers only when outgoing Main sends the exact phrase `OFFICIAL AUTHORITY TRANSFER` to the designated successor together with this measured report identity.
- Exact internal report createdAt: `2026-08-22 08:04:30 +07:00`.
- Successor absolute rotation deadline: `2026-08-22 09:04:30 +07:00`.

## 2. Locked workspace and safety boundary

- Authoritative checkout and only permitted repository boundary: `E:\Anvien`.
- Do not access `C:\`, any alternate checkout/worktree, or `E:\cheapapp.org`.
- Target `E:\cheapapp.org` remains locked until P6-D.
- Current P6-B repair forbids network access, dependency installation, package-script execution, target access, stage, commit, push, and hidden/internal subagents.
- Preserve every protected untracked Main handoff under `reports/Investigation`. Never edit, delete, stage, or commit them.
- Preserve the P6-B candidate, immutable old coder report, immutable Supervisor REJECT report, current graph/evidence, any later coder resubmission report, and the concurrent external rule drift identified below. Do not overwrite or absorb lane ownership.

## 3. Incoming transfer verification and Main bootstrap

Outgoing Main independently:

- read full root `AGENTS.md`;
- read full `working-rules`, `orchestration`, `planner`, and `supervisor` skills;
- read the orchestration session-opening template;
- ran `anvien --help` before any Anvien use;
- read the full sealed incoming report, all four Child 06 ledgers, immutable Supervisor REJECT report, immutable old coder report, and active coder output;
- verified the incoming report identity and current boundary once, without rerunning valid graph gates solely for rotation.

Verified incoming handoff:

- Path: `E:\Anvien\reports\Investigation\rp_main_260822_071000_orchestration_rotation_handoff.md`.
- Identity: `15,860` bytes / `240` LF / `0` CR / strict UTF-8 without BOM.
- SHA-256: `2758DD70990B8E14F697B1044AF0E335C74F27D177FF942A3A41C33852659FD5`.
- Internal createdAt: `2026-08-22 07:10:00 +07:00`.
- Filesystem createdAt/lastWrite: `2026-08-22T07:11:38.5017612+07:00`.

Incoming current-boundary verification at `2026-08-22T07:13:58.4570471+07:00` proved branch `master`, HEAD `b98131e44932a7bcac17b487ecb2914535927d01`, parent `ec765debff335540c77d409ebb2c9f45e4a0a77d`, `origin/master...HEAD = 0/54`, empty index, and `git diff --check` PASS. Exactly twenty untracked Main handoffs were protected after the incoming report.

Immutable input reports were reverified and read in full:

- Supervisor REJECT: `E:\Anvien\reports\Supervisor\rp_supervisor_260822_062917_by_gpt-5_p6b_typescript_stdlib_authority.md`, `26,041` bytes / `176` LF / SHA-256 `26FCA7B7678980F2B129DCF8EA3DB6345FDD269C260FAF8C5326F1B203416FB9`.
- Old coder report: `E:\Anvien\reports\coder\rp_coder_260822_055455_by_gpt-5_p6b_typescript_stdlib_authority.md`, `16,002` bytes / `197` LF / SHA-256 `3326E2658522F28824C16978CB485D34B74CD9F7CA4F03C994812E81D13D072A`.

## 4. Active plan and acceptance state

- Current plan: Child 06 Ambient and External Resolution.
- Open slice: P6-B only.
- P6-A is accepted and committed at `b98131e44932a7bcac17b487ecb2914535927d01`.
- P6-B remains unchecked, unstaged, uncommitted, and under reject-only repair.
- P6-C1/P6-C2/P6-C3/P6-D remain locked.
- The four living ledgers remain at refresh row R6 and have not yet been updated for the current repair. Coder must update them only after current validation evidence is complete; P6-B must remain unchecked until independent Supervisor PASS.
- Existing Supervisor task `01a02692-ee4d-7cf0-8b1e-600f28d8cfb2` remains IDLE at REJECT cursor `cf3cd797-7db3-42a4-b263-fcbd8b501ff4:74`. Reuse this task for the exact repair resubmission only; do not open another Supervisor.

Exact inherited blockers remain the acceptance questions:

1. lossless immutable per-source-site TypeScript authority results and counter equality;
2. canonical unique semantic identity with two-phase logical identity/envelope hashing;
3. repository/local/import receiver terminality before external member lookup;
4. exact durable ten-row P6-A compiler-vector proof.

## 5. Active coder lane

- Existing coder task: `01a02637-4ac8-7031-9043-fea65333c7b4`, host local.
- Active repair turn: `01a026b3-4e1d-7f33-8a07-8f6c5afd8cad`.
- Latest bounded cursor: `bfa88881-8500-4623-b486-d2af38779a76:422`.
- State at seal preparation: ACTIVE; full `go test ./cmd/... ./internal/... -count=1` regression rerun is in progress after the last resolver-finalizer patch. First cmd packages had passed/no tests; no new failure was yet reported.
- Coder has acknowledged handoff #20 as protected pathname-only. This new report becomes protected handoff #21 and must be communicated pathname-only after authority transfer.
- Do not open another coder. Continue monitoring only this task from the cursor above.

Fresh pre-edit graph evidence from the active turn:

- analyze PASS: `2,009` scanned / `750` parsed code / `0` failed / `111,571` nodes / `156,306` relationships;
- all selected file-detail results were current/stale=false;
- generator MEDIUM `11` impacted symbols / `1` file;
- catalog file CRITICAL `247 / 62`;
- resolution types CRITICAL `65 / 15`;
- indexes CRITICAL `242 / 36`;
- resolve CRITICAL `120 / 44`;
- analyze CRITICAL `102 / 24`;
- `recordTypeScriptLookup` CRITICAL `8 / 4 / 2 / 34`;
- call/access/type symbols each CRITICAL `29 / 12 / 6 / 32`;
- HIGH/CRITICAL were treated as scope warnings, not edit prohibitions.

## 6. Repair implementation and validated gates so far

Coder preserved production-first order: production compile-only passed before test edits. The repair currently implements:

- a P6-B-specific immutable site-record collection propagated through resolution/analyze without opening C2 graph materialization or C3 final cross-stage DTO;
- exact resolved and unavailable proof, stable site identity, aggregate-counter equality, and fail-closed conflicting payload handling;
- deterministic two-phase semantic identity committing to authority, TypeScript version, logical catalog identity, declaration range/library, semantic owner path, name, and meaning;
- unique lane-specific global/member IDs plus loader duplicate/component-drift rejection;
- external member lookup only after a genuinely external receiver; local/repository/import claims are terminal in call and access paths;
- an exact named ten-row P6-A oracle matrix plus built `analyze.Run` observability fixtures.

Validated before the latest finalizer patch:

- production compile-only for `internal/tsstdlib`, `internal/resolution`, and `internal/analyze`: PASS;
- targeted identity/loader tests: PASS;
- resolver P6-B plus legacy P3C sibling: PASS;
- site-record/counter equality, local/repository/import terminality, and genuine external receiver records: PASS;
- exact ten-row matrix: PASS with row-identifying output;
- production `go build ./cmd/... ./internal/...` and both launcher modules: PASS;
- packaged destination build eventually PASSed after exact Restart Manager holder termination under Rule 13;
- packaged real fixture: `2/1/0` files, graph `8/6`, counters `6 resolved / 0 unavailable / 0 excluded / 0 mismatch`; the unrelated callback-local repository gap was not used as proof;
- broad `go build ./...` still fails only intentional non-buildable fixtures and is not called PASS;
- full regression retained the known C#/Dart baselines and exposed three CLI environment-limit failures caused by mandatory repo-local temp inheriting authoritative root `.anvien`; `internal/repo` passed with Git ceiling, and the authoritative root index was not moved/deleted to force CLI tests green.

Current artifact identity:

- Catalog: `2,003,050` bytes, SHA-256 `F188D15A5D91925DF3E724CBAB97964813E3F6DFD9DF7408FDC7B92EA4CEA487`.
- Catalog logical hash: `dca7af22ff26510cf9075fcb587ab76649bf169edcb0001e50c234d89c9dbb0b`.
- Inventory: `2,030` symbols / `11,802` direct member rows / `14,587` semantic IDs; `14,587` unique, `0` duplicate, `0` format mismatch.
- Current packaged binary after the last finalizer patch: `73,605,632` bytes, SHA-256 `5BCE56084C58510FE120A8014587EA70E49B5A8C296BC5D038976664C9CEE9AA`, Go `1.26.3`, `ladybugdb`, dirty anchor basis.
- Compared with the rejected candidate, catalog delta is `+614,341` bytes (`+44.2383%`); the pre-finalizer repaired binary was `+642,560` bytes (`+0.8807%`) before the final `+1,536`-byte resolver rebuild.

Current benchmarks, no Owner threshold:

- embedded load: `152.147–160.025 ms/op`, `36,393,174–36,451,851 B/op`, `291,812–291,818 allocs/op`;
- global lookup: `250.3–277.2 ns/op`, `192 B/op`, `1 alloc/op`;
- inherited member lookup: `642.8–654.3 ns/op`, `216 B/op`, `4 allocs/op`.

The latest final self-index found one real duplicate source-site blocker at `anvien-web/e2e/graph-orientation-labels.spec.ts#type-reference#Record#295#26#295#32`. Root cause was identical duplicate facts reaching the same stable site. The authorized finalizer now canonicalizes only byte-for-byte/equivalent identical payloads to one immutable record/counter; the same ID with different payload remains fail-closed. Direct regression for this behavior and all `internal/resolution` plus `internal/analyze` passed. This production edit invalidated the final self-index/detect sequence, which has not yet been rerun.

## 7. First uncompleted gates for successor

Continue from the active full regression; do not restart passed gates mechanically.

1. Receive the terminal result of the current full regression and preserve exact classifications.
2. Rerun the packaged real fixture only if needed to prove the finalizer byte at the real boundary; do not open a new CLI projection owner.
3. Run one final authoritative `anvien analyze --force` after the finalizer patch. The prior final analyze failed on the duplicate site and is not final evidence.
4. Only after analyze PASS, run explicit-path `anvien detect-changes --repo E:\Anvien --scope all` and retain all counts/warnings.
5. Cleanup exactly the build/debug artifacts, including both wrapper executables and `.tmp\p6b-held-runtime\anvien-before-site-dedupe.exe`, plus fixture `.anvien`/benchmark/debug paths if present. Do not broad-clean.
6. Use planner to update all four Child 06 ledgers to the real results, including the exact ten-row denominator, unique-ID inventory, site-record canonicalization, build/runtime/regression limitations, benchmarks, final graph/detect counts, and cleanup. Keep P6-B unchecked.
7. Seal exactly one new immutable coder resubmission report and measure path/bytes/LF/CR/strict UTF-8/BOM/SHA-256/createdAt.
8. Main must independently verify report identity, exact source/diff/catalog/site records/receiver regressions/ten-row matrix/build/runtime/benchmark/graph/detect/cleanup/report-only boundary before routing to the existing Supervisor.
9. Supervisor REJECT returns only exact blockers to the same coder. Supervisor PASS permits planner finalization, required post-verdict detect, exact staging excluding every Main handoff/external drift, and isolated P6-B commit.

## 8. Bounded Git/worktree snapshot

Snapshot time: `2026-08-22T08:03:38.8507039+07:00`.

- Branch: `master`.
- HEAD: `b98131e44932a7bcac17b487ecb2914535927d01`.
- Parent: `ec765debff335540c77d409ebb2c9f45e4a0a77d`.
- `origin/master...HEAD`: left/right `0/54` (ahead 54, behind 0).
- Index: empty.
- `git diff --check`: PASS.
- Exactly nine tracked modified paths at this snapshot: four living ledgers, four P6-B production files, and one concurrent external rule drift.
- P6-B production numstat: analyze `25+/5-`, indexes `4+/0-`, resolve `330+/2-`, resolution types `38+/3-`.
- Ledger numstat remained the R6 state: actual-status `17+/8-`, benchmark `20+/15-`, evidence `72+/11-`, plan `20+/9-`.
- Concurrent external/user-owned tracked drift: `internal/aicontext/skills/orchestration/SKILL.md`, `1+/0-`, adding the rule to initialize the new visible successor fifteen minutes before rotation. It is outside P6-B, must be preserved, and must never be staged in the P6-B commit. Attribution to coder was not established.
- Status contained exactly twenty protected untracked Main handoffs, one immutable Supervisor report, one immutable old coder report, and thirty non-report entries.
- The thirty non-report entries were nine tracked modifications plus twenty-one untracked paths: nineteen authorized P6-B source/test/fixture assets and two wrapper executables pending cleanup.
- Creation of this report adds protected Main handoff #21. Expected status count increases by one unless the active coder completes authorized cleanup or creates the new resubmission report after this bounded snapshot.

## 9. Visible lane inventory

### Active

- P6-B reject-only coder task `01a02637-4ac8-7031-9043-fea65333c7b4`, active turn `01a026b3-4e1d-7f33-8a07-8f6c5afd8cad`, bounded cursor `bfa88881-8500-4623-b486-d2af38779a76:422`.

### Idle, reuse only when exact workflow requires it

- P6-B Independent Supervisor task `01a02692-ee4d-7cf0-8b1e-600f28d8cfb2`, REJECT cursor `cf3cd797-7db3-42a4-b263-fcbd8b501ff4:74`.

### Waiting successor

- New Main task `01a026f7-6a62-7d53-9d67-8a6f8500597f`, PRE-TRANSFER ACK complete, `WAITING_FOR_OFFICIAL_TRANSFER` until exact official follow-up.

## 10. Non-negotiable acceptance rules

- Reports and coder claims are evidence inputs, never self-validating acceptance.
- Independent P6-B Supervisor PASS is mandatory before Main acceptance or commit.
- Source inspection precedes trust in build/test/report claims.
- Fresh graph precedes graph-based work; do not rerun still-valid gates solely after rotation/compaction.
- CRITICAL/HIGH impact is a scope warning requiring narrow work and regression, not an edit prohibition.
- Never convert broad fixture build failure, CLI repo-local-temp limitation, sanitized-PATH loader failure, or package-script prohibition into a PASS.
- Never stage protected Main handoffs, immutable reports outside the accepted manifest, concurrent external rule drift, cleanup artifacts, or an unaccepted candidate.
- Never access `E:\cheapapp.org` before P6-D.

## 11. Transfer terminal condition

Once outgoing Main sends the designated successor an official follow-up containing exact `OFFICIAL AUTHORITY TRANSFER` plus this report's measured identity, outgoing Main terminates immediately. The successor becomes sole Main authority and owns coder monitoring plus the next rotation deadline.
