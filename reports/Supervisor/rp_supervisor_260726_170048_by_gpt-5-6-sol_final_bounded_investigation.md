# Supervisor Report: Restarted cheapapp graph root-cause investigation

Verdict: PASS

## Metadata

- Report file: `rp_supervisor_260726_170048_by_gpt-5-6-sol_final_bounded_investigation.md`
- Review time: `260726 170048 +07`
- Reviewer: `gpt-5-6-sol`
- Repo/project: Anvien investigation against `E:\cheapapp.org`
- Scope reviewed: the complete current four-file plan set, P3-A synthesis, P1/P2 slice reports, current raw probes, current target boundary, and current Anvien source/index
- Claim reviewed: the restarted investigation record is internally consistent and acceptable as bounded causal evidence for the fixed graph discrepancies, with unresolved global/product boundaries preserved
- Authority used: current user request, `E:\Anvien\AGENTS.md`, target source/graph state, current plan/ledger artifacts, direct source/probe evidence, and current Anvien self-graph evidence
- Related artifacts: `reports/Supervisor/260726_165317_graph_root_cause_causal_synthesis.md`, P2-A/P2-B/P2-C Supervisor reports, corrected plan ledgers, and the target-boundary manifest

## Executive Summary

- Problem: decide whether the restarted, context-isolated investigation can be accepted as a durable bounded causal record without silently converting fixed-case evidence into a global graph-accuracy or remediation claim.
- Decision: PASS for the bounded investigation record. The ten-row causal matrix is traceable, the P2-A/P2-B/P2-C verdicts and corrected process measurements are represented consistently, the target/graph boundary is preserved, and unresolved authority/global questions remain explicit.
- Required outcome: accept the record as `accepted-bounded` evidence only. This verdict does not approve a fix, claim global accuracy, or close product-semantic/process questions.

## Source-Level Clearance Notes

- `E:\cheapapp.org` target source and graph: clear for the fixed observations. HEAD, graph hash/inventory, and the three P2-A source hashes match the current boundary evidence; no target source was edited.
- Scanner owner group (`internal/ignore/constants.go`, `internal/ignore/matcher.go`, `internal/scanner/scan.go`): clear for the eight-path omission mechanism. The source trace and real-target ignore probe show pruning before File candidacy; the claim remains bounded.
- TypeScript identity group (`internal/providers/tsjs/definitions.go`, `internal/scopeir/facts.go`, `internal/resolution/indexes.go`, `internal/graph/types.go`, `internal/resolution/emit.go`): clear for the three fixed files. The independent AST/ScopeIR reruns reproduce array-binding loss, two identity collision groups, and 21 missing visibility flags.
- Resolver group (`internal/analyze/analyze.go`, `internal/resolution/indexes.go`, `internal/resolution/resolve.go`, `internal/providers/tsjs/*`, `internal/graphhealth/diagnostics.go`): clear for the fixed ambient and barrel controls. The real-source differential and TypeScript oracle isolate the two first-divergence families.
- Projection/command group (`internal/mcp/*`, `internal/filecontext/*`, `internal/lbugload/csv.go`, `internal/lbugnative/native_ladybugdb.go`, `internal/processes/processes.go`): clear for the stated bounded traversal, shape, representation, and no-change control observations; no completeness contract is inferred.
- Plan/ledger/report artifacts under `E:\Anvien`: clear after the R10/R11 refresh. Historical rejected/pending snapshots are labeled as historical and do not override current PASS records.

## Evidence Checked

Passed:

- Current target boundary: HEAD `a869876ab6262dacde6cd5d432d099a91852a646`; graph SHA-256 `DD3A3A987E95C91787E7C70EF68BA5D2F953D24A32D35DEFE38B8E8E606FF090`; `84,807` nodes / `114,125` relationships; fixed target source hashes unchanged; ten pre-existing Git status entries preserved; `p0-rc-c-fixture` absent.
- Target-specific artifact boundary manifest `E:\Anvien\.tmp\cheapapp-graph-root-cause-restart\p3a-target-boundary.json`: no plan-specific `reports/Investigation`, plan synthesis, or `cheapapp-graph-root-cause-restart` paths in the target; the expected repo-local `.anvien` graph remains the operational output. Existing target `.tmp`/reports content is preserved as pre-existing user state, not classified as this plan's output.
- P1/P2 causal evidence: P1-A/P1-B/P1-C/P1-D/P1-E reports and the independently reviewed P2-A/P2-B/P2-C reports are present, current, and bounded. `E2-P2A-REVIEW1`, `E2-P2B-REVIEW1`, and `E2-P2C-REVIEW2` each point to a current PASS report.
- Fresh P2-A identity reruns: six missing pattern bindings, two ScopeIR collision groups, graph `time:207`/`now:262`, 21 represented exports with zero visibility flags; target graph hash unchanged.
- Fresh P2-B differential: actual `imports=10, importUses=1, resolvedCalls=37, unresolved=542`; direct-declaration control `10, 3, 39, 540`; TypeScript ambient/barrel oracles have zero selected-line diagnostics.
- Fresh P2-C differential: actual `3,771/662/2,761/0/0` versus +2-call control `3,773/662/2,761/0/0`; independent parity rerun reports seven of seven canonical facts as one raw node, one edge, one Cypher row, and one `file-detail` sample.
- Fresh targeted validation: `go test -count=1 ./internal/mcp ./internal/filecontext ./internal/lbugload` passed (`7.504s`, `0.753s`, `1.489s` package durations). These tests support the traced command/projection mechanisms; they are not global graph-accuracy evidence.
- Current Anvien production-source diff: `git diff --exit-code -- internal` returned exit `0`; no production source, test, target graph, or target index edit was made by this investigation.
- Evidence integrity: all 44 evidence IDs referenced by the synthesis are present in the current evidence ledger; the stale active pending-review phrases and old P2-C differential were removed or explicitly marked historical/superseded.

Failed:

- None within the bounded investigation acceptance claim.

Not run:

- No full production build, global graph/TypeScript accuracy audit, public-contract decision, product-SPEC semantic review, remediation change, `anvien detect-changes`, or commit. These are outside this read-only investigation and are not needed to accept the bounded evidence record.

## Invariant Closure

- affected invariant: a source-to-graph discrepancy must remain traceable to the real target and an earliest Anvien boundary, while downstream representation/command observations must not be mistaken for upstream source truth or global completeness.
- sibling surfaces checked: target Git/hash/source state; repo-local target graph; scanner, extractor, ScopeIR, identity, resolver, graph-health, command, file-detail, database/native, and process source; raw graph probes; compiler/IR controls; all four plan ledgers; historical rejection and current PASS reports.
- residual unverified same-invariant surfaces: broader language constructs and repository prevalence, ambient/re-export policy outside the fixed cases, global process/community/semantic completeness, nullable/type-preserving public contract requirements, and remediation correctness remain explicitly unresolved or authority-blocked.

## Overall Evaluation

The record is acceptable as a bounded investigation artifact. It separates six confirmed wrong upstream causes from bounded-valid downstream traversal and bounded representation changes, preserves exact measured counts and source ranges, and records the limits of heuristic process output. The target was not contaminated, no production edit is hidden behind the review, and no global accuracy or remediation claim is implied.

## Closure addendum (2026-07-26 17:00 +07)

After this PASS was recorded, the administrative closure steps were completed without changing substantive evidence: Pn-A/Pn-B/Pn-C are marked complete, no dead plan-created artifact was removed, and the plan status is `accepted-bounded`. Historical reports and raw probes remain preserved for traceability. This addendum does not broaden the verdict.
