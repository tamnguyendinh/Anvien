# Child 02 P2-E consolidated validation matrix

- Scope: validation-only on `master` / `35939e7e6a621593d3d3065b9493a97c2c9a4f25`.
- Evidence groups: `E2-P2E-BUILD1`, `E2-P2E-PARITY1`, `E2-P2E-READERS1`, `E2-P2E-REPEAT1` all have validation verdict `PASS`.
- This is a QA evidence disposition, not an acceptance verdict.
- Denominators: persistence `8/8`; affected readers `8/8`; C04/C17 repeat boundaries accounted; C01-C17 `17/17` accounted.

## Matrix

| Row | Scenario | Nearest boundary | Observed | QA validation |
|---|---|---|---|---|
| C01 | Graph JSON Definition properties | field parity | `36,611/36,611`; missing/extra/mismatch/drop `0/0/0/0`; SelectionRange partial `0`; zero retained | PASS |
| C02 | Ladybug Definition CSV projection | native record parity | all 13 fields match by opaque ID | PASS |
| C03 | Ladybug Definition schema | native readback | missing construct coordinates `0`; partial selection `0` | PASS |
| C04 | normal analyze persistence/failure/recovery | parity + repeat M1/M2/M3/M6/M7 | unchanged/changed/failure/recovery and exact facts are truthful | PASS |
| C05 | embedding label write | tagged local-native persistence/hydration | explicit label persisted/read back; remote VECTOR attempts `0` | PASS |
| C06 | embedding schema | tagged native schema/readback | explicit label retained | PASS |
| C07 | DEFINES CSV endpoints | exact pair parity | `36,611/36,611`; missing/extra `0/0` | PASS |
| C08 | relation schema endpoints | native endpoint readback | missing graph/DEFINES endpoints `0/0` | PASS |
| C09 | exact opaque ID | focused unit + headed mounted runtime | only exact ID cyan-active | PASS |
| C10 | unique grounding fail-closed | focused unit + headed mounted runtime | ambiguous citations/panel `0/0`; one unique FUNCTION citation | PASS |
| C11 | line/column/end semantics | focused unit + headed Code Inspector | API `startLine=0`; scroll line `10`; lines 10-11 highlighted, 12 excluded | PASS |
| C12 | nodeRange | `internal/filecontext` | command exit `0` | PASS |
| C13 | Definition MCP context | `internal/mcp` | command exit `0` | PASS |
| C14 | detectChangedSymbols | `internal/mcp` | command exit `0` | PASS |
| C15 | collectRenameChanges | `internal/mcp` | command exit `0` | PASS |
| C16 | explicit label hydration | embeddings/HTTP + tagged local native | all commands exit `0`; remote VECTOR attempts `0` | PASS |
| C17 | current read/fail-closed fallback | repeat M4/M5/M6 + source classification | Ladybug current read; no-backend non-success/no stale result; recovery | PASS |

## Exact parity accounting

- Compared fields: `id`, `label`, `name`, `filePath`, `qualifiedName`, construct `startLine/startCol/endLine/endCol`, and optional SelectionRange `selectionStartLine/selectionStartCol/selectionEndLine/selectionEndCol`.
- Definition records: Graph JSON/Ladybug `36,611/36,611`.
- Missing/extra/field mismatch/drop: `0/0/0/0`.
- SelectionRange present/absent/partial: `4,941/31,670/0`.
- Real zero `startCol`: `5,650`; absence is read as NULL.
- Exact DEFINES pairs: `36,611/36,611`; missing/extra/pair-count mismatch `0/0/0`; missing graph/DEFINES endpoints `0/0`.
- Normal artifact embedding rows: `0`; C05/C06/C16 explicit-label proof comes from fresh repository-local tagged native persistence/readback/hydration.

## Runtime and visual evidence

- Built runtime health was `200/200` before headed Playwright. The visible in-app Browser mounted the build and began the approximately 315 MB graph load; two Browser MCP waits timed out/reset. This is classified as a tool/large-load limitation, not a product failure, and the same wait was not called again.
- Headed Playwright: C09-C11 `3/3`; product console/page/network errors `0/0/0`. The exact SwiftShader `GPU stall due to ReadPixels` warning is retained as a non-blocking browser diagnostic.
- All six final screenshots were opened at original resolution and visually inspected. No clipping, overlap, broken font/asset, disappearing control or layout shift was observed. Per-screenshot observations and hashes are in the paired JSON.

## Repeat and failure classification

- M1-M7: `7/7` PASS on fresh binary SHA-256 `14DEB1820B58E4BBE68E5C8B542D09231CFAB49FA73521BC0163DA754588606B`.
- Semantic determinism uses exact identities/facts/ranges/endpoints; whole-Ladybug byte identity is explicitly not claimed.
- The tagged production runtime dynamically proves native success, non-sentinel native failure with JSON-RPC non-success/no stale result, and recovery.
- Exact `ErrUnavailable` is a compile-time capability sentinel. Only `errors.Is(openErr, ErrUnavailable)` permits same-repository Graph JSON fallback; no dev/untagged binary or fake sentinel was manufactured.

## Clean-holder build and containment

- One full build, zero retry. Verified global `anvien.exe mcp` holders PIDs `15412`, `18544`, `24380`; verified related `cmd.exe` launcher PID `13688`. The three live holders were stopped; the launcher exited with them; unrelated processes killed `0`.
- Restart Manager reported zero holders and exclusive-open succeeded for all six build artifacts before build.
- Full build command exit `0`, `116.702s`; this timing is validation evidence, not a product benchmark.
- Runtime PIDs `14132` and `12256` were stopped from the P2-E manifest; ports `4848/5228` have zero listeners.
- Lane temp `.tmp/qa-child02-p2e` was removed; `.tmp/ladybug-home`, `.tmp/ladybug-native`, `.tmp/runtime-p2c` remain.
- All five main-owned plan-document hashes remain byte-identical to entry state. No production/test/plan/accepted-evidence/target-source repair was made.

Full outputs, exits, UTC intervals, artifact hashes and detailed comparison payloads are retained in the JSON evidence pairs referenced by the paired consolidated JSON.

Handoff: supervisor — independently review the P2-E evidence and decide acceptance.
